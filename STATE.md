# Beakn HVA — Current State

**Last updated:** 2026-06-06 (HVA-246 — pagination + sortable column headers on all 4 support pages: Pending / In-progress / Orders / Activity, 25 per page, URL-driven `?sort=` + `?dir=` + `?page=`)

This file captures what's live, what's next, what's blocked, what's deferred. Update after every PR merge.

---

## Phase 1 — Live on prod

### Customer-facing
None yet. Customers raise requests via beakn.in main site, not via HVA. HVA is internal-only.

### Captain portal (13 pages)
- Dashboard with team performance metrics, pending approvals, pending collections (HVA-80, HVA-168)
- Requests tab with search + pagination + filter-by-exec (HVA-127, HVA-153)
- My Team list with exec drill-down — calendar, day plan, report cards, weekly view (HVA-154, HVA-167)
- Contacts page with unified card UI (HVA-73)
- Mobile hamburger drawer (HVA-152)
- Captain approval gate — exec can't bypass to Order Executed (HVA-137)
- Edit access on contacts, requests, team (HVA-163; no delete anywhere)
- Approvals queue, Collections queue, Unassigned requests bucket

### Sales exec portal (11 pages)
- Day plan: Start My Day → visit tasks → close day workflow (HVA-60, HVA-64)
- Dashboard with pending tasks (incl. rolled-over), postponed, completed today (HVA-169)
- Tasks page with Pending / Postponed / Completed accordion (HVA-170)
- Tasks: move semantics for Pending+Postponed, clone for Completed (HVA-170-FIX1)
- Requests tab with 5 buckets, mirrors captain pattern (HVA-65)
- Request detail with customer info + status timeline (HVA-66)
- Contacts/Leads with unified form (Customer/Business toggle) (HVA-73)
- Mobile hamburger drawer mirrors captain pattern; 8 items: Dashboard / Today / Tasks / Contacts / Requests / Resources / Announcements / Profile (HVA-51, HVA-156)
- Resources surface (admin-published sales scripts / pricing / brand assets / training, grouped by category) (HVA-156)
- Announcements surface (admin broadcasts with severity badges + per-user unread badge on drawer) (HVA-156)
- Edit access on contacts, requests, tasks (HVA-159; no delete anywhere)
- Calendar picker for future-dated tasks (HVA-157)
- Auto-task creation on captain assignment (HVA-158)

### Admin portal (14 pages — fully responsive as of HVA-117)
- Dashboard (3-column layout, collapses to 1-col on mobile)
- Operations: Admin Help Inbox / Other-city Queue / Assist Requests (list + detail) / Leaderboard
- Settings → Organization: Cities CRUD / Captains CRUD with reset-password / Executives CRUD
- Settings → Audit & Content: Resources / Resource Categories / Announcements / Announcement Categories
- Settings → Notifications: Customer Support Phone
- Settings → Workflow: Holidays
- Shell: persistent left sidebar at lg+, mobile drawer + hamburger topbar below lg (HVA-117)

### Public-facing pages
- Customer visit-request form at /request (HVA-34, HVA-35)
- Customer tracking at /track/[token]
- Login
- Set-password (first-login pin, HVA-26)

### Cross-cutting
- Nightly task roll-over cron at 21:31 IST (HVA-169)
- Audit log on status changes (per `config.audit_enabled_events`)
- Status timeline visible on request detail
- Three-layer auth defence (proxy.ts + layout gates + per-action authorize)
- Better-Auth phone+password sessions with 5-attempt rate limit
- Email via SMTP (nodemailer); WhatsApp channel adapter exists but stub until provider activates

---

## Recently shipped (most recent first)

| Date | Ticket | Summary |
|---|---|---|
| 2026-06-06 | HVA-246 (HVA-245-FIX2): Pagination + sortable column headers on all 4 support pages | **Walk-feedback from Sandeep after HVA-245.** "Pagination + sort by customer / product / date — all support pages." Memory `list-pages-need-filter-search-pagination` already says every list page needs filter + search + pagination as universal UX policy; Orders shipped with all three but Pending / In-progress / Activity didn't paginate and none of the 4 had sortable column headers. **Sort columns per page (locked in discussion 2026-06-06)**: Pending + In-progress get **customer / product / order age** sort keys (matches the data shape); Orders gets **customer / state / last activity** (sortable header on each); Activity gets **date / customer**. Page size **25** everywhere. URL contract `?sort=<key>&dir=asc|desc&page=N` shared across all 4. **New shared components** under `app/(support)/_components/`: `SortableColumnHeader` (clickable header with ↕ idle / ↑ asc / ↓ desc indicator, cycles asc → desc → cleared on each click, pushes URL params via `router.push` inside `startTransition`, resets `?page=` to 1 on sort change) and `Pagination` (Previous + page indicator "X–Y of N · page A of B" + Next; renders just the row count when totalCount ≤ pageSize). **`loadDispatchQueue` extended**: return type changed from `QueueRow[]` to `QueueResult = { rows, totalCount, page, pageSize }`; new `sort` enum (`customer` | `product` | `age`) + `dir` + `page` + `pageSize` options; user-driven sort completely replaces the default priority/target/created ordering (the original is the *default* when no sort is requested); separate `COUNT(*)` query for totalCount since the main query gets LIMIT/OFFSET. **`loadAllOrders` extended**: new `sort` enum (`customer` | `state` | `activity`) + `dir`; state sort uses a SQL `CASE WHEN ... END` rank (0=pending, 1=in_progress, 2=done) so asc puts the most-needs-attention orders first. **`loadActivityFeed` extended**: now returns `ActivityFeedResult` shape; date sort happens in SQL, customer sort fetches a capped window (500 rows) ordered by date desc then sorts client-side (cheaper than joining visit_requests for ORDER BY since the page is short anyway). **Activity page** now renders either the grouped Today/Yesterday/DD-MMM view (when `sort=date` or unsorted) OR a flat list (when `sort=customer`). **All 2 callers + 4 tests updated** for the new return shape — `dispatch-action.test.ts` and `orders-queries.test.ts` destructure `{ rows: queue } = await loadDispatchQueue()` now. **5 new tests in orders-queries.test.ts**: customer-sort orders alphabetically, page+pageSize slice correctly + no overlap between pages, totalCount returns regardless of pagination. **Pre-PR gate**: tsc clean, next build clean, **1003/1003 vitest pass** (5 new). |
| 2026-06-06 | HVA-245-FIX1: /support/orders 500 — postgres-js returns timestamp strings from raw SQL MAX() | **Walk-bug from HVA-245.** Clicking the Orders tab returned a 500 with `TypeError: a.lastActivityAt.toISOString is not a function`. **Root cause**: the `loadAllOrders` query uses a raw SQL correlated subquery `(SELECT MAX(dsh.changed_at) FROM dispatch_status_history ...)` to compute last activity. Drizzle's `sql<Date | null>` annotation is a TYPE-LEVEL claim only — at runtime postgres-js returns the timestamp as a `string` (not a Date) because the value comes from a raw SQL expression that Drizzle's row-mapper can't introspect. The page mapper called `r.lastActivityAt.toISOString()` which crashed. **Fix**: changed the field's type annotation to `sql<string \| null>` (matches runtime) and wrap with `new Date(r.lastDispatchAt)` in the result mapper. **Regression test added**: tests/support/orders-queries.test.ts now asserts `lastActivityAt instanceof Date` AND `typeof .toISOString === 'function'` for every returned row — this exact bug can't recur silently. **Pre-PR gate**: tsc clean, next build clean, 6/6 orders-queries tests pass. |
| 2026-06-06 | HVA-245: Support nav v2 — Pending / In-progress / Orders / Activity tabs filled | **Walk-feedback from Sandeep after HVA-242 ship**: "orders tab and activity tab both are showing empty. If possible, I also need a pending tab to show all the pending items clearly." Nav went from 3 tabs (Queue / Orders-placeholder / Activity-placeholder) to **4 tabs** with concrete content: **Pending** (renamed from Queue), **In-progress** (new), **Orders** (placeholder filled), **Activity** (placeholder filled). **Pending** = line items where `qty_dispatched = 0` (no dispatch row yet) — what support hasn't touched. **In-progress** = items with at least 1 dispatch row AND `(qty_remaining > 0 OR any dispatch not yet handed_off)` — partial dispatches + mid-flight courier handoffs. Both reuse the existing `SupportQueueTable` (same dispatch flow). Implemented via a new `mode` option on `loadDispatchQueue` with three values: `all` (legacy backwards-compat) / `pending` / `in_progress`. **Orders** = new `lib/support/orders-queries.ts > loadAllOrders` — every ORDER_CONFIRMED+ visit_request with a computed `dispatchState` pill (pending / in_progress / done) rolled up from its line items, sorted by last activity (latest dispatch_status_history.changed_at, fallback to order createdAt), paginated 50 per page, searchable by customer name / phone / city via ILIKE. New `OrdersListTable` client component with debounced search + URL-driven `?q=` + `?page=N` + Previous/Next pagination buttons. **Activity** = `loadActivityFeed` returns the last 200 dispatch_status_history rows joined with author + items, page server-groups by IST date and renders Today / Yesterday / DD MMM headers. Each row links to the order's detail page. Empty states are friendly. **Status pill rollup logic**: order is `done` when every line item has `qty_remaining = 0` AND every dispatch is at `handed_off`; `pending` when no dispatches exist for any line item; `in_progress` otherwise. Same matrix is the basis for the Pending/In-progress queue filters. **Tests** in `tests/support/orders-queries.test.ts` (5 cases): `mode=pending` excludes partials, `mode=in_progress` excludes untouched + done + includes packed-not-handed-off, `loadAllOrders` rolls up state correctly, search by customer name filters, `loadActivityFeed` returns events in reverse-chrono with items summary + dispatch_created + dispatch_packed events for a single advance flow. **Updated `tests/lib/support-nav.test.ts`** for the new 4-tab nav shape + new `/support/in-progress` route. **Pure additive — no schema, no actions, no auth, no audit changes**. **Pre-PR gate**: tsc clean, next build clean, **1001/1001 vitest pass** (7 new). |
| 2026-06-06 | HVA-244 (HVA-242-FIX): DispatchDialog qty input — can't edit on mobile | **Walk-bug from HVA-242.** Sandeep opened DispatchDialog on /support/orders/[id] for an item with qty_remaining=8, wanted to dispatch 4 of 8, found the qty field pre-filled with 8 but unable to change it on his phone. **Root cause**: `type="number"` renders inconsistently on iOS Safari (sometimes full keyboard instead of numeric pad); cursor lands at end of value so user has to backspace before typing; 80px-wide field with no focus auto-select makes the affordance feel read-only. **Fix**: switched input to `type="text"` + `inputMode="numeric"` + `pattern="[0-9]*"` (the reliable iOS numeric-keypad combo), added `onFocus={(e) => e.currentTarget.select()}` so the existing value is highlighted (one tap → type "4" → done), added `−` and `+` stepper buttons either side of the input clamped to [1, quantityRemaining] for one-tap adjustments, widened input + centered text + bumped to h-10 for thumb-friendly tap target, added small helper hint "Tap the number to edit · max 8". Same DispatchDialog is used by both /support queue and /support/orders/[id], so both flows are fixed. **Pure UI** — no schema, no actions, no audit, no auth changes. **Pre-PR gate**: tsc clean, next build clean. |
| 2026-06-06 | HVA-243: /requests/[id] redesign — tabs + accordions + sticky next-action header | **Walk-feedback from Sandeep after HVA-241 shipped:** the request detail page was overpopulated for ORDER_CONFIRMED+ requests — customer info + status timeline + line items + payments + notes + comments + admin help + a row of action buttons all stacked vertically. Mobile execs scrolled past sections every visit. **New shell** at `app/requests/[id]/_components/RequestDetailShell.tsx` (radix Tabs, client) takes 4 ReactNode children (overview / order / activity / admin) and renders them as tab panels. **Sticky header** at `_components/StickyRequestHeader.tsx`: back button + customer name (truncated) + status badge + city badge + phone CTA + edit pencil + **single resolved primary action**. Primary action priority `approve > markComplete > advance > assignExec` (resolved server-side from `computeActionVisibility`); all other verbs (`rollback / reassign / reject / markRejected / reschedule / adminHelp`) live in the Admin tab where they belong. **4-tab layout** (per discussion 2026-06-06): **Overview** = Customer info accordion (default open) + Address accordion + Tracking link accordion. **Order** = Quotation/items/payments accordion (default open, embeds `CollectionSection`) + Dispatch history accordion (read-only, ORDER_CONFIRMED+ only). **Activity** = Status timeline accordion (default open) + Notes accordion + Order comments accordion (ORDER_CONFIRMED+ only). **Admin** = Manage request accordion (rollback/reassign/reject/mark-rejected) + Reschedule accordion + Admin help accordion. All accordions default-closed except the per-tab essential. **Read-only dispatch mirror**: exec/captain now see what support sees — added `canAdvance` prop to `DispatchHistoryBlock` (defaults true so /support page stays unchanged); the read-only mirror loads via `loadOrderDetail` gated to ORDER_CONFIRMED+ stages. **Embedded prop** added to `NotesSection` and `AdminHelpSection` so they skip their own card wrapper + heading when nested inside an accordion (no double-card). **Initial tab logic**: ORDER_CONFIRMED+ requests open on Order tab so dispatch state is one click away; everything else opens on Overview. **Banner section** above tabs renders terminal-state summary card (when cancelled) + pending-approval waiting card (when exec at PENDING_CAPTAIN_APPROVAL). **Pure UI** — no schema, no actions, no audit, no auth changes; the entire data-loading + role-gate path of page.tsx is untouched. **Pre-PR gate**: tsc clean, next build clean, **994/994 vitest pass** (no new tests — pure rearrangement; existing component tests cover the islands). |
| 2026-06-06 | HVA-242 (HVA-231 Phase 4): Dispatch action on /support/orders/[id] — per-row checkboxes + sticky bar | **Walk-bug from HVA-239 fixed.** Support could read everything on the detail page (items, dispatch history, comments) but the only path to start a dispatch was bouncing back to /support queue. Now: items table on `/support/orders/[id]` has a checkbox column for rows with `qty_remaining > 0`, fully-done rows show a green "Done" pill instead, header has a select-all checkbox (ignores done rows). Once anything is checked a sticky bar with backdrop-blur appears at the bottom of the section: `[ Clear ] [ Dispatch selected (N) ]`. Clicking opens the **existing** `DispatchDialog` (refactored to accept a smaller `DispatchableItem` shape: lineItemId / productName / contextLine / quantityRemaining — so both `/support` queue and `/support/orders/[id]` build their own rows). On success: dialog closes, `router.refresh()` triggers, items + dispatch history reload in place — no navigation. **Visibility gate**: `canDispatch` derived from `session.user.role === support \|\| super_admin` so exec/captain who land here see the items table read-only (no checkboxes, no sticky bar) — matches the read-only intent we already had. **New client component** `app/(support)/support/orders/[id]/_components/ItemsDispatchTable.tsx` replaces the inline server `<table>` block on the detail page. **Refactored DispatchDialog**: dropped the dependency on `SupportQueueRow` type, exports new `DispatchableItem` interface. Queue caller in `SupportQueueTable.tsx` updated to construct the new shape inline. **Pure UI change** — no new server actions, no schema, no auth surface, no audit events. **Pre-PR gate**: tsc clean, next build clean, **994/994 vitest pass** (no new tests — pure UI rearrangement; existing dispatch action tests still cover the wire). |
| 2026-06-06 | HVA-241 (HVA-231 Phase 3): Order comment threads — Slack-style per-order thread for support ↔ exec ↔ captain | **First user-visible Phase 3 ticket.** Once an order is at ORDER_CONFIRMED, support / assigned exec / assigned captain / super_admin can post comments on a per-order thread visible to the whole internal team. Customer never sees these. **Schema** (migration 0067): new `order_comments` table — id (UUIDv7), request_id FK CASCADE, author_user_id FK RESTRICT, optional parent_comment_id FK RESTRICT (single-level reply nest), body TEXT with DB CHECK 1..2000, mentions JSONB array of user_ids, created_at. Indexes on (request_id, created_at), (author), and partial (parent_comment_id) where non-null. Append-only by design — no edit, no delete (mirrors HVA-73 notes contract). **Audit + notification rules**: extends `audit_enabled_events` defaults (dual-write per CLAUDE.md) with `order_comment_added`; migration seeds 8 `notification_rules` rows for `support.order_comment_added` — in_app + push × {exec_assigned, captain_owning_city, support_team_all, mentioned_users}. WhatsApp deliberately skipped (too chatty for thread updates). **Server action** `addOrderCommentAction` in `lib/order-comments/actions.ts`: full auth via shared `canViewOrderComments` (write visibility = read visibility), validates parent belongs to same request, validates every `mentionedUserIds[i]` belongs to the legal pool (`loadMentionPool` = support + super_admin + assigned exec + assigned captain), drops self-mentions, INSERT, audit emit, setImmediate fan-out via `dispatchNotification('support.order_comment_added', ...)`. **Engine extensions** in `lib/notifications/engine.ts`: new `mentioned_users` recipient_role resolver (reads `context.mentionedUserIds: string[]`); global `self_notify_suppressed` guard skips any recipient whose id matches `context.authorUserId` (so support author doesn't ping themselves via `support_team_all` etc.). **Composer** `composeOrderCommentAddedInApp` with @mention-aware title variant ("mentioned you" vs "commented"). **UI** (`components/order-comments/`): server `OrderCommentsBlock` loads timeline + mention pool, client `OrderCommentsClient` renders parents chronologically with replies indented under, "Reply" button on each parent flips the composer's parentCommentId, @mention picker with multi-select + role badges, 30s polling via Page Visibility API + `router.refresh()` so the thread updates while the tab is visible. Mount on `/support/orders/[id]` (always) and `/requests/[id]` (gated to ORDER_CONFIRMED / INSTALLATION_SCHEDULED / INSTALLATION_DONE / ORDER_EXECUTED_SUCCESSFULLY). **Tests** in `tests/order-comments/add-comment.test.ts` (7 cases): support write happy path (row + audit + timeline), exec RBAC matrix (assigned can, other-request exec blocked), body length boundaries (empty trim → reject, 2000 chars → ok, 2001 → reject), reply parent must belong to same request (cross-request rejected, same-request reply attaches parent), @mention pool validation (in-pool ids ok, outsider exec rejected), notification fan-out (support author → exec + captain + other-support get in_app rows, author gets none — self-notify suppression verified end-to-end), migration seed shape assert. **beforeEach re-seeds** the 8 migration 0067 rules since `truncateAll()` wipes notification_rules. `order_comments` added to `SAFE_TRUNCATE_TABLES` ahead of users (FK RESTRICT). **Pre-PR gate**: tsc clean, next build clean, **994/994 vitest pass** (7 new), 27/27 e2e unchanged (no shell touched outside /support detail page). |
| 2026-06-06 | HVA-240 (HVA-231 Phase 2 PR-C): Dispatch notifications — support broadcast + exec/captain in-app + push + WhatsApp templates | **Third + last user-visible piece of Phase 2.** Wires the dispatch workflow into the existing notification engine so the right people get pinged automatically. **3 new event types**: `support.order_ready_for_dispatch` (broadcast to all active support users when a request transitions INTO ORDER_CONFIRMED — fires from `lib/status-transition.ts` orthogonal to the per-transition `emits_event` map), `support.dispatch_recorded` (fires from `addDispatchAction` per affected request — multi-order dispatches fan out per-customer so each exec + captain gets a targeted ping), `support.dispatch_advanced` (fires from `advanceDispatchStageAction` on every packed / handed_off transition). **New engine resolver**: `support_team_all` in `lib/notifications/engine.ts:resolveRecipients` — selects every active user with role='support', mirrors the `super_admin` broadcast shape. **Composers** in `lib/notifications/compose/dispatch-events.ts`: 3 in-app composers (title + body + linkUrl) + 2 WhatsApp template composers (`internal_items_dispatched_v1`, `internal_dispatch_advanced_v1`) with body params Meta-template-ready. **Migration 0066**: 14 `notification_rules` rows seeded — in_app + push enabled by default, WhatsApp shipped `enabled=false` until Sandeep submits + Meta approves the 2 new UTILITY templates (same `enabled=false` pattern as HVA-228's `internal_hard_warning_v1`). Admin flips them on via `/admin/settings/notifications/rules` post-approval. **Tests in `tests/support/dispatch-notifications.test.ts` (7 cases)**: 3 composer unit tests (pure functions), rules-seed presence check (asserts WhatsApp ships disabled with templateKey set), dispatch fan-out → exec + captain get in_app rows, advance fan-out → notification on stage flip, broadcast → every active support user gets the order_ready_for_dispatch ping. **beforeEach re-seeds** the migration 0066 rules because `truncateAll()` wipes `notification_rules` between tests (existing harness convention; migration-seeded data isn't preserved across truncates). **Pre-PR gate**: tsc clean, next build clean, **987/987 vitest pass** (7 new), **27/27 e2e** (no UI baselines disturbed). |
| 2026-06-06 | HVA-239 (HVA-231 Phase 2 PR-B): Per-order detail view + dispatch lifecycle UI | **Second user-visible piece of Phase 2.** Layers the order detail page + the stage-advance action on top of HVA-238's queue. **New page** `/support/orders/[id]` shows: header (customer + city + status_stage + exec + captain), items section (every line item incl. fully-dispatched ones with qty_total / qty_dispatched / qty_remaining / priority / target date), dispatch history section (chronological list of dispatch events with stage badge + items + qty + notes). **`loadOrderDetail` query** in `lib/support/order-detail.ts`: 3 sequential queries — request join, items with correlated dispatched SUM subquery (RAW SQL for the subquery because Drizzle's `${dispatchItems}` interpolation in correlated FROM was being misresolved), dispatches join. Latest stage per dispatch resolved client-side from history rows ordered by changedAt desc. **`DispatchHistoryBlock` client component**: one card per dispatch event with stage badge (amber created / sky packed / emerald handed_off), per-item rows, optional notes display. "Mark packed" / "Mark handed off" button on cards that have a next stage; disabled at terminal (handed_off). **Server action** `advanceDispatchStageAction` in `app/(support)/support/_actions/advanceDispatchStage.ts`: auth (support / super_admin), validates dispatch exists, validates current stage = predecessor of toStage (no skipping, no reverse), single tx INSERT into dispatch_status_history (UNIQUE per dispatch+stage enforces no double-flip at DB), emits `dispatch_advanced` audit event. **Queue rows now link to detail page**: customer cell + product cell are Link to `/support/orders/[requestId]`; per-row Dispatch button preserved. **Tests** in `tests/support/dispatch-lifecycle.test.ts` (8 cases): happy path created → packed → handed_off, skip-stage rejection (created → handed_off forbidden), terminal-stage rejection (handed_off → packed), RBAC (captain blocked), unknown dispatch (not-found), loader returns null for missing request, loader includes both fully + partially dispatched items, loader reflects latest stage. **Drizzle gotcha fix bundled**: PR-A's correlated subquery worked because the parent query used `from(dispatchItems)` (so the dispatchItems interpolation was unambiguous), but PR-B's outer query was `from(quotationLineItems)` — interpolating `${dispatchItems.qtyInThisDispatch}` inside a sub-SELECT got resolved without prefixing the subquery's own alias, leading to a `column "qty_in_this_dispatch" does not exist` failure that silently returned 0 via COALESCE. Switched to raw SQL for the subquery. **Pre-PR gate**: tsc clean, next build clean, **980/980 vitest pass** (8 new), **27/27 e2e** (no UI baselines disturbed). |
| 2026-06-06 | HVA-238 (HVA-231 Phase 2 PR-A): Support dispatch queue + multi-order dispatch action | **First user-visible piece of Phase 2.** `/support` is no longer a placeholder — it's the real dispatch queue. Server-renders every `quotation_line_items` row whose parent visit_request is at `ORDER_CONFIRMED+` (sequence_number ≥ 6) AND has remaining qty > 0. **Query** in `lib/support/dispatch-queries.ts` uses a correlated SUM(dispatch_items.qty) subquery for remaining math; sorts by priority desc (CASE pivot: high=3, med=2, low=1) → target_dispatch_date asc (NULLs last) → createdAt asc. Search by customer name or product name (substring, case-insensitive). Capped at 200 rows in v1. **Queue UI** (`SupportQueueTable` client wrapper): table with customer + city / product + SKU / qty left ("3 of 5") / priority pill (low slate, med amber, high rose) / target date / order age. Per-row Dispatch button + multi-row checkboxes + "Dispatch selected" CTA at top. Debounced search input drives `?q=` URL param. Empty state when no items pending. **DispatchDialog**: pre-populated with selected items, each gets a qty input defaulted to qty_remaining, plus optional free-text notes. Submit triggers `addDispatchAction`. **Server action** at `app/(support)/support/_actions/addDispatch.ts`: auth gate (support or super_admin), Zod validation (1–50 items, qty > 0, qty ≤ 100k), duplicate-line_item dedup in payload, batched stage+remaining lookup, single tx writes `dispatches` row + N `dispatch_items` + initial `dispatch_status_history` (stage=`created`), emits `dispatch_created` + N `dispatch_item_added` audit events. **Multi-order dispatch** works: one event can include items from multiple visit_requests (clean schema since dispatch_items is a junction). **Tests**: 19 in `tests/support/dispatch-action.test.ts` covering validator (6: empty, qty=0, qty>cap, >50 items, notes), RBAC (4: anon, captain, exec, super_admin escape hatch), validations (4: wrong stage → reject, qty>remaining, duplicate items, cumulative remaining across dispatches), success (2: single + multi-order writes everything correctly), queue queries (3: hides fully-dispatched, sorts by priority+date, search filter). **Test harness fix**: added `dispatches` / `dispatch_items` / `dispatch_status_history` to `tests/helpers/db.ts > SAFE_TRUNCATE_TABLES` (before users) so the truncate cascade respects RESTRICT FKs. **Audit allow-list bug found + fixed**: HVA-235's migration UPDATEd the live config row to include dispatch events, but `lib/config-schema.ts` defaults were missed — testcontainer (no seeded config row) falls back to defaults and silently dropped dispatch audit writes. Added the 3 dispatch events to the defaults too (dual-write rule per CLAUDE.md). **Pre-PR gate**: tsc clean, next build clean, **972/972 vitest pass** (19 new), **27/27 e2e** (no UI baselines disturbed — /support change visible only to support role which isn't in the e2e seeded users). |
| 2026-06-06 | HVA-236 (HVA-235-FIX1): Admin UI to create / list / activate / deactivate / reset-password support team users | **Walk-bug from HVA-235**: support role shipped but no admin UI to onboard support users — admins would have to insert via SQL. Sandeep: *"These are basics and very important. Don't skip anything like this."* Now saved as memory `feedback_dont-defer-admin-basics.md` — never split the admin onboarding UI from the role/feature that needs it. **New page** at `/admin/settings/organization/support`: mirrors the executives admin pattern. Lists all support users with name + phone + email + active status. Search box (matches name / phone / email substring). Pagination at 20. **Add support user** dialog: full name + 10-digit phone + optional email. Server generates a temp password (via `generateTempPassword` shared with the executive flow), hashes via Better-Auth scrypt, shows once in a "copy-this-once" follow-up dialog. **Edit dialog**: change name / phone / email. Role stays `support` (no role pivot). **Per-row actions**: Reset PW (generates a new temp password + revokes all sessions + sets `must_change_password=true`), Deactivate / Activate toggle (deactivate revokes all sessions + flips `is_active=false`). **Audit**: 5 new event types (`support_user_created`, `support_user_updated`, `support_user_deactivated`, `support_user_activated`, `support_user_password_reset`) added via dual-write (lib/config-schema.ts defaults + migration 0065 UPDATE). **Nav**: new "Support Team" entry under Settings → Organization, between Executives and the next category. **API routes**: `POST /api/admin/support` (create), `PATCH /api/admin/support/[id]` (edit), `POST /api/admin/support/[id]/{deactivate,activate,reset-password}` — each: super_admin gate via `requireSuperAdmin()`, role check on target user (refuses 400 if target is not `support`), single-tx writes with audit log emission. **Tests**: 14 new in `tests/admin/support-users.test.ts` covering RBAC (anon/captain/exec → 401 or 403), happy paths (create returns user+tempPassword, edit, deactivate revokes sessions, activate round-trip, reset-password new hash), guards (already-inactive 409, already-active 409, role mismatch 400, phone collision against any role 409, invalid phone shape 400). **Pre-PR gate**: tsc clean, next build clean, 944/944 vitest (14 new + 1 updated admin-nav assertion), 27/27 e2e. |
| 2026-06-06 | HVA-235 (HVA-231 Phase 1.1): support foundation — dispatch schema + `support` role + `/support/*` shell | **Three-in-one PR per the "no more than 3 surfaces" cap.** Lands everything HVA-231 Phase 2 (dispatch action UI) needs to plug into. **Migration 0064**: `ALTER TYPE user_role ADD VALUE 'support'` (idempotent); new `dispatch_stage` enum (`created`/`packed`/`handed_off`); three new tables — `dispatches` (one row per dispatch event; `dispatched_by_user_id` FK RESTRICT for attribution; no request_id since multi-order dispatches link via items), `dispatch_items` (junction: FK to dispatches CASCADE + FK to quotation_line_items RESTRICT, `qty_in_this_dispatch` CHECK >0, UNIQUE per (dispatch, line_item) so one dispatch can't list the same item twice), `dispatch_status_history` (lifecycle audit with UNIQUE per (dispatch, stage)). Indexes on (dispatched_by), (created_at), (dispatch_id, lineitem_id), (dispatch_id, changed_at). Audit allow-list extended with `dispatch_created` / `dispatch_advanced` / `dispatch_item_added` via dual-write. **`support` role**: enum extended in `db/schema/auth.ts`; `lib/auth/roles.ts` gets `USER_ROLES.SUPPORT` + ROLE_HOME mapping (`/support`); `lib/notifications/preferences.ts` gets the support role's recipient-role list (empty in v1 — Phase 2 wires notifications). `proxy.ts` gates `/support/*` to support + super_admin (via the existing super_admin escape hatch). **`/support/*` shell**: new route group `app/(support)/` with `layout.tsx` (auth gate via new `decideSupportAccess`, mirrors `decideExecAccess`), `SupportSidebar` (240px persistent left rail at lg+, hidden on mobile), `SupportTopbar` (56dp sticky header with current-page title resolved via `activeSupportNav`). Three placeholder pages: `/support` (Queue), `/support/orders`, `/support/activity`, all showing "Coming in Phase 2" empty state. Mobile drawer + bell + push-prompt deliberately deferred to Phase 2 to keep this PR small. **Tests**: `tests/lib/support-authz.test.ts` (8 cases: anonymous, support → allow, exec/captain/admin → their home, unknown role, no role, next-path encoding) + `tests/lib/support-nav.test.ts` (8 cases: nav shape, activeSupportNav exact + prefix + null branches); plus updated `tests/smoke/harness.test.ts` to assert the new 4-value enum. **Type-inference fix**: discovered HVA-234's previous "tsc clean" was an incremental cache illusion — `LineItemCreateInput = z.infer<...>` made `priority` required since `.default('med')` infers to the output type. Fixed by changing the export to `z.input<...>` so callers can omit `.default()` fields. Tests stay clean; runtime behaviour unchanged. **Pre-PR gate**: tsc + next build + 946/946 vitest (929 prior + 16 new authz/nav + 1 fixed enum assertion) + 27/27 e2e (LineItemsSection unchanged on the seeded request → baselines stable). |
| 2026-06-06 | HVA-234 (HVA-231 Phase 1.0): line items foundation — extends `quotations`, new `quotation_line_items`, manual entry UI | **Shared foundation for HVA-231 (support dispatch) + HVA-230 (ECOM webhook).** Until now HVA's `quotations` table stored only total + notes; no per-item structure. Support can't dispatch items if items don't exist, and the webhook handler has nowhere to put the portal's line items. This ticket is the unblocker. **Migration 0063**: extends `quotations` with `source` enum (`manual` / `portal`), `portal_quotation_id` (UNIQUE-when-set for webhook idempotency), `raw_payload` jsonb, `last_webhook_at`. New `quotation_line_items` table: 1:N with `quotations`, CASCADE delete. Columns: `position` (display order), `product_name`, `product_sku` (nullable), `quantity`, `unit_price_paise`, `line_total_paise`, `gst_percent` (nullable), `notes`, `priority` enum (`low`/`med`/`high`, default `med`), `target_dispatch_date`. CHECK constraints for non-negative numbers; partial UNIQUE index on `portal_quotation_id`; indexes on `(quotation_id)`, `(priority, target_dispatch_date)`, `(product_sku)`. Audit allow-list extended with `line_item_added` / `line_item_updated` / `line_item_priority_changed` via dual-write (config-schema.ts + UPDATE in migration). **Server actions** at `app/requests/[id]/_actions/lineItems.ts`: `addLineItemAction`, `updateLineItemAction`, `setLineItemPriorityAction`. Each: full ActionResult shape, Zod validation, auth gate via existing `canExecEditRequest` / `canCaptainEditRequest` (no new auth surface), audit emission, `revalidatePath('/', 'layout')`. **Server-authoritative line_total**: `line_total_paise = quantity * unit_price_paise` ALWAYS computed server-side; client cannot inject a mismatched total. Position auto-incremented per quotation. **UI**: new `LineItemsSection` client island on `/requests/[id]` collection block. Renders below the quotation headline. Table view: product / SKU / qty / unit ₹ / line ₹ / priority pill / target date / edit action. Add + edit via modal dialog with: name + SKU + qty + unit price + GST% + notes + priority + target date. Auto-computes line total client-side as user types. Hides itself when no items + no edit permission. Reuses HVA-149's `useServerMutation` for both modes. **Validators** in `lib/validators/quotation.ts`: `lineItemCreateSchema` (UUID quotationId, non-empty product name, qty ≥ 1, unit price ≥ 0, GST 0..100, priority enum default `med`), `lineItemUpdateSchema` (all fields optional but ≥1 required), `lineItemPrioritySchema` (focused setter). **Tests**: 15 new cases in `tests/quotations/line-items.test.ts` covering schema edge cases, server-side line_total enforcement, position auto-increment, RBAC (exec assigned passes, other-exec blocked, super_admin allowed), focused priority setter. **Pre-PR gate**: tsc clean, next build clean, **914/914 vitest pass** (15 new), **27/27 e2e pass** (LineItemsSection returns null when no quotation, so visual baselines unchanged). |
| 2026-06-06 | HVA-229: broaden exec streak to also count completed face-to-face tasks linked to a request or lead | **Walk-bug fix from Sandeep 2026-06-06**: "yesterday I finished 2 tasks in Veera, it is still showing 'No active streak. Last visit completed on 19 May.'" Investigation: Veera completed 3 "Customer home visit" tasks on 2026-06-05, all linked to requests, but didn't separately advance those requests to VISIT_COMPLETED — so `request_status_history` had zero rows and the streak calc (HVA-201 logic, status-only) ignored her activity entirely. **Decision**: NOT auto-advancing the linked request on task-done (multi-visit cycles like pitch → measure → close are real; forcing the status would break captain approval downstream). Instead broaden the streak's "active day" definition. **Sandeep-locked decisions**: D1 — only 4 face-to-face task types count: `Customer home visit`, `Outlet visit`, `Sales pitch`, `Stall Activity` (NOT Follow-up / Installation / Other); D2 — task must have a non-null `link_request_id` OR `link_lead_id` (prevents streak padding via bogus visit entries); D3 — `completed_at` IST date is the streak day, NOT `task_date` (matches "exec was actually working" intent). **Implementation**: `lib/leaderboard/streak.ts` now runs the existing status_history query in parallel with a new tasks query, unions days into a single Set per exec, then walks backward as before. Both `loadStreakForExec` (single) and `loadStreaksForExecs` (batch) updated. `lastActiveDay` reflects the max across both sources. **Tests**: 4 new cases on top of HVA-201's 10 — qualifying task → +1, non-qualifying type → unchanged, qualifying type but unlinked → unchanged, both sources on same day → 1 (Set dedup). Total 14 streak tests. **Pre-PR gate**: tsc clean + next build clean + full vitest suite passes. |
| 2026-06-05 | HVA-200: optimistic UI on Add Task / Add Lead / Record Payment | **Applies the HVA-150 opt-in optimistic carve-out** (canonical pattern in `components/notes/NotesSection.tsx`) to the three remaining high-risk double-submit surfaces. Plain `useState` + merge-by-id; NOT React's `useOptimistic` hook. **Pattern**: parent client wrapper owns optimistic state; form (Sheet/Dialog) accepts `optimistic?: { onAdd, onReconcile, onRemove }`. On submit, form fires `onAdd(temp)` + closes immediately + awaits server. On success: `onReconcile(tempId, serverId)`. On failure: `onRemove(tempId)` + error toast. Once `router.refresh` lands the canonical row, the merge dedups by id. **Surface 1 — Add Task**: new `app/(exec)/today/_components/PostSubmissionTasksList.tsx` (client) owns the tasks list rendering + bottom action FAB row. `PostSubmissionView` becomes a thin server shell that hands data to the wrapper. `AddTaskSheet` accepts `optimistic` prop; add mode only (edit/clone keep the existing useTransition path). **Surface 2 — Add Lead**: `LeadsFilterClient` (already client) extends with `useState<LocalLead[]>`. `AddLeadSheet` accepts `optimistic` and uses `useServerMutation` in a manual mode (`suppressErrorToast: true` + manual onClose before mutate). `AddLeadFab` passes handlers through. **Surface 3 — Record Payment**: new `app/requests/[id]/payments-block.tsx` (client) carved out of the server-side `CollectionSection`. Holds optimistic payments list + renders `PaymentRecordButton` with handlers wired. `PaymentRecordButton` extended to support optimistic; uses the existing `/api/requests/[id]/payments` response that already returns `payment.id`. **Deliberate scope guard on payment summary**: the Net received / Balance due / Overpaid block stays server-rendered so SSOT math via `lib/collection-summary.ts` survives the carve-out. Summary catches up on the next `router.refresh` (~200ms). Tradeoff documented in the file header. **Optimistic row UX**: `opacity-70 pointer-events-none` + a "Saving…" badge on payments. Auto-cleanup after 3s post-reconcile so optimistic state doesn't carry stale id-renamed rows forever (matters if user adds 10 rows rapidly). Every opt-in surface tagged `// HVA-150:` per CLAUDE.md convention. **Pre-PR gate**: tsc + next build + 27/27 e2e baselines unchanged (refactors preserved layout). |
| 2026-06-05 | HVA-198 PR-B: captain/exec specs + GitHub Actions CI (HVA-138 spike punted) | **Second half of HVA-198 — completes the original ticket scope minus the maintenance README + CI dashboard (those land separately).** **New specs**: `tests/e2e/captain-workflow.spec.ts` (login as Arjun → /captain/requests list → screenshot + open the seeded request → /requests/[id] → screenshot) + `tests/e2e/exec-requests.spec.ts` (login as Veera → /requests list → screenshot + open same request → /requests/[id] → screenshot). 12 new visual baselines (4 screenshots × 3 viewports). **Seed extension**: `tests/setup/e2e-seed.ts` now inserts ONE assigned visit_request linking E2E Customer → Veera → Arjun in Hyderabad so both list views always have at least one row to click into. Added `sampleRequest` to the `SeededE2EUsers` interface. **GitHub Actions CI** (`.github/workflows/playwright.yml`): runs on push to main + every PR, sets up Node 22 + pnpm 11.1.1 (matched to packageManager), installs Chromium via `playwright install --with-deps chromium`, runs the full gate — `tsc --noEmit`, `next build` (with dummy DATABASE_URL + BETTER_AUTH_SECRET for the build phase), `pnpm test` (vitest with its own testcontainer), `pnpm test:e2e` (the orchestrator that spins up its own testcontainer + next-server). Uploads `test-results/` + `playwright-report/` as artifacts on failure with 7-day retention. Concurrency group cancels in-progress runs when a new commit lands on the same ref. Job timeout 15m. **HVA-138 spike punted**: spent 15 min trying RTL 16.3.2 (latest; the original "RTL 17" doesn't exist on npm). React 19 + Vite 8 + Vitest 4 + RTL 16.3.2 hit a `TypeError: jsxDEV is not a function` at every render call. Tried `@vitejs/plugin-react`, `@vitejs/plugin-react-swc` (blocked by pnpm onlyBuiltDependencies), explicit `optimizeDeps.include`, and `esbuild.jsxDev: false` — none resolved the JSX-dev-runtime mismatch with Vite 8's prebundling. Playwright CT would add similar friction (PWCT config + Next.js module mocking via Vite). Spike outcome: React component test harness needs a dedicated sprint with budget for the prebundling investigation, not a 15-min validation gate. Filed for follow-up. **Pre-PR gate**: tsc clean, next build clean, 27/27 e2e pass (5 specs × 3 viewports + 3 smoke × 3 viewports). |
| 2026-06-05 | HVA-198 PR-A: Playwright authenticated flows infra | **First half of HVA-198 — the infra + one authed spec, leaving 2 more specs + CI for PR-B.** Sandeep agreed to a two-PR cadence to land the testcontainer wiring incrementally. **Problem solved**: Playwright's built-in `webServer` block starts BEFORE `globalSetup`, so a globalSetup that boots a testcontainer can't pass its connection string to the server. **Solution**: a custom runner at `scripts/run-e2e.ts` owns the lifecycle — boots `postgres:16-alpine` via `@testcontainers/postgresql`, applies migrations using the same SHA256-tracked logic vitest uses, seeds three canonical users (Veera/Arjun/Sandeep) + a Hyderabad city + a sales_executive row linking Veera→Arjun, writes credentials to `tests/e2e/.e2e-users.json` (gitignored), builds (or reuses) the Next.js production bundle, spawns `pnpm next start -p 3100` with the testcontainer URL + a generated BETTER_AUTH_SECRET in env, polls `/api/health` for up to 60s, then invokes `pnpm exec playwright test ...` with the rest of argv. Pass-through exit code so CI gating works. Cleanup on SIGTERM/SIGINT kills the next-server child + stops the container. **Playwright config rewrite**: dotenv import + DATABASE_URL host-form remap dropped; webServer block dropped entirely (runner owns lifecycle); 3 viewports added — desktop (1280×800), tablet (768×1024), mobile (375×667 with deviceScaleFactor:2 + isMobile + hasTouch). All using Desktop Chrome to avoid Mobile Safari's separate baseline tax. `maxDiffPixelRatio: 0.05` preserved from HVA-151. **New helpers**: `tests/setup/e2e-boot.ts` (testcontainer + migrations), `tests/setup/e2e-seed.ts` (raw-`postgres` user seed — no Drizzle dependency to keep the runner boot path simple), `tests/e2e/helpers/login.ts` (`loginAs(page, 'exec'\|'captain'\|'superAdmin')` reads E2E_USERS_FILE, fills the 10-digit phone form correctly, waits for navigation away from /login), `tests/e2e/helpers/db-reset.ts` (`resetExecState(userId)` deletes day_plans + tasks so each test starts hermetic — the spec's `beforeEach` resets Veera before each iteration so desktop test-2's day_plan submission doesn't leak into tablet test-1). **First authed spec**: `tests/e2e/exec-daily-loop.spec.ts` — 2 tests covering login → /today pre-submission view (visual baseline) + Start My Day → post-submission view (visual baseline, anchored on the "Add task" button which is uniquely visible post-sub). 6 baselines committed under `tests/e2e/exec-daily-loop.spec.ts-snapshots/`. **Scripts**: `test:e2e` now runs `tsx scripts/run-e2e.ts`; `test:e2e:update` adds `--update-snapshots`; new `test:e2e:direct` keeps raw `playwright test` for the rare case someone has a live DB already. **Add Task / Mark Done / Postpone / Close Day steps + captain-workflow + exec-requests specs + GitHub Actions CI** all go to PR-B. **Pre-PR gate**: tsc clean, next build clean, 15/15 e2e tests pass twice in a row (baselines stable on replay). |
| 2026-06-05 | HVA-90 cities deferred fields (closes HVA-164) | **Lights up the deferred fields on `/admin/settings/organization/cities`** that HVA-110 left as TODO. Sandeep multi-ticket prompt (PR #1): build cities config — Discord webhook + Other routing + support phones. After recon found the page already exists (327 LOC, HVA-110 shipped 2026-05-16), Sandeep picked **"Option A: just add deferred fields on top of existing page"** so the work is purely UI + actions, no migration (all schema columns already exist). **New: `lib/admin/discord-webhook-validator.ts`** — live ping helper. POSTs a one-line test message to the webhook URL with a 5s timeout. Branches by Discord HTTP status: 401/403 → "Invalid Webhook Token", 404 → "Unknown Webhook (channel deleted or webhook revoked)", 429 → rate-limit hint, 5xx → generic status. AbortError → timeout copy. Real Discord errors surface verbatim via the response body's `message` field. **New: `cityConfigUpdateSchema`** in `lib/validators/admin-cities.ts` — multi-field PATCH (captainRoutingEmail / otherRoutingEmail / discordWebhookUrl) with per-field email/URL preprocessors. Preserved the HVA-110 `cityRoutingEmailUpdateSchema` for back-compat. Critical fix in the preprocessor: `undefined` means "absent → no change" but explicit `null` means "clear the column" (matching the HVA-110 contract the existing cities.test asserts). **PATCH route rewrite** (`/api/admin/cities/[id]`): accepts the new fields; calls Discord ping inline when discordWebhookUrl is non-null; preserves the HVA-110 "Other.*super_admins" error message verbatim so legacy tests pass; emits a new `city_config_updated` audit event when multiple fields change, or the legacy `city_routing_email_updated` event when only the routing email did (keeps existing audit dashboards stable). **UI**: rebuilt `cities-client.tsx` with a multi-field "Edit city" modal. The Other row's Edit button is now ENABLED (was disabled per HVA-110) — modal renders the `other_routing_email` field instead of `captain_routing_email` for that row + the Discord webhook field for both. Discord webhook shown truncated in row display with full URL on hover. **New: top-of-page `SupportPhonesSection`** with two inline phone inputs (customer_support_phone + admin_support_phone) + per-field dirty-state + Save buttons. Posts to the existing `/api/admin/config/customer-support-phone` + new `/api/admin/config/admin-support-phone` (mirror of the customer route, same Zod schema). Same +91 + 10-digit format. **New: `adminSupportPhoneUpdateSchema` + `/api/admin/config/admin-support-phone/route.ts`**. **HVA-164 closed as absorbed** — DB column existed (HVA-110), now there's the admin UI; Sandeep can set Hyderabad/Ahmedabad routing emails from the page (operational data step, not code). **Tests**: 10 cases in `tests/admin/discord-webhook-validator.test.ts` (shape reject, every Discord HTTP branch, timeout, network error) + 11 cases in `tests/admin/admin-cities-validator.test.ts` (legacy + new schema, normalisation, partial updates, malformed inputs). **Pre-PR gate**: tsc + next build + 895/895 tests pass (21 new). |
| 2026-06-05 | Exec /tasks unified table (Option B) | **Sandeep**: *"plan the same tasks page for sales executives as well"* → after planning, picked **Option B (replace accordion with table)**. The exec /tasks accordion is gone; the page now uses the same `<TasksTableFilters>` + `<TasksTableView>` the captain + admin already had. New `<ExecTasksTableShell>` client wrapper manages the row actions: pending/postponed rows show a "Move" / "Reschedule" button that opens `MoveTaskSheet`; completed rows show a "Re-add" button that opens `AddTaskSheet` in clone mode (same behavior the old accordion had). **`TasksTableView` gained a `renderRowActions` prop** — optional per-row ReactNode slot the exec uses, captain + admin omit (they're observational). **`TasksTableFilters` gained `showExecFacet` prop** so the exec dropdown can hide when the page is already self-scoped. **Deleted**: `app/(exec)/tasks/_components/TasksPageView.tsx` + `CompletedTasksList.tsx` (dead after the rewrite). `TaskRowWithAction.tsx` kept — still used by `/today`'s `StartMyDayWithRecentTasks`. The exec /tasks page now has the same search + status + from/to + sort + pagination story as the captain + admin tasks pages — consistent UX across all three portals. **Pre-PR gate**: tsc + next build + 874/874 tests pass. |
| 2026-06-05 | Tasks pages + reports fixes | **Two prod issues + one feature.** Sandeep: *"All tasks should have a proper sort by date filter. This should be both in sales, executive, captain, and admin. Reports are not showing up in the sales executive portal and admin >> City >> captains portal."* **(1) Exec /reports role typo (3 files)**: `role !== 'sales_exec'` but the canonical role string is `sales_executive` (`USER_ROLES.SALES_EXECUTIVE` per `lib/auth/roles.ts`). So every actual exec hit the page → redirected to `/login` → reports never rendered. Fixed in `/exec/reports/page.tsx`, `/exec/reports/[reportKey]/page.tsx`, and `/exec/reports/[reportKey]/export/route.ts`. Same role gate now correctly allows `sales_executive` + `captain` + `super_admin`. **(2) Admin captain-portal /reports was a "Coming soon" stub**: replaced with a full mirror of `/captain/reports` landing — same category groups, same per-report cards, but scoped header ("Captain portal · Reports") + URL pre-filter on click (`?captain={captainId}` appended so the admin report-detail page lands with that captain pre-selected). **(3) New `/captain/tasks` + `/admin/tasks` pages** (the user wanted tasks-with-filter "in sales, executive, captain, and admin"): shared `lib/tasks/tasks-table.ts` loader that supports scope (exec/captain/global) + status filter (all/pending/postponed/completed) + date-range (?from=&to= against task_date for pending+postponed, completed_at IST for completed, OR'd for 'all') + captain + exec dropdown filters + free-text search across description + customer name + sort direction (?dir=asc|desc) + 20/page pagination. Two new client components: `<TasksTableFilters>` (URL-driven, debounced search) + `<TasksTableView>` (server-rendered table with status pills, customer links, paginator). Captain page hides the Captain column + Captain dropdown (it's always their own team). Admin page exposes both. Exec /tasks left as-is — it already has from/to on completed and pending/postponed sort oldest-first which is the right UX for daily work. **Nav entries**: `/captain/tasks` added to `lib/captain/nav.ts` ("Tasks" between My Team and Team Calendar); `/admin/tasks` added to `lib/admin-nav.ts` under Operations as "All Tasks" (after All Requests). Updated `captain-nav.test.ts` snapshot. **Pre-PR gate**: tsc + next build + 874/874 tests pass. |
| 2026-06-05 | Exec dashboard fixes: tile dedupe + revenue-today + leaderboard single-day + notif backfill + streak dormancy | **Four bugs Sandeep reported on prod, plus one dormancy nudge.** **(1) Exec dashboard had too many tiles**: dropped the `PerformanceCard` — true duplicate of `HeroMetrics` (both showed today's revenue/visits/orders in different layouts). One fewer card per render; the rest stays (Sandeep: *"Drop only duplicates"*). **(2) Revenue today showing 0**: HeroMetrics returned `EMPTY_HERO_METRICS` (all zeros) when the exec hadn't started their day plan today, even when they HAD collected payments. Fixed by falling back to `loadFinancialMetricsForDate` when no day plan exists — payments don't require a day plan, never did. **(3) Leaderboard single-day always showed weekly**: `DateRangePicker` had a "skip ?date= param when today" optimization that produced an empty querystring on single-day-today picks. The leaderboard's default-with-no-params is a 7-day range, so picking today silently fell through to the weekly default. Fixed by always emitting `?date=` in single-day mode. Captain dashboard URL gets a tiny extra param; everything else just works. **(4) Notification popup truncated**: HVA-228 composers had a `shorten()` that capped body at 160 chars BEFORE storing — defeating the whole point of the popup. The previous "fix" only affected new notifications. Backfilled the one stale prod row by joining `in_app_notifications.user_id + created_at` to `warnings.exec_user_id + message_snapshot` within a 60s window; body went from 160 chars → 363 chars. **(5) Streak dormancy line**: diagnosis showed the streak feature works but there were ZERO `VISIT_COMPLETED` or `ORDER_CONFIRMED` transitions in 16 days, so every exec's streak was 0 and the badge hid by design. New `<ExecStreakLine>` component shows: "No active streak. Last visit completed on May 19 (17 days ago). Time to get back to it." with amber tint at 3+ days, rose tint at 7+ days. Returns `null` when truly new (no qualifying history at all). Replaces the old "streakDays > 0 only" gate. `loadStreakForExec` now returns `{ days, lastActiveDay }` instead of just `number`; tests updated + 2 new cases for the dormancy paths. **Pre-PR gate**: tsc + next build + 874/874 tests pass (10 streak tests including 2 new). |
| 2026-06-05 | HVA-201 streak + notification body fix + exec warning stat cards | **Three follow-ups in one ship.** **(1) HVA-201 streak indicator finally landed**: *"Let us finish HVA 201, without over complicating it"*. Built on-the-fly — NO new schema, NO new cron (the original "needs schema + cron" framing was wrong). `lib/leaderboard/streak.ts` runs one batch GROUP BY query over the last 60 IST days of `request_status_history`, then walks backward from yesterday counting consecutive days with at least one VISIT_COMPLETED or ORDER_CONFIRMED transition attributed to the exec via `assigned_exec_user_id`. New `<StreakBadge>` shows 🔥 + count with tone scaling (orange → amber → red-orange at 14+). Dropped into the leaderboard arena rows (next to city/captain line) + the exec /dashboard as a "You've been active for N consecutive days. Keep it going." encouragement card. Today is excluded from the count so the streak doesn't flicker through the workday. **8 streak tests** in `tests/leaderboard/streak.test.ts` cover: empty history → 0, yesterday-only → 1, 5 consecutive → 5, gap breaks streak, today-only → 0 (excluded), ORDER_CONFIRMED also counts, batch returns per-exec map. **(2) Notification body full-text fix**: *"Notifications message I couldn't see complete message in popup. the only reason I need popup is to see full message."* The HVA-228 warning composers had a `shorten()` truncating to 160 chars BEFORE storing in `in_app_notifications.body`. So the popup that read from `body` got the truncated version too — defeating the whole purpose of the popup. Removed `shorten()` from all 3 exec-warning composers; full message_snapshot now stored. Drawer list already line-clamps visually so the compact list view is unaffected; the popup reads the same field and gets the full text. Whitespace-preserving render in the dialog (`whitespace-pre-wrap break-words`) so the multi-line template renders cleanly. **(3) Exec warning stat cards (replaces the verbose banner)**: *"Both designs what we have done for notifications & Sales executive dashboard warning both are not helpful... the pill which we have designed previously we have to enhance that. instead we made something which is useless. we have to soft/hard warnings number with a good design."* Built `<ExecWarningStats>` — two stat cards side-by-side: Soft (amber, count + "Soft warning(s)" label) | Hard (rose, count + "/5" threshold + "Hard warning(s)" label). When hard reaches 5/5, the right card switches to a filled-rose alert state with white type + ring + "Eligible for termination" sub-label for max salience. Number is the hero (28-32px black weight). Renders nothing when both counts are 0. The previous `<ExecWarningBanner>` file is deleted entirely — no orphan code. **Pre-PR gate**: tsc + next build + 872/872 tests pass (8 new streak tests). |
| 2026-06-05 | HVA-229: notification popup + unified targets table + exec warning highlight | **Three UX improvements rolled into one ship.** Sandeep 2026-06-05: *"Notifications when clicked could not open them. any notification when clicked should show a popup and by clicking close i have to close it. this should be in admin, captain and sales executive."* + *"Team targets page why cant we bring them in one table. we need search, pagination and filter. because today we have 32 sales executives."* + *"Soft warning and Hard Warning should Heighlight properly in Sales Executive portal."* **(1) Notification popup**: `NotificationBell` click previously navigated immediately to `linkUrl`; now opens a `Dialog` with the full title + body + relative time + optional "Open detail page" button + **Close** button. Same UX for admin, captain, exec since they all use the shared component. The detail-page navigation is preserved but opt-in — exec can read the full warning text in place before deciding to drill. Mark-as-read still happens server-side via the existing action. **(2) Unified team-targets table** (replaces the old TeamTargetArena + AdminExecWarningRoster pair on `/admin/targets`): new `lib/admin/targets-table.ts` server loader merges three sources (target progress + warning roster + captain/city) into one `TargetsTableRow` per exec, then filters / sorts / paginates server-side. URL-driven: `?q=` (search across exec/captain/city), `?captain=` / `?city=` / `?status=` (filters), `?sort=` + `?dir=` (column sort: name, orders, revenue, combined, softActive, hardActive), `?page=` (1-based, 20/page). New `TargetsTableFilters` (client component, debounced search + 3 dropdowns + clear-all button); `TargetsTableView` (server-rendered table with sortable column headers, progress bars per cell, soft/hard count pills, WarningButtons in the rightmost cell, aggregate strip above, paginator below). Eligible-for-termination rows get a subtle rose row tint. Built to scale to 32 execs cleanly. **(3) Exec warning highlight** (replaces the small `WarningCountsPill` on `/dashboard`): new `ExecWarningBanner` — a proper alert card with severity-coded colour scheme (amber for soft-only, rose for any hard, deep-rose for 5/5), showing a heading, subline, the most recent active warning's metric/period/values + reason snippet, and Sandeep's contact phone as a tel: link. The "View details" link is reserved for a future exec-side history page; for now it's omitted on exec to avoid breaking the existing nav. `/today` not yet updated — its multi-return structure needs a refactor; deferred. **Files touched**: `components/notifications/NotificationBell.tsx` (popup), `lib/admin/targets-table.ts` + `components/targets/TargetsTableFilters.tsx` + `components/targets/TargetsTableView.tsx` (table), `app/admin/targets/page.tsx` (rewired), `components/warnings/ExecWarningBanner.tsx` (banner), `app/(exec)/dashboard/page.tsx` (banner drop-in). **Pre-PR gate**: tsc clean, next build clean, 864/864 tests pass — no new tests added in this ship (UI-heavy; no new server logic worth covering beyond what's already tested via the underlying loaders). **Deferred / open**: `/today` page exec warning banner (multi-return refactor needed); `/captain/team/[execId]` exec-drill warnings link in the existing header (not in this PR — drill page works via direct URL); HVA-201 streak indicator (still pending — needs schema + cron, see Linear). |
| 2026-06-04 | HVA-228: soft + hard warnings | **Performance-management feature.** Sandeep 2026-06-04: *"We want to develop a feature called soft warning and hard warning in admin portal … to warn sales executives who are not performing or not meeting their targets."* Admin issues warnings from the targets section; 5 active hard warnings flag the exec for termination (manual fire, not auto). **Schema** (migration 0061): new `warnings` table with `exec_user_id`, `kind` ('soft'\|'hard'), structured metric snapshot (`metric_code`, `period_label`, `current_value`, `target_value`), `reason` (10–500 chars required), `message_snapshot` (text rendered at issue time), `issued_by_user_id`, plus revoke fields (`revoked_at`, `revoked_by_user_id`, `revoked_reason`). Counts derived via `WHERE revoked_at IS NULL` — no delete, no soft-delete tombstone. **Server actions**: `issueWarningAction` (super_admin-gated, builds the templated message via composer, inserts row, dispatches notification fire-and-forget); `revokeWarningAction` (admin-only after offline captain discussion); `deactivateExecAction` (manual fire — double-checks `hardActive >= 5` server-side). All audited. **Notifications** (migration 0062 + 5 composers): 4 events shipped — `exec.soft_warning_issued` (in-app + push), `exec.hard_warning_issued` (in-app + push + WhatsApp **shipped disabled** until `internal_hard_warning_v1` Meta template approves), `exec.warning_revoked` (in-app + push), `exec.fifth_hard_warning` (in-app + push to super_admin so Sandeep sees the banner trigger in his own drawer). Channel literal is `push` not `web_push` (matches existing HVA-54 enum). **UI components** (new `components/warnings/` directory): (1) `IssueWarningDialog` — admin form with 5 structured inputs (metric / period / current / target / reason) + live preview pane showing the exact message before send, since *"when the admin clicks the button, you have to show all the variables that have to be filled"*; (2) `WarningButtons` — Soft + Hard outlined buttons, amber + rose; (3) `WarningHistoryList` — chronological list with expand-message + per-row Revoke (admin only); (4) `WarningCountsPill` — compact "N soft · N/5 hard" pill, slate/amber/rose tone based on severity; (5) `FireExecBanner` — 5/5 rose banner with a separate Deactivate button gated to active hard ≥ 5; (6) `AdminExecWarningRoster` — table on /admin/targets below the existing arena, one row per exec with name + captain + counts + compact buttons. **Pages**: `/admin/settings/organization/executives/[id]/warnings` (admin drill — counts pill + fire banner + WarningButtons + history with revoke); `/captain/team/[execId]/warnings` (captain read-only — same data, no buttons); `/admin/targets` augmented with the AdminExecWarningRoster below the arena; exec dashboard gets the WarningCountsPill below the header (only when any active warning exists). **Templates** (in-app + push): drafted approved-as-shipped messages — soft is motivational ("If something's blocking you — reach me at +91 98856 98665"); hard is strict ("hard warning N/5 … At serious risk … speak to your captain within 48 hours"). Sandeep's "from Sandeep" signature on both. Stored as message_snapshot so audit survives future template edits. **Tests**: `tests/warnings/warnings.test.ts` — 10 cases covering metric formatting (paise→₹, count, percent), template rendering (exec name + numerical context + reason + 5/5 framing on hard), counts ignore revoked rows, fireFlag flips at exactly 5, history orders desc with revoker names resolved, roster aggregates per-exec. `tests/helpers/db.ts` updated to truncate `warnings` before users (FK restrict). Pre-PR gate: tsc + next build + 864/864 tests pass. **Operational note**: Sandeep still needs to (a) write + submit `internal_hard_warning_v1` UTILITY template to Meta, (b) enable the WhatsApp rule via `/admin/settings/notifications/rules` once approved (or run the SQL update). |
| 2026-06-04 | HVA-227: graphs date-range filter | **Follow-up to HVA-226.** Sandeep 2026-06-04 on prod: *"why i cant add from date and to date in both reports & graphs why are you skipping those ?"* — table reports already had `<ReportFiltersBar>` with date inputs, but I shipped HVA-226 graphs without one (mea culpa — I'd noted it as a follow-up in the Linear ticket, should've just done it). **New** `<GraphsDateRangeFilter>` client component: two `<input type="date">` + Apply button + three quick presets (7d / 30d / 90d) URL-driven via `?from=YYYY-MM-DD&to=YYYY-MM-DD`. Active preset gets the primary-color pill style; custom window shows a `from → to (N days)` caption. Client-side clamps `from <= to` before submit; server-side `isValidIstDate` rejects malformed and falls back to the 30-day default, with an additional server clamp for defence in depth. **Wired into all 3 graphs pages** (admin / captain / exec) — each parses `searchParams` for `from`/`to`, hands them to `loadGraphsBundle()`, and passes a recalculated `windowLabel` so empty-state messages reflect the user's window. The `<GraphsView>` and 6 chart components are untouched — the only mod is the page wrappers + the new filter component. **Tests**: 2 new cases in `tests/reports/graphs.test.ts` — `graphRevenueTrend` zero-fills a custom 7-day window AND a 90-day window, confirming the day-bucket builder respects user-supplied ranges. Total 16 graph tests, 854 in the full suite. Tsc clean, next build clean. **Follow-ups still open**: bucket toggle (day/week/month — currently day-only). |
| 2026-06-04 | HVA-226: reports/graphs visual dashboard | **Sprint 4 graphs page lit up.** Sandeep 2026-06-04: *"In reports Graph Section is pending, shall we start working on it"* → *"make it look beautiful and responsive. no errors and proper testing."* Replaces the long-standing `/admin/reports/graphs` placeholder ("ships in Sprint 4") with a curated 6-chart visual dashboard, mirrored to `/captain/reports/graphs` (captain scope) and `/exec/reports/graphs` (self scope). **Charts** (recharts 3.8.1, added as a new dep — first chart usage in the codebase, justified by 6 charts × 3 portals = 18 reuses): (1) Revenue trend — area chart with gradient fill, net cash (inbound − outbound) per IST day; (2) Visits & orders by day — grouped bar, two series, DISTINCT request_id per stage code; (3) Conversion rate trend — smooth line, daily orders÷quotations percent; (4) Top execs by orders — horizontal bar, top 5 by `assigned_exec_user_id` attribution; (5) Status funnel — horizontal bar, distinct requests reaching each stage, progressive fade; (6) City revenue share — donut with center total + side legend, omits zero-revenue cities. **Bundle loader** `loadGraphsBundle()` runs the 6 queries in parallel server-side; `<GraphsView>` is a server component that hands each card off to a client component (recharts is client-only). Default window: last 30 IST days via `defaultReportRange()`. **SSOT discipline**: net cash math, IST timezone wrap on every timestamptz date cast, DISTINCT request_id on status_history joins, exec attribution via `visit_requests.assigned_exec_user_id` (NOT the action-taker captain — `feedback_attribution-vs-action-taker.md` rule). **Empty states**: every chart degrades to an icon + helper text when zero data; revenue/visits/conversion are zero-filled across all days in the window so the X-axis is continuous. **Mobile**: 1-col stack on phones, 2-col grid from `lg:`. Cards have hover-shadow polish. Charts use `<ResponsiveContainer>` so they scale to the parent width. **Theme**: shared `chart-theme.ts` with Deep Teal #0F766E primary palette + 7 accent colors; tooltip uses the popover background tokens so it matches dark mode. Lakh/Crore shorthand for paise axis labels. **Landing-page CTAs**: captain + exec reports landings get the same "Graphs view" CTA the admin landing already had. **Tests**: 14 cases in `tests/reports/graphs.test.ts` covering: zero-fill on empty days, net cash math (inbound 100 − outbound 30 = 70 paise), DISTINCT request_id (one request reaching VISIT_COMPLETED+ORDER_CONFIRMED counts 1+1, not 1+2), attribution to assigned exec (not the captain clicker), exec scope isolation (ExecA's 500 paise vs combined 1400 paise). HVA-225 transition test side-effect fix (captain-reject row tightened to `requires_reason=true` in HVA-225 ship needed an explicit `reason:` in two no-op-flag tests). Pre-PR gate: tsc + next build + 852 tests all clean. |
| 2026-06-04 | HVA-225: engine refactor + admin full edit | **Phase 4 — `lib/status-transition.ts` engine is now table-driven.** Replaces the legacy `allowForwardSkip` / `allowRollback` / `allowSpecificBackwardTransition` caller-flag validation with a single `status_transitions` lookup. The flags stay in the function signature for back-compat (no-ops; the seed covered every legal pair). Validation gates added: `is_active=false` → `TRANSITION_INACTIVE`, `allowed_role` mismatch → `ROLE_NOT_ALLOWED` (super_admin bypasses), `requires_reason=true && empty reason` → `REASON_REQUIRED`, `requires_quotation=true && no quote row` → `QUOTATION_REQUIRED`. All four are new rejection paths; existing pass paths preserved by the seed. **Event dispatch generalised**: the hardcoded `customerStageEventMap` + `PENDING_CAPTAIN_APPROVAL → request.pending_approval` block is gone; replaced with a single `transitionRow.emitsEvent` lookup. Same fire-and-forget contract. **Full per-row admin edit modal**: `<EditTransitionModal>` lets admin edit `allowed_role` (4 options) / `requires_reason` / `requires_quotation` / `requires_datetime` / `auto_task_type` (7 options) / `is_active` / `description` per transition. `emits_event` stays read-only. The Phase A `requires_datetime` guard now lives on a per-edit basis: admin can edit other fields freely, but enabling the calendar on non-VISIT_SCHEDULED transitions returns a friendly error pointing at HVA-226 (the generalised calendar plumbing follow-up). **Regression**: the existing 46 transition-route tests pass unchanged after the refactor (`mark-installation-complete`, `approve`, `reassign`, `captain-assign`) — confirms the table seed mirrors the legacy flag logic byte-for-byte. Tsc + next build + full suite clean. |
| 2026-06-04 | HVA-224: three workflow knobs | **Phase 3 of admin-configurable lifecycle.** Three net-new admin behaviours that the codebase didn't enforce before. **(1) Approval timeout** — config key `pending_captain_approval_timeout_hours` (default 24, 0=off) + new cron `/api/cron/escalate-stale-approvals` that runs hourly and emits `request.approval_overdue` for any PENDING_CAPTAIN_APPROVAL row stuck past the threshold. Deduped via the new `approval_escalated` audit event (one escalation per request per breach window). **(2) Refund window** — config key `refund_window_days` (default 30, 0=off) + enforcement in `app/api/requests/[id]/payments/route.ts` — outbound payment rejected with HTTP 409 when ORDER_CONFIRMED was more than the window ago. **(3) Audit retention** — config key `audit_log_retention_months` (default 24, 0=off) + new cron `/api/cron/prune-audit-log` daily 02:30 IST deletes audit_log rows older than threshold. Records the count via new `audit_log_pruned` audit event so the policy itself stays traceable. **Three admin pages** under `/admin/settings/workflow/{approval-timeout,refund-window,audit-retention}` using a shared `<NumericConfigClient>` + `setNumericWorkflowConfigAction` so each page is ~30 lines. **Nav**: three entries added under Workflow & Status (Status Stages / Transitions / Approval Timeout / Refund Window / Audit Retention / Holidays). **Audit events**: `approval_escalated` + `audit_log_pruned` added to defaults. Tsc + next build + tests clean. **Cron setup left as Sandeep's one-line action**: register the two new crons in the host crontab (15 \* \* \* \* curl … escalate-stale-approvals; 30 2 \* \* \* curl … prune-audit-log). |
| 2026-06-04 | HVA-223: transitions table + admin calendar toggle | **Phase 2 of admin-configurable lifecycle.** Sandeep 2026-06-04: *"If I want any stage that requires a calendar then …"* — generalises the hardcoded `VISIT_SCHEDULED` calendar check so admin can toggle the picker per transition. **New table** `status_transitions` (migration 0060) — one row per legal (from, to) pair, seeded from current hardcoded enforcement: 9 forward +1 / 1 forward_skip (HVA-68 mark-installation-complete) / 9 rollback -1 (HVA-141) / 1 specific_backward (HVA-137 captain reject), 20 rows total. Columns: `kind`, `allowed_role`, `requires_reason`, `requires_quotation`, `requires_datetime`, `auto_task_type`, `emits_event`, `description`, `is_active`. UNIQUE on (from_stage_id, to_stage_id). Migration is idempotent (`CREATE TABLE IF NOT EXISTS` + `ON CONFLICT DO NOTHING` on the seed). **`requires_datetime` is now table-driven**: `app/requests/[id]/page.tsx` looks up the (current, next) pair via new `loadTransitionByPair()` helper (React-cache'd) and passes `requiresDatetime` to `<AdvanceStatusButton>`, replacing the old hardcoded `nextStatus.code === 'VISIT_SCHEDULED'` check. **New admin page** at `/admin/settings/workflow/transitions` shows every transition in a table with kind / role / reason / quote / datetime-toggle / auto-task / event columns. **Phase A guard**: the calendar action plumbing (auto-task creation + visit_scheduled_at column) is only wired for transitions where `to_code = 'VISIT_SCHEDULED'`. `setTransitionRequiresDatetimeAction` returns a friendly error when admin tries to enable on other transitions, pointing at HVA-225 (the side-effect generalisation ticket). **Other flags are read-only display** in this PR — engine enforcement remains in `lib/status-transition.ts`. Admin gets full visibility of the workflow rules; HVA-225 will migrate enforcement to the table. **Nav**: "Transitions" entry added under Workflow & Status, sibling of "Status Stages". **Audit**: new `status_transition_changed` event type captures before/after JSON. **Regression test** at `tests/admin/transitions.test.ts` — 9 cases asserting seed coverage (20 rows / kind counts), the right pair carries the datetime flag, HVA-68/HVA-137 pairs are correctly seeded, rollback rows match -1 sequence delta, loader returns null for unseeded pairs. Tsc + next build + tests clean. |
| 2026-06-04 | HVA-222: status stages CRUD | **Phase 1 of admin-configurable lifecycle.** Sandeep 2026-06-04: *"there are multiple stages in the request section. Today, those multiple stages are static. Now admin has to have complete configuration control to modify those settings."* After an exhaustive audit of admin-configurable surfaces (~80 entries), Sandeep approved a 3-PR sequence: HVA-222 = status stages catalog (this PR), HVA-223 = transition rules + side-effects (next), HVA-224 = net-new behaviour (approval timeout, refund window, audit retention). **Edit-in-place pattern** — `code` stays the immutable identifier so `lib/status-transition.ts` constants and existing `visit_requests.status_stage_id` rows keep working. Admin can edit name / sequence_number / is_active / is_terminal / description. **Migration 0059** adds `is_terminal` + `description` columns, drops the UNIQUE constraint on `sequence_number` (replaced with a non-unique ORDER BY index) so admin can reorder without temp-value gymnastics, and backfills `is_terminal=true` on the highest-seq stage. **New page** at `/admin/settings/workflow/status-stages` — table sorted by sequence, each row shows code (mono, read-only) + label + sequence + is_active toggle + is_terminal toggle + description + "× requests on this stage" counter + inline Edit + per-row Delete (disabled if any requests reference the stage). Add modal validates UPPER_SNAKE_CASE code regex. **Server actions** at `app/admin/settings/workflow/status-stages/actions.ts` — `updateStatusStageAction` / `createStatusStageAction` / `deleteStatusStageAction`, all super_admin-gated + audited via the new `status_stage_changed` event type with before/after JSON. Delete fails gracefully with row count when FK-bound. **Data layer** at `lib/admin/status-stages.ts` — `loadStatusStagesWithCounts()` does two parallel queries (stage catalog + GROUP BY count) instead of correlated subquery, since Drizzle's `${visitRequests}` interpolation fights with self-join aliasing. **Nav**: "Status Stages" entry added under Workflow & Status in `ADMIN_NAV`. **Regression test** at `tests/admin/status-stages.test.ts` — 7 cases. Tsc + next build + tests clean. |
| 2026-06-04 | HVA-221: list-page UX sweep | **Three list pages brought into compliance with the universal "filter + search + pagination" policy.** Sandeep 2026-06-04 on prod: *"do we have paginations for all pages. we are not skipping this right ?"* Audit found 8 list pages without pagination; the recent HVA-50 notification rules editor (shipped same morning in PR #228) was itself one of them — fresh regression to fix. **Fixed**: (1) `/admin/settings/notifications/rules` — URL-driven search across event_type/recipient_role/template_key + event_type dropdown + channel dropdown + SQL pagination at 50. Server-side filters via `ilike` + `eq` predicates on `notificationRules`. (2) `/captain/team` — already had search; added SQL pagination at 50 with `limit/offset` + COUNT(*) total. (3) `/admin/settings/organization/executives` — added client-side search box (filters name/phone/email/captain/city in-memory) on top of the existing captain dropdown filter + client-side pagination at 50. **Deferred** with explicit rationale on the Linear ticket: 5 remaining pages (cities=9 fixed, captains=1 in cycle, audit categories=10 fixed, announcement categories=5 fixed, captain announcements=1) are all sub-30-row fixed-or-quasi-fixed sets where pagination is moot — follow-up if any grows past 30 rows. Tsc + next build + tests clean. |
| 2026-06-04 | HVA-202: Finance snapshot calc FIX | **TWO calc bugs on the Finance hero tiles fixed.** Sandeep 2026-06-04 on prod: *"Calculations in finance dashboard, the error is still showing. Calculations are not right. After finishing these tests, try to check the calculation's of Received & Outstanding in finance Dashboard"* and then *"in finance >> Received >> check the table. both values are different. Card is showing 222801 table is showing 293801"*. **Bug A — Outstanding under-reported by ₹25,801**: `loadFinanceSnapshot` computed `outstandingPaise = totalQuotedPaise − receivedPaise` as a single portfolio-level subtraction. Two over-paid customers on prod (Modi: ₹83k paid vs ₹60k quoted → row outstanding −₹23k; KCR: ₹52.8k paid vs ₹50k quoted → row outstanding −₹2.8k) silently cancelled the legitimate outstanding from other customers. The Outstanding tile showed ₹272,199 instead of the true ₹298,000 owed. **Bug B — Received tile mismatched its own drilldown table by ₹71,000**: the snapshot's Received calc INNER-JOINed `quotations`, dropping 2 pre-quote deposits (₹71k) — but the Received drilldown table (loadFinanceReceivedDetail) doesn't join quotations. Tile said ₹222,801, the table said ₹293,801. **Fix**: per-row clamp — compute `outstanding = SUM(GREATEST(0, quoted - net_paid))` and `creditsOwed = SUM(GREATEST(0, net_paid - quoted))` separately, looping over a per-request CTE. Received now sums across EVERY non-cancelled request regardless of quote presence, matching the drilldown table by construction. Both new fields (`creditsOwedPaise` + `creditsOwedCount`) added to the `FinanceSnapshot` type. **UI**: `<FinanceSnapshot>` renders a 5th amber "Credits owed" tile when creditsOwedPaise > 0 (hidden otherwise — keeps existing 4-tile layout intact for clean books). Tile links to a new `/credits-owed` slug in `FinanceTileDetailPage` which renders `OrderDetailTable` filtered to `outstandingPaise < 0` with the amount displayed as positive under a "Credit owed" column. **Regression test**: `tests/captain/finance-snapshot.test.ts` — 3 cases: (1) over-paid row doesn't cancel another row's outstanding + credits surface separately, (2) refund nets correctly (Singham 75k quoted + 5k inbound + 10k outbound refund → outstanding 80k credits 0), (3) Received includes pre-quote deposits. Reconciliation invariant asserted: `totalQuoted + creditsOwed = received_on_quoted + outstanding`. **Production impact**: Outstanding tile jumps from ₹272k to ₹298k (+₹25.8k correction surfaced as Credits owed); Received tile jumps from ₹222.8k to ₹293.8k (matches drilldown table). Tsc + next build + 822 tests clean. |
| 2026-06-03 | Reports Sprints 2-4 + admin polish | **44-report library complete + admin captain-portal polish + All Requests admin page + HVA-50 notification rules editor.** Sandeep 2026-06-03 confirming the 5-task batch: *"Ship all reports as you planned. dont wait for my approval for all 1 to 5 tasks. i want you to properly execute. Mainly in reports verify all calculations properly... by the way use the same reports in captain and executive portals also."* and *"Dont skip pagination. Filters and Sort."* **Reports build (34 new loaders on top of Sprint 1's 10)**: `lib/reports/team.ts` — 8 per-exec + captain rollup loaders, centralised `loadExecAggregates` does 5 parallel queries (payments / tasks / orders / quotations / contacts) once and rebuilds every team report on top. `lib/reports/geography.ts` — 4 per-city loaders + 8 operational (DayPlanClose / TaskRollover / ApprovalsQueueDepth / ApprovalSla / CancellationTrend / RefundTrend / OutstandingAging). Approval SLA + aging use raw SQL CTEs (LATERAL latest_transition; aging CASE). `lib/reports/lifecycle.ts` — 4 lifecycle reports (StatusFunnel / StuckRequests / AverageStageTime via LEAD() window / RequestIntake). `lib/reports/customer.ts` — 4 customer reports (NewLeads / LeadConversion / RepeatCustomers via GROUP BY phone HAVING COUNT>1 / RequestDistribution BHK). `lib/reports/notifications-targets.ts` — 6 reports across whatsapp_dispatches (per-template counts, delivery rates, failure reasons) + targets (per-exec achievement, per-city rollup with proportional split for multi-city execs, pacing). **Captain mirror** at `/captain/reports/*` reuses the registry with `scope: { kind: 'captain', captainUserId }` — every loader respects scope via `lib/reports/scope.ts` helpers (`vrScope` / `tasksScope`). **Exec mirror** at `/exec/reports/*` with `scope: { kind: 'exec' }` and `Team` + `Geography` categories filtered out at the landing. Both surfaces have the same filter bar + bucket toggle + sort header + pagination + CSV export route as admin. Exec nav drawer gains a `Reports` entry. **Admin captain-portal polish**: `/admin/portal/[captainId]/approvals` upgraded from MVP top-5 list to full mirror — search + exec-dropdown filter, per-row exec note + completedAt, pagination, no approve/reject (admin is read-only). `/admin/portal/[captainId]/contacts` upgraded from MVP top-50 to full mirror — search + exec dropdown + type filter + pagination, reusing `fetchTeamContacts`. **All Requests admin page** at `/admin/operations/requests` — replaces the long-standing placeholder. Bucket tabs (All/Open/Assigned/Completed/Cancelled) + city + exec filters + search + pagination, powered by `fetchCaptainRequests({ isSuperAdmin: true })` so no team-scope predicate. Admin sidebar entry updated. **HVA-50 notification rules editor** at `/admin/settings/notifications/rules` — groups all `notification_rules` rows by `event_type`, renders one row per `(channel, recipient_role)` with a toggle switch. Switch flips `enabled` via `toggleRuleAction` (super_admin only, audited via `notification_rule_toggled` event). Body composition still lives in code; `template_key` is shown read-only as a future hook. **Push notifications** verified — entire stack is already wired (web-push package + `lib/notifications/channels/web-push.ts` adapter + `/api/push/subscribe` POST/DELETE + sw.js push/notificationclick handlers + `<PushPromptBanner>` on all three layouts + VAPID env vars on prod). **Test guide**: walk-through saved to `/var/www/beakn-docs/beakn-test-guide.md`. Tsc + next build + 819 tests clean. |
| 2026-06-03 | Finance tile dedicated pages | **Tile click now navigates to a full page with the table — sheet pattern dropped.** Sandeep on prod after the earlier sheet ship: *"I don't understand why it is not opening a separate page and showing a proper table in that ? It has to open a separate page and show a proper table."* Re-architected to dynamic routes. **New routes** (one dynamic segment per portal): `/captain/collections/[tile]`, `/finance/[tile]`, `/admin/portal/[captainId]/collections/[tile]`. The dynamic `[tile]` accepts the four slugs `order-book` / `pipeline` / `received` / `outstanding`; anything else → 404. The pre-existing `/captain/collections/calendar` + `/finance/calendar` static routes still take precedence (Next.js App Router rule — static > dynamic). **New shared renderer** `<FinanceTileDetailPage>` at `app/(captain)/captain/collections/_components/FinanceTileDetailPage.tsx` accepts a scope (captain / exec self-view / admin-portal), back-link target + label, and a page number. Loads up to 250 rows via the same `loadFinanceOrderList` / `loadFinanceReceivedDetail` queries used in the slice. **Two table flavours** in `<FinanceTileDetailTable>`: `<OrderDetailTable>` (Order Book / Pipeline / Outstanding — columns: Customer with phone subline / City / Exec / Stage / Quotation / Paid net / Outstanding / Age days) and `<PaymentDetailTable>` (Received — columns: Date / Customer / Amount / Mode / Recorded by / Exec; refunds rendered as negative rose-coloured rows; reconcile footer shows `Inbound ₹X − Refunds ₹Y = Net ₹Z` so the math matches the tile headline). **FinanceSnapshot reverted** to a server component — tiles are now `<Link>`s with hover-lift, removed the open-tile state machine + sheet mounting. `<FinanceTileSheet>` component deleted. Tsc + next build clean. |
| 2026-06-03 | Finance tile drilldowns | **Finance hero tiles (Order Book / Quotation Pipeline / Received / Outstanding) become clickable buttons** that open a slide-over Sheet with a sortable table of the underlying rows — across all three portals. Sandeep 2026-06-03: *"when we click on the tiles we have to provide a detailed summary in a tabular format."* **Tile semantics in the table**: (1) Order Book — confirmed-order requests with Customer / City / Quotation / Paid (net) / Outstanding / Age days. (2) Quotation Pipeline — pre-confirmed quotations with Customer / City / Quotation / Submitted date / Age days, no Paid column. (3) Received — payment-level rows with Date / Customer / Amount / Mode / Recorded-by; refunds render as negative rose-coloured rows so the tile total + the row sum line up exactly. (4) Outstanding — filters the all-quotations set to rows where outstanding > 0, sorted by outstanding desc. Each sheet is capped at 100 rows with a "See full list with filters and pagination →" footer link to the page's main filtered list below the tiles. **New loader**: `loadFinanceReceivedDetail` in `lib/captain/finance-queries.ts` returns per-payment rows scoped via the existing `resolveScope` helper so captain / exec self-view / admin-portal all reuse the same scoping rules. The other three sheets reuse `loadFinanceOrderList` with `pageSize: 100`. **Components**: new `<FinanceTileSheet>` client component handles the slide-over + table rendering with two table flavours (FinanceOrderRow / FinancePaymentRow). `<FinanceSnapshot>` becomes a client component that holds the open-tile state, renders the 4 tiles as `<button>`s with hover-shadow / focus-ring affordances, and mounts the 4 sheets. **Wired into**: `/captain/collections`, `/(exec)/finance`, `/admin/portal/[captainId]/collections`. The exec-portal page gets the drilldown for the first time (it was MVP before); the admin-portal page also gets a `<ViewOnlyNotice>` banner. Tsc + next build + tests clean. |
| 2026-06-03 | Refund + portal Ship 3 | **CRITICAL calc fix — refunds were not subtracting from revenue / paid / outstanding** + admin captain-portal team & requests upgraded to full mirrors with exec drill. Sandeep on prod 2026-06-03: *"For Singham, today we added ₹5,000 payment and refunded ₹10,000. Now overall order value 75,000. But screens show 65,000 pending in captain Hyderabad open requests, and Revenue ₹5,000 in dashboards — the same ₹5,000 we refunded back."* Root cause: 8 places summed `direction = 'inbound'` only and ignored outbound refunds. **Files fixed (net = inbound − outbound on non-voided payments)**: `lib/metrics/revenue.ts`, `lib/metrics/outstanding.ts` (paid subquery), `lib/captain/dashboard-queries.ts:loadPendingCollections` (paid subquery), `lib/captain/exec-drill-queries.ts:loadExecPendingCollections` (paid column), `lib/admin/city-drill-queries.ts:loadCityOpenRequests` (per-row outstanding subquery), `lib/leaderboard/queries.ts` (revenue metric), `lib/exec/target-progress.ts:loadAllExecTargetProgress` + `loadOneExecTargetProgress` (revenue meters), `lib/exec/dashboard-queries.ts:loadExecBestOfPeriod` (top customer), `lib/today/metrics.ts:loadFinancialMetricsForDate` + `loadHeroMetricsAtSubmissionTime` (exec today hero revenue). `inboundPaymentCount` retained where it appears in the schema — uses `COUNT(*) FILTER (WHERE direction='inbound')`. The two places already netting correctly (`lib/captain/finance-queries.ts`, `lib/captain/sidebar-counts.ts`) were left untouched. **Net effect**: Singham's refund now correctly subtracts. Revenue tile = ₹0 (5k inbound − 5k inbound − 10k outbound = -10k for today's view, but across history net = 0 for the request). Outstanding tile on the request = ₹75,000 (quotation total, net paid = 0). All cross-portal tiles now agree by construction. **Portal Ship 3 additions** (also in this PR): `/admin/portal/[captainId]/team` upgraded from MVP roster list to full mirror of `/captain/team` — TeamSearchInput + TeamWindowToggle + per-member TeamMemberCard with active-request / overdue / contacts stats; new exec drill at `/admin/portal/[captainId]/team/[execId]` mirroring the captain's 7-tab drill (Today's Plan / Calendar / Performance / Open Requests / Pending Collections / Red Flags / Audit Trail) — same components, same queries, just URL-scoped and read-only (admin doesn't own the request → server-action mutations would no-op). `/admin/portal/[captainId]/requests` upgraded from MVP 100-row list to full mirror — RequestBucketTabs + RequestsFilterClient + RequestsTable + RequestCardMobile + Pagination, all URL-state-driven. Shared component contract additions (all default to `/captain` paths so the live captain UI is byte-identical): `basePath` on TeamMemberCard / TeamSearchInput / TeamWindowToggle / ExecDrillTabsNav / PendingApprovalsCard / PendingCollectionsCard, `backFallback` on ExecDrillDownHeader, `pathname` on DashboardHeader. Tsc + next build + 819 tests clean. |
| 2026-06-03 | Admin captain-portal Ship 2 | **12 captain pages mirrored read-only under /admin/portal/[captainId]/...** Ship 1 wired the dashboard + layout + nav strip; Ship 2 makes every link in that strip lead somewhere. **Direct mirrors** (no behavioural compromise, identical UI): `/admin/portal/[captainId]/leaderboard` (LeaderboardView with basePath retargeted), `/admin/portal/[captainId]/targets` (TeamTargetArena scoped to URL captainId), `/admin/portal/[captainId]/announcements` (AnnouncementsView with the captain's ack-rate merge), `/admin/portal/[captainId]/resources` (ResourcesView for captain role), `/admin/portal/[captainId]/profile` (ProfileView of the captain's account), `/admin/portal/[captainId]/calendar` (CalendarClient + CalendarFiltersBar with the captain's team calendar events), `/admin/portal/[captainId]/assist` (AssistListView with basePath + detailPath retargeted), `/admin/portal/[captainId]/reports` (stub mirror of the captain stub). **MVP list pages** (data correct, full filter+pagination polish deferred): `/admin/portal/[captainId]/team` (active exec roster), `/admin/portal/[captainId]/requests` (active non-cancelled, non-terminal requests in the captain's cities; capped at 100), `/admin/portal/[captainId]/approvals` (loadPendingApprovals queue + stale flag), `/admin/portal/[captainId]/collections` (loadPendingCollections summary + aging buckets), `/admin/portal/[captainId]/contacts` (fetchTeamContacts for the captain's team; first 50). Every MVP list page renders a small `<ViewOnlyNotice>` strip at the top explaining edits are disabled in admin view. **Behaviour on writes**: the captain server actions all gate on session.user identity, so admin clicks on any edit button (where visible) no-op at the action layer — admin can't accidentally write as the captain. Ship 2.5 polish work: replace MVP list pages with full mirrors of the captain pages' filter bars + per-row actions visibly disabled (vs structurally hidden today). Tsc + next build + 819 tests clean. |
| 2026-06-03 | Admin captain-portal Ship 1 | **Replace clumsy city drill with read-only captain portal mounted inside admin shell.** Sandeep 2026-06-03: *"the way the city is look is not good. we need to have one single button to open complete city portal. this city portal has to be a view access. it has to be exact replica of caption portal. with out editing features. the city dashboard which is the captain portal is looking too clumsy."* Confirmed defaults: same date picker as captain, buttons disabled (Ship 2 layer), one button only on city tile, captain dashboard shows mix of all their cities (the captain's natural scope, not city-scoped). **Architecture**: URL is captain-id-scoped — `/admin/portal/[captainId]/dashboard` — so clicking ANY city the captain owns lands on their full multi-city dashboard (one button, one view). **New layout** at `app/admin/portal/[captainId]/layout.tsx` wraps the page in: (1) a sticky amber "VIEW MODE — Admin viewing &lt;captainName&gt;'s portal" banner with an Exit link back to the admin dashboard; (2) a horizontal nav strip mirroring `CAPTAIN_NAV_ITEMS` but with hrefs rewritten to `/admin/portal/[captainId]/&lt;segment&gt;` — so navigating between Dashboard / Requests / Team / etc. stays inside the admin shell. The outer `/admin/layout.tsx` already gates super_admin and provides the admin sidebar; this layout adds onto it. **New page** at `app/admin/portal/[captainId]/dashboard/page.tsx` reuses the captain dashboard components verbatim (`<DashboardHeader>` + `<PerformanceCard>` + `<PendingApprovalsCard>` + `<PendingCollectionsCard>` + `<ExecStatusList>`) and feeds them the same captain queries (`loadTeamPerformance(captainId)` + the rest) — no data forks, no math drift. Date picker URL state lives at `/admin/portal/[captainId]/dashboard` via a new `pathname` prop on `<DashboardHeader>` (defaults to `/captain/dashboard` so the live captain dashboard is unchanged). **Card retargeting**: `<PendingApprovalsCard>` and `<PendingCollectionsCard>` gain an optional `basePath` prop (defaults `/captain`) that prefixes the "View all" + per-row links, so admin clicks navigate to `/admin/portal/[captainId]/approvals` etc. (Ship 2 wires those pages). **AdminCityGrid retarget**: city tile now opens `/admin/portal/&lt;captain.userId&gt;/dashboard` in a new tab when a captain is assigned; cities without a captain fall back to the legacy drill (one tile per setup-required city). **/admin/operations/cities/[cityId] simplified to a redirect** — the previous 5-section clumsy page (PendingApprovals + Collections + ExecStatus + OpenRequests + Roster) is gone; the route resolves city → captainId → 302 to the portal. Bookmarks still work. Read-only enforcement on Ship 1 is structural — the dashboard surface has no edit buttons to disable. Ship 2 wires `readOnly` semantics across the interactive captain pages (Requests / Approvals / Team / Collections / Calendar / Contacts / Assist) and adds the remaining 12 captain pages as `/admin/portal/[captainId]/&lt;page&gt;` mirrors. Tsc + next build + 819 tests clean. |
| 2026-06-03 | Admin city drill | **Captain-level surfaces reinstated inside the admin shell.** Sandeep 2026-06-03: *"in admin >> dashboard >> city >> I could not see a complete city portal of the captain"*. The drill page already had city header + window metrics + open requests + exec roster, but was missing the three operational lists that make the captain dashboard useful: Pending Approvals, Pending Collections, and the per-exec Status list (with day-plan state, today's visits/collections, overdue flag). Adding them required new city-scoped variants on the captain loaders — extracted as parallel exports so the existing captain dashboard wrappers stay unchanged: **`loadPendingApprovalsForCityIds(cityIds, filter)`** (the existing `loadPendingApprovals` is now a thin wrapper that resolves cityIds from `cities.captain_user_id`), **`loadPendingCollectionsForExecIds(execIds, filter)`** (existing version resolves execIds via `loadCaptainTeamIds`), **`loadExecStatusesByCityId(cityId, filter)`** + shared `loadExecStatusesByFilter(rosterFilter, filter)` helper (existing `loadTeamExecStatuses` becomes a thin wrapper that passes `eq(salesExecutives.captainUserId, captainUserId)`). `lib/admin/city-drill-queries.ts` exposes the city-direct facade: `loadCityPendingApprovals`, `loadCityPendingCollections`, `loadCityExecStatuses` — each thin wrappers that turn a `cityId` into the appropriate scope. The drill page consumes these alongside the existing window-metrics + open-requests + exec-roster loaders in one `Promise.all` (7 parallel queries, TTFB bounded by the slowest). Three new sections render at the top of the right column: `<PendingApprovalsCard>`, `<PendingCollectionsCard>`, `<ExecStatusList>` — the same components the captain dashboard uses, no fork. Same date picker drives all of them (URL state `?date=` or `?from=&to=`). Net effect: opening the admin city tile now shows everything a captain would see for that city, without escaping into the captain shell. Tsc + next build + 819 tests clean. |
| 2026-06-03 | Info cards | **Tooltip explainer on every metric tile.** Sandeep 2026-06-03: *"I need info cards for every tile present in the sales executive, captain and admin portal in every page. info tile has to clearly explain how the calculations is done."* The SSOT registry (`lib/metrics/registry.ts:METRIC_DEFINITIONS`) already carries a plain-English explainer per metric key; this PR threads that text through every tile renderer via the shared `<InfoTooltip iconOnly>` component (radix tooltip, 44dp tap target, body becomes the screen-reader aria-label). **Surfaces wired**: `AdminKpiTiles` (4 tiles: Visits / Orders / Conversion / Productive), `AdminRevenuePanel` (7 rows across Money + Pipeline columns; the 3 non-SSOT rows — Open Quotation Value / Open Requests / Completed Today — get bespoke explainers explaining their snapshot semantic), `app/admin/operations/cities/[cityId]` city drill window-metric tiles (6 tiles), `HeroMetrics` on the exec dashboard (3 hero tiles). Captain `PerformanceCard` already had its own `<InfoTooltip>` per metric (HVA-80) — left untouched, just sanity-confirmed the wording still matches the SSOT semantic. Same text everywhere for the same metric, by construction. Tsc + next build + 819 tests clean. |
| 2026-06-03 | Team targets page | **Move TeamTargetArena off dashboards onto dedicated pages.** Sandeep 2026-06-03: *"team targets should move to a separate page"*. New routes `/admin/targets` and `/captain/targets` host the `<TeamTargetArena>` that previously sat on each dashboard. Admin scope = all execs (global); captain scope = own team only (via `captainUserId` filter on `loadAllExecTargetProgress`). Dashboard pages slimmed accordingly — admin dashboard drops the arena block + the second-round-trip target query; captain dashboard drops the same. Sidebar entries added: `Team Targets` under Operations in `ADMIN_NAV`, between Leaderboard and Reports in `CAPTAIN_NAV_ITEMS` (icon `flag` on both). `<ExecTargetCard>` (single-exec view) stays on the exec dashboard — that's per-exec personal progress, not a team arena. Inline snapshot in `tests/captain-shell/captain-nav.test.ts` regenerated. Tsc + build + 819 tests clean. |
| 2026-06-03 | SSOT FIX | **PR #216 follow-up — extend SSOT to per-city tiles.** Sandeep on prod 2026-06-03 right after PR #216 shipped: *"i can still see the same in admin"*. Root cause: PR #216 routed only the global / captain-team / exec-self loaders through the SSOT — the per-city loaders that power admin's **city card grid** (`loadCityCards`) and the **admin city drill page** (`loadCityMetricsForWindow`) were missed. Those still counted `ORDER_EXECUTED_SUCCESSFULLY` for orders and filtered visits via `tasks.link_request_id` instead of via `sales_executives.city_id`, so admin's KPI strip (correctly SSOT) and admin's city tile (legacy) showed different numbers for the same scope. **Fix**: both functions now call `loadMetrics({ cityId }, range)` with the appropriate keys. `loadCityCards` runs the SSOT loaders per city in parallel via `Promise.all(cityRows.map(...))` — one round-trip set instead of three grouped scans. `loadCityMetricsForWindow` collapses from 5 hand-written sub-queries to a single `loadMetrics(['visits','revenue','orders_count','quotations_count','new_requests','conversion_pct'], { cityId }, range)`. Imports trimmed (no more direct `payments` / `requestStatusHistory` / `statusStages` / `tasks` references; the SSOT loaders carry them). User-visible consequence: admin's per-city orders count now matches what the captain sees on their dashboard for the same window — the last remaining cross-portal drift surface in the admin shell. Tsc + next build clean. |
| 2026-06-03 | Metrics SSOT | **Single source of truth for every dashboard tile.** Sandeep on prod 2026-06-03: *"in admin it is showing some random 3L + in captain it is showing 2L+ these details confuse everyone. one single truth to everyone is required."* Root cause: three different formulas for "orders" across the three portals — admin counted ORDER_EXECUTED_SUCCESSFULLY only, captain + exec counted ORDER_CONFIRMED + ORDER_EXECUTED_SUCCESSFULLY (union), the leaderboard + exec target progress counted ORDER_CONFIRMED only. Same drift on a smaller scale across visits / revenue / conversion (different IST-wrap discipline, different attribution columns, different DISTINCT rules). **The fix is structural** — every numeric tile now reads through one shared registry. **(1) `lib/metrics/*` foundation**: `types.ts` defines `MetricScope = { execUserId? captainUserId? cityId? }` + `DateRange = { fromDate, toDate }` (set exactly one scope key, or none for global); `scope.ts` exposes `visitRequestsScopeFilter` + `tasksScopeFilter` that turn a scope into a Drizzle WHERE clause with deterministic precedence; `constants.ts` collects `STATUS_CODES` + `VISIT_TASK_TYPES` so a typo lives in one place. **(2) Ten loaders** in `lib/metrics/*.ts` — `revenue` (inbound payments by paymentDate), `orders_count` + `orders_value` (ORDER_CONFIRMED transitions, IST-cast on changed_at, DISTINCT request_id / EXISTS-against-quotations so rollback+reconfirm can't double-count), `visits` (completed visit-typed tasks by taskDate), `quotations_count` + `quotations_value` (submitted_at IST-cast), `conversion_pct` (orders ÷ visits as % composing the underlying loaders so the three numbers always agree by construction), `outstanding` (snapshot, Bug 7 semantics — includes executed-but-unpaid), `new_requests` + `cancelled_requests` (IST-cast on created_at / cancelled_at), `productive_minutes` (SQL CASE on the estimated/actual time bucket), `pending_approvals` (snapshot). All loaders share the same attribution rule: `visit_requests.assigned_exec_user_id` for money/order metrics, `tasks.exec_user_id` for visits/productive — never an action-taker column. **(3) Registry + parallel helper**: `lib/metrics/registry.ts` exposes `loadMetrics(keys, scope, range)` that fans every requested loader out in parallel and returns a typed `{ [key]: number | null }`; `METRIC_DEFINITIONS` carries the label / unit / icon / plain-English calculation explainer that the upcoming info card surface will render verbatim. **(4) Shared visual layer**: `<MetricTile>` renders one tile (paise → INR currency, count → en-IN number, percent → "75%", minutes → "2h 15m") with a ⓘ popover for the calculation explainer; `<MetricGrid>` is a responsive grid wrapper that takes a tile spec + the loadMetrics result. Ready for use; not yet swapped into the existing dashboards in this PR. **(5) Cross-portal alignment**: `lib/admin/dashboard-queries.ts:loadAdminGlobalMetrics` / `loadAdminRevenueSnapshot.receivedTodayPaise` + `pendingOutstandingPaise` / `loadAdminCounts.cancelledToday` + `pendingCaptainApprovals` now flow through SSOT. `lib/captain/dashboard-queries.ts:loadWindowAggregates` (revenue/visits/quotations_count/orders_count) routes through SSOT — and because `loadPerformanceForExecIds` is the exec dashboard's PerformanceCard backend too, the exec self-view aligns automatically. **User-visible consequence**: admin will now count slightly MORE orders than before (was filtering to ORDER_EXECUTED_SUCCESSFULLY only — missed every confirmed-but-not-yet-executed order); captain + exec may count slightly FEWER in edge cases where a single order transitioned through both stages inside the window (was double-counting via the union). All three now match the leaderboard + exec-target semantic. **(6) Regression test** at `tests/metrics-ssot.test.ts` seeds a single-team single-city universe and asserts admin global / captain team / exec self all read the same revenue / visits / orders / quotations / pending-approvals / cancelled numbers via different loaders. **Out of scope for this PR (queued as separate ships)**: replace the existing bespoke tile components (AdminKpiTiles / AdminRevenuePanel / PerformanceCard) with the new shared `<MetricGrid>`; reinstate "Open city details" with admin-shell captain data; move `<TeamTargetArena>` to dedicated `/admin/targets` + `/captain/targets` pages; info-tile sweep — every tile gets a ⓘ calculation explainer (the registry already carries the explainer text). Tsc + next build + 814 tests clean. |
| 2026-06-03 | Bug 7 + Bug 8 | **Bug 7: executed-but-unpaid stays in Open Quotation + Outstanding. Bug 8: exec belongs to ONE city (schema change).** Sandeep's 2026-06-02 audit-confirm answers. **Bug 7 (small)**: `loadAdminRevenueSnapshot` was filtering out `ORDER_EXECUTED_SUCCESSFULLY` requests entirely — once an order was marked executed, even with balance owed, its residual disappeared from both "Open Quotation Value" + "Pending Outstanding". Dropped the `!= TERMINAL_POSITIVE_CODE` exclusion; cancelled stays excluded. Fully-paid executed requests self-eliminate from Outstanding via the `if (due > 0)` clamp; their value still counts toward "Open Quotation Value" (interpreted as "total face value of quotations on the books"). **Bug 8 (medium)**: schema migration `0058_exec_city_id.sql` adds `sales_executives.city_id UUID REFERENCES cities(id) ON DELETE SET NULL` + an index. Backfill auto-sets city_id for single-city captains (the SQL ` WHERE COUNT(cities)=1` subquery); multi-city captains' execs stay NULL until admin reassigns. Drizzle schema mirrors the column on `salesExecutives`. **Validator + API**: `executiveCreateSchema` + `executiveEditSchema` now require `cityId`. `POST /api/admin/executives` and `PATCH /api/admin/executives/[id]` validate the chosen `cityId` is owned by the chosen captain — 400 with `fieldErrors.cityId` if not. **Form UI**: Executive create/edit dialog adds a City dropdown filtered to the chosen captain's owned cities; changing the captain resets the city selection. Save button now requires both `captainUserId` AND `cityId`. The list view shows the exec's single city (falls back to all captain's cities for legacy NULL rows). **Per-city queries**: 5 callsites updated to use `sales_executives.city_id` directly instead of the captain→cities hop. `loadCityHeader.execCount` filters by `cityId`. `loadCityExecs` returns execs assigned to this city only. `loadCityMetricsForWindow` (visits sub-query) joins `tasks → salesExecutives.city_id = cityId` instead of the captain hop. `loadCityCards` exec roster aggregates by `salesExecutives.cityId`. `loadAllExecTargetProgress` (exec-target arena) shows the exec's single city in `cityNames` via a `city_id` lookup. All five places eliminate the multi-city captain double-count. **Out of scope** (per Sandeep's 2026-06-03 redirect, queued in a separate plan): single-source-of-truth audit across admin/captain numbers; reinstate "Open city details" with admin-shell captain data; move `<TeamTargetArena>` off dashboards onto dedicated pages; info-tile sweep across every metric in every portal. |
| 2026-06-03 | Admin city drill | **Admin city drill: new-tab, no captain escape, date filter + 6 window metrics.** Sandeep on prod 2026-06-03 reported the city tile was opening in the same tab (he wanted new tab), and the city's "May 5 to today" view was zero because the "Open in captain portal" quick action was routing him into the captain shell (where as super_admin he has no team / no data → zeros everywhere). Three fixes bundled: **(1) AdminCityGrid `<Link>` gains `target="_blank" rel="noopener noreferrer"`** — admin keeps the dashboard open while drilling. **(2) Removed the "Open in captain portal" quick action** entirely from the city drill sidebar — admins aren't captains, jumping them into the captain shell was the wrong default + the root cause of his zero-values complaint. **(3) Added `<DateRangePicker>`** (same component the captain dashboard uses) to the admin city drill — URL state `?date=YYYY-MM-DD` or `?from=YYYY-MM-DD&to=YYYY-MM-DD`, default today single-date. The picker drives a new `loadCityMetricsForWindow(cityId, fromDate, toDate)` in `lib/admin/city-drill-queries.ts` returning six metrics (visits / collections / orders / quotations / new requests / conversion %) — one parallel round-trip with 5 sub-queries (visits joins tasks→sales_executives→captain→cities since tasks don't carry city_id directly; collections via visit_requests.city_id; orders + quotations + new requests via visit_requests.city_id with timestamptz casts wrapped in `AT TIME ZONE 'Asia/Kolkata'`; orders also DISTINCT request_id so rollback+reconfirm doesn't inflate). All same calc-integrity discipline as the captain + admin dashboard audit that shipped 2026-06-02. Sidebar now shows: captain identity card, 6-tile "Window metrics" grid (3×2 layout), Quick actions (Edit city / Manage captains / Manage executives — no captain portal jump). Right column unchanged: open requests (current snapshot) + team roster (current team, "today's task count" per exec). Header gets `<DateRangePicker>` inline. Status line shows "Metrics for May 5 – Jun 3 · 30 days". Tsc + next build clean. |
| 2026-06-02 | Calc audit | **Captain + admin dashboard calculation audit — 6 bugs fixed.** Sandeep on prod 2026-06-02 flagged dashboards showing wrong calcs + team details missing: *"calculations are very important. make sure nothing goes wrong. if this goes wrong the overall efforts we are putting will be on toss."* Spawned a focused audit agent across `lib/captain/dashboard-queries.ts` + `lib/admin/dashboard-queries.ts`, found 6 confirmed bugs in two bug-classes the codebase has been hit with before (leaderboard 2026-06-01, exec-target 2026-06-02 earlier today): **Class A — rollback double-count** (3 bugs): admin `loadAdminGlobalMetrics` (completedOrdersToday lines 142-154), `loadAdminCounts` (completedToday lines 280-303), and `loadCityCards` (ordersToday per city lines 415-422) all used `COUNT(*)` over a `request_status_history` join when the correct shape is `COUNT(DISTINCT request_id)` — a rollback + re-promote within the same IST day inflated the count once per round-trip. All three switched to `COUNT(DISTINCT ${requestStatusHistory.requestId})::int`. **Class C — IST timezone on date casts** (3 bugs): captain `loadWindowAggregates` (quotations lines 206-207, orders lines 225-226) and `loadPendingCollections` (lines 615-620) all cast `quotations.submittedAt::date` / `requestStatusHistory.changedAt::date` in Postgres session timezone (UTC), then compared against IST date strings — after 18:30 UTC each day, the cast lands one IST-day off, dropping today's late-evening quotations + orders out of the bucket. All three wrapped in `(... AT TIME ZONE 'Asia/Kolkata')::date`. **Verified clean** across both files (per the audit's per-function verdict): `loadPendingApprovals`, `loadTeamExecStatuses` (per-exec roster), `loadAdminRevenueSnapshot`, `loadAdminAlerts`, `loadFirstTimeSetupStatus`. Attribution column (Class B) is correct everywhere — every exec-credit aggregate uses `visit_requests.assigned_exec_user_id`, never `payments.recorded_by_user_id` / `quotations.submitted_by_user_id` / status-history actor. **"Team details not showing up" hypothesis**: per-exec list IS rendered on `/captain/dashboard` via `<ExecStatusList>` showing status / visits today / collections today / overdue tasks / red-flag — plus the new `<TeamTargetArena>` from this morning's targets ship. Most likely root cause is the Class C bugs zeroing the per-exec numbers after IST midnight; the fixes here should resolve it. If the list is still empty post-deploy, fall-back hypothesis is the captain has no `sales_executives` rows linked to active `users` (would surface clearer empty-state UX as a follow-up). **SUSPECT items raised for Sandeep** (not bugs, semantic clarifications): (1) visits-completed counts every completed visit-task per request — multiple tasks on the same request count as multiple visits (intended?); (2) open quotation snapshot drops requests at `ORDER_EXECUTED_SUCCESSFULLY` even with balance outstanding (policy intent?); (3) pendingOutstanding = inbound − outbound — refund increases outstanding (correct semantically, double-check); (4) exec roster per city when a captain owns multiple cities — exec appears in each city. All filed for clarification post-walk. tsc + next build clean. |
| 2026-06-02 | Exec targets | **Monthly sales target — admin-set, dashboards everywhere.** Sandeep on prod 2026-06-02: "for each sales executive we have sales target ... admin decides ... ₹7L for each month, sales exec should successfully close ₹7L confirmed orders AND ₹7L revenue collection ... this is to be encouraging to them and it has to be in the dashboard. Same for captain and admins". Picked Design 2 (twin horizontal bars) for exec dashboard, Design 3 (mini-arena ranked rows) for captain + admin dashboards per his selection. **(1) Config + migration 0057** seeds `monthly_exec_target_paise` config key (default ₹7L = 70_000_000 paise), category `targets`, validation min 0 / max ₹1Cr. Mirrors existing config-schema patterns. Admin-editable. **(2) Admin settings page** at `/admin/settings/targets/monthly` — promoted the ADMIN_NAV "Targets" subgroup from `comingSoon` to live, single-key editor mirroring the customer-support-phone page (inline card + Edit Dialog). Form input is rupees; API `PATCH /api/admin/config/monthly-exec-target` validates with new `monthlyExecTargetUpdateSchema` (z.coerce.number int min 0 max 1Cr), multiplies × 100 → paise before `setConfig` with actor metadata for the audit row. **(3) Data layer**: new `lib/exec/target-progress.ts`. `getCurrentMonthWindow()` resolves IST calendar month (monthStart / monthEnd / daysLeft / daysElapsed / monthLabel). `loadAllExecTargetProgress(window, target, { captainUserId? })` runs three parallel queries — execs list, ORDER_CONFIRMED order value by `requestStatusHistory.changedAt AT TIME ZONE 'Asia/Kolkata'::date` joined to quotations, inbound revenue by `payments.payment_date`. **Both attributed to `visit_requests.assigned_exec_user_id`** (not the actor who recorded — saved memory `attribution-vs-action-taker` principle, Sandeep's "Arjun" answer for the reassignment edge case). DISTINCT-handled by GROUP BY exec; sums via SQL bigint cast to int. Returns one row per active exec sorted by combinedRatio desc (avg of orders + revenue ratios). `loadOneExecTargetProgress(execId, ...)` is the narrow variant for the exec dashboard. **(4) `<ExecTargetCard>`** (Design 2) — primary-tinted gradient card with blurred ring accent, two stacked horizontal progress bars (orders + revenue), per-meter pacing pill (Target hit / Ahead of pace / On pace / Push needed — uses `daysElapsed/daysInMonth × 1.1 or × 0.7` thresholds), encouraging status line that adapts to ratio + days remaining (`Push ₹X this week` when behind; `Outstanding month` when both hit; `7 days left, push ₹X first` in the last week). Tones: emerald/sky/amber — never punishing red. **(5) `<TeamTargetArena>`** (Design 3) — for captain + admin dashboards. Header has team-aggregate twin bars (violet for orders, emerald for revenue, gradient fills) + days-left chip. Per-exec ranked rows: rank pill (gold gradient on #1 with subtle glow when combinedRatio > 0), avatar, name + cities, combined% chip (emerald > 100% / sky > 70% / amber > 30% / muted otherwise), and two slim stacked bars (Orders / Revenue with rupee value + label). Borrows the leaderboard arena visual vocabulary but stays in the light theme — it's a dashboard panel, not a full page. **(6) 3-dashboard integration**: exec `/dashboard` shows `<ExecTargetCard>` between header + StatusBanner; captain `/captain/dashboard` shows `<TeamTargetArena>` (captainUserId-filtered) directly under the DashboardHeader; admin `/admin/dashboard` shows `<TeamTargetArena>` (all execs, title "All executives — monthly targets") between AdminKpiTiles and AdminCityGrid. Each call site adds `loadMonthlyTargetPaise()` + a target-progress query to the existing Promise.all fan-out, second roundtrip when needed because target arena depends on the resolved target value. **(7) ADMIN_NAV update**: Targets subgroup promoted from `comingSoon` to live with one item (Monthly target). Tsc + next build clean. |
| 2026-06-02 | Admin polish | **City tiles equal-height + city drill restructured.** Sandeep on prod 2026-06-02: "in admin dashboard all city tiles should be in same height — Other Cities are not in same height" + "when i click on any city ... check how you can make sidebar and overall design". **(1) Tile height fix** — `<AdminCityGrid>` `<li>` gained `h-full`; the inner `<Link>` is now `flex flex-col h-full`; the stats strip got `mt-auto` so every tile's bottom rupee/visits/orders row aligns at the same baseline regardless of differing captain-row content above. CSS-only. **(2) City drill restructure** — `/admin/operations/cities/[cityId]` was a flat hero-then-2-col layout; rewritten as a proper **left sub-sidebar / right main-content split**. Left sticky column (`lg:sticky lg:top-20`, 340px) carries the city context: captain identity card (gradient mini-hero + avatar + email + exec count), today's pulse compact tiles stacked vertically, and a Quick actions divider list (Edit city settings / Manage captains / Manage executives / Open in captain portal — explicit escape hatch, no longer the default city tap). Right main column (`minmax(0,1fr)`) holds the two scrollable lists (open requests on top, team roster below, the team list now a 2-col grid at `sm+` so it doesn't get too tall). Mobile (`<lg`) stacks everything 1-col in the same order, sticky disabled. Three new helper components co-located in the page: `<CompactStat>` for the sidebar tiles, `<ActionLink>` for the quick-action divider rows, `<EmptyState>` consolidated for both empty list states. Out of scope (follow-up): "Today vs yesterday" deltas on the drill page, paginate open requests past 50, exec drill-down from the team roster. |
| 2026-06-02 | Libromi webhooks | **WhatsApp delivery/read telemetry — full webhook receiver shipped.** Sandeep shared Libromi's webhook-management docs; per the discovery plan, registered webhook.site → sent test messages → captured the full payload spectrum (sent / delivered / read / failed with errors). Format is **Meta WhatsApp Cloud API verbatim** (`entry → changes → value → statuses`) plus Libromi's top-level `message_id_map` that links Meta's `wamid.HBg…` to Libromi's send-response `messageId`. **Security finding**: Libromi sends NO signature header — no HMAC, no bearer. Defended with two layers: (1) the URL itself contains a 48-hex-char `[secret]` path segment validated against `LIBROMI_WEBHOOK_SECRET` env var (wrong/missing → 404, no body parse); (2) only events whose `external_id` (Libromi messageId) is present in the new `whatsapp_dispatches` table get a DB write — spoofed messageIds are logged + dropped with 200. **(1) Migration 0056** adds `whatsapp_dispatches`: external_id UNIQUE (Libromi messageId), wamid (Meta id, filled on first webhook), recipient_phone, template_name, event_type, recipient_role, request_id (FK visit_requests SET NULL), recipient_user_id (FK users SET NULL), sent_at (default NOW), provider_sent_at / delivered_at / read_at / failed_at (all nullable, idempotent — set only if currently NULL), failure_code + failure_reason for the failed path. 7 indexes (UNIQUE on external_id + 6 lookup indexes). **(2) WhatsApp channel adapter** (`lib/notifications/channels/whatsapp.ts`) now persists a dispatch row on every successful provider.send: pulls externalId from the result, recipient_role from context (set by the engine), requestId + recipient user id from context (priority order execUserId → captainUserId → assistExecUserId → assistCaptainUserId). Uses `onConflictDoNothing(externalId)` so the stub provider's constant `'stub_whatsapp'` id doesn't violate UNIQUE across repeat dev/test sends. Telemetry insert failures are best-effort (warn-log + continue) — never break the actual send. **(3) Receiver at `/api/webhooks/libromi/[secret]`** — dynamic path with secret validation, force-dynamic Node.js runtime. Parses the Meta payload, walks `entry[].changes[].value.statuses[]`, looks up each event's external_id via `body.message_id_map[ev.id]`, applies a status-specific patch via `applyStatus()`. Per-column idempotency (only set if currently NULL) makes Libromi retries safe. Always 200 — even malformed JSON (so Libromi doesn't loop on garbage). One bad event in a batch doesn't poison the rest (caught + logged + continue). **(4) Tests** — 8 new vitest cases in `tests/api/libromi-webhook.test.ts` covering URL-secret defence, missing-secret 404, allowlist defence (spoof external_id → 200 + no DB write), happy-path 3-event walk (sent → delivered → read sets the 3 columns), failed-event capture (failureCode + dedup-joined failureReason), idempotency (replay doesn't overwrite original timestamp), malformed JSON. All 812 tests green (804 + 8 new). **(5) Secret installed** on VPS: `LIBROMI_WEBHOOK_SECRET=<48 hex chars>` in `/opt/beakn-home-visit-app/.env.local` (chmod 600). **(6) Webhook URL registered** with Libromi management API at `POST https://server2-wc.libromi.cloud/api/v1/webhook` — replaces the webhook.site discovery URL with the prod endpoint. Post-merge operator steps: walk one real send to confirm a dispatch row gets stamped through sent → delivered → read in `whatsapp_dispatches`. Out of scope (follow-up): admin observability surface (delivery rates per template, today's failed sends), inbound message handling (Phase 2 marketing strategy per Sandeep). |
| 2026-06-02 | HVA-79 + payment.received | **Customer WhatsApp opt-in (HVA-79) + payment.received → captain rule.** Two related shipments bundled. **(1) HVA-79 opt-in.** New migration 0054 adds `visit_requests.whatsapp_opt_in BOOLEAN NOT NULL DEFAULT TRUE`. The public `/request` form gains a Switch row ("Send me updates on WhatsApp at this number") with a copy explainer, defaulted on; persists onto the row. The engine's `customer` recipient resolver for the `whatsapp` channel now checks `context.customerWhatsappOptIn === false` and returns `{ reason: "customer opted out of whatsapp" }` BEFORE attempting phone resolution — opted-out customers get an audit row with `status: 'skipped'` rather than a permanent send failure or a silent drop. The check uses explicit `=== false` so a missing context key (legacy callers, tests) defaults to opted-in and preserves prior behaviour. Six dispatch sites updated to SELECT `whatsappOptIn` and pass `customerWhatsappOptIn` into context: `app/api/customer-request/route.ts` (request.created), `lib/visit-schedule/actions.ts` (request.scheduled), `lib/reschedule/actions.ts` (request.rescheduled), `lib/status-transition.ts` (QUOTATION_SUBMITTED / ORDER_CONFIRMED / INSTALLATION_COMPLETE), `app/api/track/[token]/cancel/route.ts` (request.cancelled_by_customer), `app/api/requests/[id]/reject/route.ts` (request.rejected). Validator schema: `whatsappOptIn: z.boolean()` (required — defaults from form's defaultValues + DB column default; not `.default(true)` because that would make Zod input != output and break react-hook-form's Resolver type). **(2) `payment.received` → captain rule.** The event was already emitted from `/api/requests/[id]/payments` POST (HVA-125, 2026-05-30) but had zero notification rules so dispatches were a no-op (rulesMatched=0). Migration 0055 seeds one rule: `captain_owning_city` → WhatsApp → template `captain_payment_received`, `enabled = false` pending Meta approval. New composer `captainPaymentReceived` in the registry plus a small `formatRupeesFromContext()` helper that reads `context.amountPaise`, casts via `Number()`, formats with `en-IN currency INR maximumFractionDigits=0`. Payments route SELECT extended to include `execName` (LEFT JOIN users on assigned_exec_user_id) so the template's {{4}} = exec name parameter resolves correctly per the architectural attribution-vs-action-taker principle (deal-owner credit, not whoever clicked Record Payment). Template draft appended to `/var/www/beakn-docs/beakn-whatsapp-templates-internal.md` as "Tier 3 / Template 22" for Sandeep to submit to Libromi. Activation post-Meta-approval: single SQL flip `UPDATE notification_rules SET enabled = true WHERE channel = 'whatsapp' AND template_key = 'captain_payment_received';`. Tsc + next build + 804 tests clean. |
| 2026-06-02 | HVA-117 dashboard | **Admin dashboard redesign + admin city drill (no more captain-shell escape hatch).** Sandeep on prod 2026-06-02: "one of the worst designs I have ever seen. Tap on any city, it opens the captain portal with captain side navigation." Chose Premium fintech (Stripe/Linear) visual direction over Arena/Map options. **(1) Hero**: new `<AdminDashboardHero>` — primary-tinted gradient card, IST-aware greeting ("Good evening, Sandeep"), date strip, huge `formatRupees` revenue (`text-4xl sm:text-5xl font-bold tabular-nums`), emerald/rose pill with arrow + relative delta vs yesterday, decorative blurred ring in the corner. **(2) KPI strip**: new `<AdminKpiTiles>` — 4-tile grid (Visits / Orders / Conversion% / Productive) with tone-coloured icon chips (sky / emerald / violet / amber), large display value, delta pill per tile. Conversion uses `'pp'` delta kind so deltas read `+2pp` not `+5%`. **(3) Cities**: new `<AdminCityGrid>` — premium card per city with left accent stripe coloured by `classifyCity()` health rule (Healthy / At risk / Critical based on `visitsToday`, `execCount`, `nonSubmitterCount`); large city name, captain avatar + name, "N not started" badge for non-submitters, 3-stat strip (Revenue / Visits / Orders) on a `bg-muted/40` chip with vertical dividers; hover lifts the card. **Tap now targets `/admin/operations/cities/[cityId]`** instead of the captain-layout escape (fixes Sandeep's "worst things" complaint). **(4) Revenue + Pipeline**: consolidated `<AdminRevenuePanel>` — was two cramped left-column cards, now one 2-col card with tone-coloured icons per row. Pending captain approvals row stays a link to `/admin/operations/admin-help` with amber emphasis when non-zero. **(5) Alerts**: refreshed `<AdminAlertsFeed>` — section header with rose count badge when non-empty, emerald check-circle in empty state ("All admin queues are clear"), each alert row now a card with tone-coloured icon tile, kind label, title, time, chevron — much stronger hierarchy than the prior flat list. **(6) New admin city drill at `/admin/operations/cities/[cityId]`**: stays in admin shell. Header card mirrors dashboard hero treatment. Today's pulse tiles reuse the same `loadCityCards` aggregate so numbers don't drift. 2-col bottom: exec roster (with tasks-today count per exec) + open requests (top 50, newest first, `/requests/[id]` deep link, shows outstanding amount if any). New `lib/admin/city-drill-queries.ts` provides `loadCityHeader`, `loadCityExecs`, `loadCityOpenRequests`. **(7) Shared formatters** moved to `app/admin/dashboard/_components/format.ts`: `formatRupees`, `formatRupeesShort` (₹12.4L / ₹1.05Cr for hero numbers), `formatHours`, `computeDelta` (handles count/pct/pp kinds + 0-baseline edge cases + "new"/"—" displays), `greetingFor` (IST hour → Good morning/afternoon/evening). Old `GlobalAggregatesColumn.tsx`, `CityCardGrid.tsx`, `AlertsFeed.tsx` removed (no external imports). Dashboard now fans out 7 parallel queries (today metrics, yesterday metrics, revenue, counts, city cards, alerts, setup status) — TTFB bounded by the slowest. 804 tests green; tsc + next build clean. Out of scope for follow-up: 7-day sparklines on hero, click-through on KPI tiles, swipeable city cards on mobile. |
| 2026-06-02 | HVA-117 admin | **Admin portal: responsive shell + targeted page fixes.** The admin layout was previously laptop-only — at viewports below `lg` the 240px sidebar squeezed the content area to a horizontally-scrolling sliver. Sandeep called the full revamp; first slice is the shell + page-content audit. **(1) `<AdminSidebarSheet>`** — new client component, mirror of `CaptainSidebarSheet`. Mobile drawer (280px wide, opens from left, 44dp tap targets), reuses `ADMIN_NAV` so labels stay in sync with desktop; accordion behaviour for the Settings subgroups (Organization / Audit & Content / Notifications / Workflow & Status / Targets / AI & Report Cards) honouring the HVA-89 6-card spec including the "Coming soon" placeholders; auto-closes on route change via a `usePathname` watcher. Logout button mirrors captain's footer (`logoutAction` via `useTransition`). **(2) `<AdminMobileTopbar>`** — new `lg:hidden` sticky 56dp band: hamburger button (opens the sheet) + page title from `resolveAdminPageTitle()` + `<NotificationBell>`. **(3) `app/admin/layout.tsx` updated** — wraps `<AdminSidebar>` in `<div className="hidden lg:contents">` so the desktop column is `display: none` below `lg` but transparent to flex layout at `lg+` (identical pattern to `app/(captain)/layout.tsx`). `<AdminTopbar>` gains `hidden lg:flex` so it only renders alongside the desktop sidebar. **(4) Existing `<AdminTopbar>` h1 hardened** — `truncate min-w-0` to handle long page titles; bell wrapped in `shrink-0`. **(5) Per-page audit**: most admin pages were already mobile-clean (rows use `flex-wrap` with `min-w-0` parents; padding is `px-4 sm:px-6`; grids collapse to 1-col on mobile via Tailwind responsive prefixes). Three genuine overflow risks fixed: pagination nav in `admin-help-client` + `other-city-client` gained `flex-wrap` (Previous/Page-of/Next buttons now wrap below count text at narrow widths instead of overflowing), and `executives-client` filter `<SelectTrigger>` changed from `min-w-48` to `w-full sm:min-w-48 sm:w-auto` so the captain-filter no longer forces 192px when only 343px of usable width exists. **(6) Dashboard verified responsive** — `grid grid-cols-1 lg:grid-cols-[minmax(260px,1fr)_2fr_minmax(280px,1fr)]` already collapses to 1-col stack on mobile; no changes needed. **(7) Leaderboard verified** — already responsive from the HVA-201 arena redesign + font-fit fix shipped the same day. **Out of scope for this PR (filed for Phase B)**: per-page polish (sticky filter rails, larger touch targets in CRUD action menus, swipe-to-delete card actions, bottom safe-area padding for iOS). Phase A is the structural enabler; Phase B is iterative polish on top. tsc + next build + 804 tests all green. |
| 2026-06-01 | HVA-49 + HVA-155 Part C | **13 internal WhatsApp templates wired + day-close cron.** Sandeep: "finish everything related to WhatsApp, internal and external. I'll get templates approved by tomorrow." Sent 13 internal templates (Tier 1: `exec_request_assigned`, `exec_visit_rescheduled`, `exec_request_approved`, `exec_request_rejected`, `exec_day_close_reminder`, `captain_new_request`, `captain_pending_approval`; Tier 2: `exec_request_reassigned_to_you`, `exec_request_reassigned_off_you`, `exec_customer_cancelled`, `exec_assist_approved`, `exec_assist_rejected`, `captain_assist_request`) to `/var/www/beakn-docs/beakn-whatsapp-templates-internal.md` for Sandeep to submit; full event-to-channel matrix at `/var/www/beakn-docs/beakn-whatsapp-trigger-map.md`. **Code wiring done in this PR; templates pending Meta approval.** **(1) Registry refactor**: `WHATSAPP_COMPOSERS` now keyed by `template_key` instead of `event_type` so the same event can fan out multiple recipient roles with different templates. Channel adapter looks up by `args.templateKey`; missing key fails fast with `no_template_key_for_<event>`. **(2) `targetUserName` plumbed through**: engine's `userTargetForChannel` now returns `{ target, userName }`; AdapterArgs gains optional `targetUserName`. Composers split on whitespace to use the first word as `{{1}}` (so "Veera Kumar" renders as "Hi Veera"). Falls back to "there" when unresolved (customer dispatches, missing fullName). **(3) New `exec` recipient role** in engine — semantically clean for the cron event (self-targeting, not a request assignment); reads `context.execUserId`. **(4) 13 new composers** in `lib/notifications/compose/whatsapp-events.ts` — registry now totals 21 (8 customer + 10 exec + 3 captain). Shared helpers: `firstName(userName)`, `sanitiseFreeText()` (control-char strip + 60-char truncate for rejection/reason fields), `execRequestUrl/captainRequestUrl/captainApprovalsUrl/dayCloseUrl/execAssistUrl/captainAssistUrl`. **(5) Dispatch context extensions**: `lib/status-transition.ts` `request.pending_approval` now LEFT JOINs `users` to pass `execName`; `lib/assist/actions.ts` `assist.created` now passes `execName` for the `captain_assist_request` template. **(6) Day-close cron (HVA-155 Part C)**: new `lib/cron/day-close-reminder.ts` (`fireDayCloseReminders(now?)`) finds execs whose `day_plans` row for IST-today is still open (`closed_at IS NULL`), fires `cron.day_close_reminder` per exec; new bearer-auth route `/api/cron/day-close-reminder` mirrors `roll-over-tasks`; `docs/cron.md` updated with the 21:30 IST line. **(7) Migration 0053**: seeds all 13 rules with `enabled = false` so dispatches don't burn Libromi quota on permanent failures while Meta approves. Idempotent via `ON CONFLICT ... DO UPDATE`. **Activation path post-Meta-approval**: single per-template SQL — `UPDATE notification_rules SET enabled = true WHERE channel = 'whatsapp' AND template_key = '<template_name>';` — no deploy needed. Whole ship: 9 files changed, +780/-90, 797 tests green, tsc + build clean. |
| 2026-06-01 | HVA-201 polish | **Leaderboard font sizes reduced ~25% + IST timezone fix on quotations/orders date filters.** (1) Sandeep on prod 2026-06-01: "the font size is too big, it has to accommodate 8-digit number — reduce by 25% both name and number." Two-step Tailwind reduction (closest to 25% across the rank tiers): top-1 metric value `text-2xl → text-lg` (24px → 18px), top-3 (non-top1) value `text-xl → text-base` (20→16), rank 4+ value `text-base → text-xs` (16→12); top-3 name `text-base → text-xs` (16→12), rank 4+ name `text-sm → text-[11px]` (14→11). 8-digit rupee values (`₹1,23,45,678`) now fit cleanly in the value column at 375px viewport across all rank tiers. (2) **Pre-existing IST timezone bug caught by tests running after 18:30 UTC (= midnight IST roll-over).** `quotations.submitted_at::date` and `request_status_history.changed_at::date` were casting timestamptz to date in Postgres session timezone (UTC), then compared against IST-computed date strings from `getIstDateString()`. After IST midnight, a quotation submitted at 23:30 IST would have UTC date one day prior and miss the same-day filter. Fix: wrap both columns in `(<col> AT TIME ZONE 'Asia/Kolkata')::date` so the date cast respects IST. Two test failures that surfaced this (`quotations metric counts rows by submitter in window` + `REGRESSION (2026-06-01): quotation credited to assigned exec, NOT to the captain who submitted it`) now pass; this also closes a latent prod bug where leaderboard "today" / "this week" totals could drop the last few hours' quotations/orders. 797 tests green. |
| 2026-06-01 | HVA-46 + HVA-47 | **Customer-facing WhatsApp templates wired end-to-end.** Sandeep submitted 8 Meta-approved Utility templates to Libromi on 2026-05-31; live API probe confirmed all 8 APPROVED in `en`. This PR connects them to the notification engine. **New `WHATSAPP_COMPOSERS` registry** in `lib/notifications/compose/whatsapp-events.ts` with one composer per event_type, each producing a Meta-shaped `TemplateMessage` with body params populated. **Tracking URL helper** (`https://visits.beakn.in/track/${trackingToken}`) shared across 7 of the 8 composers. Visit-date params formatted via `toIst()` (e.g. `2 Jun 2026, 4:30 PM`). **Dispatch sites extended** to pass `customerPhone` + `trackingToken` into context (the `customer` recipient role resolves to phone via `directAddress`): (1) `app/api/customer-request/route.ts` for `request.created` → `tracking_link_confirmation`; (2) `lib/visit-schedule/actions.ts` for `request.scheduled` → `visit_scheduled`; (3) `lib/reschedule/actions.ts` for `request.rescheduled` → `visit_rescheduled`; (4) `lib/status-transition.ts` for `request.quotation_submitted` / `request.order_confirmed` / `request.installation_complete` (ORDER_EXECUTED_SUCCESSFULLY also dispatches `installation_complete`); (5) `app/api/track/[token]/cancel/route.ts` for `request.cancelled_by_customer` → `customer_cancellation_received`; (6) `app/api/requests/[id]/reject/route.ts` for `request.rejected` → `we_had_to_cancel` (reads `customer_support_phone` from config for the {{2}} param, fires even when no exec assigned). **Migration `0052_hva46_47_whatsapp_customer_rules.sql`** seeds 8 `notification_rules` rows (channel=whatsapp, recipient_role=customer, template_key=<name>); idempotent via ON CONFLICT on the (event_type, channel, recipient_role) UNIQUE. All 797 tests green; tsc + next build clean. |
| 2026-06-01 | HVA-201 arena | **Leaderboard arena redesign.** Sandeep: "the leaderboard should be more energetic, like a gaming dashboard." (1) Forced dark midnight theme on `/leaderboard` regardless of global theme — wrapping `<div className="dark">` overrides next-themes for this page only. Multi-stop radial gradient background (deep midnight → faint teal arena spotlight → black) + subtle grid overlay. (2) Top 3 elevated: gold/silver/bronze gradient row backgrounds, big colored medal pills with `1ST/2ND/3RD` labels + Material trophy icons, avatar rings in matching metal colors, row height 80px (vs 64px for #4+); `#1` has a 2.4s gentle glow-pulse animation (CSS keyframe in `globals.css`, respects `prefers-reduced-motion`); gradient `LEADERBOARD` title text (amber → rose → cyan). (3) **Rank delta arrows.** `loadLeaderboard` now runs current + prior period (same-length, ending one day before the current window) in parallel and computes `rankDelta` per row. Each row shows `▲ +2` (emerald), `▼ 1` (rose), `— SAME` (slate), or `NEW`. Ranking logic extracted into `buildRankedRows` helper to compose cleanly for both windows. **Out of scope (filed as follow-up)**: streak indicator needs `leaderboard_streaks` table + nightly cron. All 797 tests green. |
| 2026-06-01 | Attribution sweep | **Architectural principle established + full audit.** Sandeep on prod 2026-06-01: "Captain will not do any sales and will not give any quotations. Whatever he gives or whatever he updates is only on behalf of the sales executive, and the portal should be like that." Audited every aggregation query in `lib/`. **Bug found + fixed**: `loadExecBestOfPeriod` "Top Customer" card on /dashboard filtered payments by `recorded_by_user_id` — same bug class as the leaderboard. If a captain recorded a payment on the exec's request, it didn't appear in the exec's top-customer roll-up. Fixed to attribute via `visit_requests.assigned_exec_user_id`. **Verified already correct**: `lib/today/metrics.ts` (revenue/quotations/orders all use assigned_exec since day one), `lib/captain/dashboard-queries.ts` (explicit 2026-05-27 fix), `lib/captain/finance-queries.ts` (filters through visit_requests), `lib/captain/exec-drill-queries.ts` Pending Collections (uses assigned_exec). **Display-only / intentional uses preserved**: audit timeline shows `actor_user_id` (correct — that's the "who did what" feed for accountability); notes show author (audit display, not attribution). **Memory rule `attribution-vs-action-taker` strengthened** from "guideline" to "ARCHITECTURAL PRINCIPLE" with explicit list of banned columns for attribution + intentional display-only carve-outs + testing rule. Two new regression tests in `tests/exec-dashboard/` (captain-records-payment-on-exec-request scenario + reverse scenario where exec records on someone else's request, asserting attribution follows the assigned_exec correctly). 797 tests passing. |
| 2026-06-01 | HVA-201 fix | **Credit attribution bug — revenue + quotations now credit the assigned exec, not the action-taker.** Sandeep on prod 2026-06-01: leaderboard showed Veera ₹0 for 26 May–1 Jun even though her request had a ₹5,000 payment. Production diagnosis: payment was recorded by Arjun (Veera's captain) on Veera's request; old query grouped by `payments.recorded_by_user_id` so Arjun got credited, not Veera. Same pattern for quotations (captain commonly submits on behalf of exec). **Fix**: revenue + quotations queries now JOIN `visit_requests` and `GROUP BY visit_requests.assigned_exec_user_id`. Credit follows the deal-owner, not whoever clicked the button. Visits + Orders + Task% queries audited — already correctly use `tasks.exec_user_id` or join via visit_requests, no changes needed. **Testing rule learned**: aggregation tests must include a cross-role scenario (captain triggers action, assert exec gets credit); happy-path tests where action-taker = assigned exec are insufficient. Two new regression tests added (`REGRESSION (2026-06-01): revenue credited to assigned exec...`, same for quotations). Memory rule `attribution-vs-action-taker` saved so future metric work uses the right grouping by default. |
| 2026-06-01 | HVA-201 follow-up | **Leaderboard UI + calc polish.** Sandeep on prod 2026-06-01: spacing not proper, needed a calendar interface for date-to-date, asked to verify calcs. Three fixes: **(1) Calendar picker.** Dropped the fixed Today/Week/Month time tabs; the page now embeds the existing `<DateRangePicker>` component (same one driving `/dashboard`, `/captain/team/[execId]`) that supports both single-date and from→to range modes. URL state: `?date=YYYY-MM-DD` or `?from=&to=`; `?metric=` preserved across switches. Default when no params: last-7-days range ending today. **(2) Orders calc bug fix.** `loadLeaderboard` was counting `request_status_history` rows for ORDER_CONFIRMED, which double-counts requests that get rolled back and re-advanced (rare in practice but real). Switched to `COUNT(DISTINCT request_id)` so each request is credited once per window. Other 5 metric calcs audited and confirmed correct. **(3) Spacing + heights.** Header row now has the title + date picker on one row (saves vertical space). The two heavy tab-bar strips (border-b + different bg) collapsed into a single rounded-chip strip below the header. List row height locked to `h-16` for consistent rhythm. Medal badge tightened from `w-9 h-9` to `w-8 h-8`. "Your rank" callout has its own bordered card with a primary-tinted outline. Page padding moved from `py-6 space-y-5` to a tighter `pb-12` + section-level spacing for cleaner rhythm. `LeaderboardWindow` type changed from string union (`today | this_week | this_month`) to `{mode:single,date}` or `{mode:range,from,to}` — matches the rest of the codebase's DateFilter shape. |
| 2026-05-31 | HVA-201 | **Global exec leaderboard.** Three thin entry points (`/leaderboard` exec, `/captain/leaderboard`, `/admin/leaderboard`) share one server component. URL-driven tabs: time window (Today / This Week / This Month — default Week, IST midnight boundaries respecting `config.week_start_day` = Tuesday) × metric (Beakn Score composite + Revenue + Visits + Quotations + Orders + Conversion% + Task completion%). **Composite "Beakn Score"** normalises each metric to 0-100 (value÷max-across-execs × 100) then weighted-sums via admin-tunable `config.leaderboard_composite_weights` (defaults 0.35/0.20/0.15/0.10/0.10/0.10); runtime renormalises if partial admin edits make weights not sum to 1.0. **Tie-break chain**: revenue desc → conversion% desc → visits desc → name asc; ties share the same rank. **UI**: top-10 always visible with gold/silver/bronze medal badges on top 3 (M3 tokens, no emojis); rank 4+ get muted circle badges; viewer's "you" row highlighted in primary tint, and if outside top 10 floats at the bottom in its own card with "Your rank" pill. Zero-activity execs (incl. new joiners) still rank at the bottom. Nav entries added to exec drawer (between Finance and Resources), captain sidebar (between Finance and Reports), admin Operations group. New `lib/leaderboard/queries.ts` runs 5 parallel SQL aggregations + composes in JS. New migration 0051 seeds the weights config row. 7 new tests (ranking, ties, composite blend, null-handling, quotations 1:1 visit-request boundary). |
| 2026-05-31 | HVA-45 | **WhatsApp provider abstraction (Libromi Connect).** New `lib/whatsapp/` module with two providers: `libromiWhatsAppProvider` (POST https://wa-api.cloud/api/v1/messages with Bearer auth; exponential-backoff retry on 429 honoring `retry_after_seconds`; classifies UNAUTHENTICATED / VALIDATION_ERROR / INVALID_ARGUMENT as permanent and 5xx + network errors as transient) and `stubWhatsAppProvider` (dev/test fallback, logs + returns delivered). `getWhatsAppProvider()` factory reads `WHATSAPP_PROVIDER` env var (default `stub`). **Channel adapter rewrite**: `lib/notifications/channels/whatsapp.ts` no longer stubs every send — it now requires a registered composer per event type (mirrors the in-app adapter contract). New empty `WHATSAPP_COMPOSERS` registry at `lib/notifications/compose/whatsapp-events.ts`; HVA-46/47 will register the first two (request.submitted, request.status_changed). **Env vars**: WHATSAPP_PROVIDER, LIBROMI_API_TOKEN, LIBROMI_FROM_PHONE, optional LIBROMI_CHANNEL_ID. Documented in .env.example. **HVA-44 Interakt cancelled** in Linear — Libromi replaces it; no multi-provider env switch in scope. Net effect: outbound pipeline ready; flipping `WHATSAPP_PROVIDER=libromi` + adding a token + composer is all that's needed to go live. Tests: 6 new (no-composer fail, composer-throw fail, stub-delivers, libromi-success, libromi-permanent-401, libromi-429-retry-recover). |
| 2026-05-31 | HVA-83 follow-up | **Captain exec drill-down UI polish.** Sandeep on prod 2026-05-30: tiles cluttered, unnecessary tutorial captions, spacing too loose. Three fixes: **(1)** `ExecDrillDownHeader` identity block now lays out as 2 rows instead of 4 — name + red-flag badge on row 1; cities + phone + tap-to-call icon all on row 2 as a single horizontal line that scrolls on overflow. Removed the "Cities reflect the captain's assignments; per-exec city attribution is not modelled today" footer that nobody needed to see. Removed the "phone number duplicated next to a call icon" — now the phone IS the tap-to-call link. **(2)** Stat tiles shortened labels (Visits today → Visits, Collections today → Collected, Active requests → Active) so they fit in one line on mobile; padding trimmed; value uses tabular-nums. **(3)** Page body stripped of tutorial copy ("Read-only view of this exec's day plan…", "Pick a date range to scope…", "Week/Month grid view ships in HVA-83-FOLLOWUP"). Outer page padding tightened `py-6 space-y-5 → py-5 space-y-4`. Identity section padding tightened `pt-4 → pt-4 pb-3`. |
| 2026-05-30 | HVA-150 / 55 | **Locked-decision review.** Sandeep reviewed the two architectural locks blocking these tickets. **HVA-55 SSE — kept locked, cancelled in Linear.** 30s polling (HVA-53) + Web Push (HVA-54) already cover the urgent live-update cases; SSE adds ~6-8 hrs of infra (Postgres LISTEN/NOTIFY pipe, per-user stream, heartbeat, reconnect, leak audit) for marginal UX gain. **HVA-150 Optimistic UI — partial lift.** CLAUDE.md updated: optimistic UI is now opt-in for the 4 high-risk double-submit surfaces (Add Task, Record Payment, Add Lead, Add Note); default pattern remains server-action → revalidate → re-render via `useServerMutation`. Canonical pattern is the local-`useState`+merge-by-id approach `components/notes/NotesSection.tsx` already implements (NOT React's `useOptimistic`). Per-surface implementation filed as a follow-up ticket. |
| 2026-05-30 | HVA-132 | **Notification engine: address-resolution observability + diagnosis.** Diagnostic verdict: the engine already handles missing email correctly. `userTargetForChannel` returns `{ok: false, reason}` when a user has no email/phone; the dispatch loop records `{status: 'skipped', error}` in the deliveries array and aggregates the skip count in the `notification_dispatched` audit row + pino log. **No silent fallback to super_admin.** Sandeep's 2026-05-17 observation (captain Arjun assignment email landing in super_admin's inbox) was likely either Arjun's test email being set to Sandeep's address OR a separate `super_admin` recipient rule firing for the same event. **Small fix shipped**: when email or whatsapp address resolution fails, also emit a `warn`-level pino log line `recipient_address_unresolved` with `{event, channel, recipientRole, userId, reason}`. Now `docker logs beakn-app \| grep recipient_address_unresolved` surfaces all "captain X has no email" cases without a DB query. |
| 2026-05-30 | HVA-56 | **Offline indicator banner.** New `<OfflineBanner>` client component mounted at the root layout (above all routed content). Listens to `online`/`offline` window events; renders a sticky amber banner with `wifi_off` icon + "You're offline. Some actions will sync when you reconnect." when offline. Auto-clears on reconnect AND fires a green sonner toast "Back online. Your next actions will go through normally." so the user knows it's safe to act. Initial state syncs from `navigator.onLine` on mount so loading the page already offline still surfaces the banner. **Out of scope** (Phase 2 follow-up): pending-sync badges on individual task/payment/lead cards + the actual sync queue. Banner alone gives execs the visibility they need on home visits in low-signal areas. |
| 2026-05-30 | HVA-120 / 121 | **Announcements ack rates surfaced to captain.** `/captain/announcements` now calls `loadTeamAnnouncementAckRates` in parallel with the per-user query and merges `{ackCount, ackTotal}` into each announcement before rendering. `AnnouncementsView` gained an explicit `showAckRates` prop (was previously tied to admin-mode overlay) — captain page sets it true so team managers see "12/26 acknowledged (46%)" inline on every announcement card while still being able to acknowledge themselves. **HVA-121 verified already complete**: `loadPublishedResourcesForRole(role)` enforces visibility scoping; `ResourcesView` ships with multi-select tag chips + text search + category filter; both `/resources` (exec) and `/captain/resources` use the role-scoped query. Download tracking deferred (would need a `resource_downloads` audit table — separate ticket if needed). |
| 2026-05-30 | HVA-83 | **Captain exec drill-down — 7 URL-driven tabs.** `/captain/team/[execId]` was a single scrolling page; refactored to 7 tabs per spec: **Today's Plan**, **Calendar**, **Performance**, **Open Requests**, **Pending Collections**, **Red Flags** (Phase 3 placeholder), **Audit Trail**. Tabs are server-rendered, URL-driven (`?tab=...`), bookmarkable; the date filter and audit page are preserved across tab switches via `preservedQuery`. **Sticky header** (exec name + phone + cities + quick stats + Mark Unavailable toggle) renders above all tabs. **New `<ExecDrillTabsNav>`** server component renders the tab strip as Links with M3-style underline-active styling; horizontally scrollable on mobile. **3 new query helpers** in `lib/captain/exec-drill-queries.ts`: `loadExecOpenRequests` (visit_requests assigned to this exec, not cancelled, not terminal-positive), `loadExecPendingCollections` (quoted-minus-paid per request, sorted by outstanding desc), `loadExecAuditTrail` (paginated audit_log filtered by actor_user_id). **5 new tab content components**: OpenRequestsTab, PendingCollectionsTab (with total-outstanding header), RedFlagsTab (Phase 3 placeholder), AuditTrailTab (paginated, 25 rows/page, links to /requests for visit_request entities), plus existing DayPlanSection/DayCloseReportSection/WeeklyReportCard/LeadsEnrolledCard/UnavailabilityScheduleSection/AIDailyReportCard distributed across Today + Calendar + Performance tabs. Calendar tab v1 reuses DayPlanSection in range mode; real Day/Week/Month grid deferred to HVA-83-FOLLOWUP. Auth gate unchanged (`canCaptainViewExec`, 404 on off-team). |
| 2026-05-30 | HVA-135 + 147 + 125 | **Small-fixes bundle.** **HVA-135** (defense-in-depth): the `/api/requests/[id]/status` POST handler now checks per-row ownership before forwarding to `transitionRequestStatus`. Sales-executive callers must own the request (`assignedExecUserId === actorUserId`); captain + super_admin bypass (captain has team-scoped visibility, super_admin is global). proxy.ts could only gate by role; without this guard an exec could in theory POST any request's status route. 403 + "not assigned to you" message on mismatch. **HVA-147**: removed `'/'` from `PRECACHE_URLS` in `public/sw.js`. The root path's HTML changes per build and the proxy redirects it based on session state — caching it produced stale + role-mismatched payloads for some users on install. Bumped cache version to `beakn-v4-hva147` so existing service workers re-register and drop the bad entry. **HVA-125**: emit `payment.received` event from `/api/requests/[id]/payments` POST for inbound payments (refunds intentionally skipped — they'd be a separate event if added). Stub-level — no composer / rule exists yet, but the dispatch site is wired in the fire-and-forget setImmediate pattern matching the rest of the codebase, so when notification rules for this event ship later, no revisit needed. Context payload carries requestId, customerName, cityId, cityName, cityCaptainUserId, execUserId, amountPaise, paymentMode, paymentDate, recordedByUserId. |
| 2026-05-30 | HVA-98 | **Branded 404 + error + global-error pages.** Replaces Next.js's default unstyled pages. **`app/not-found.tsx`** (new) — 404 page with the Beakn logo, "Page not found" headline, and a primary "Go home" button that links to `/`; proxy.ts routes that to ROLE_HOME or `/login` based on session state, so the button works correctly for both authed and unauthed visitors. **`app/error.tsx`** (replaced) — per-segment client-error boundary with the same logo + branded copy; offers both "Try again" (calls Next's `reset()`) and "Go home" buttons. Surfaces the digest as a small monospace ref code when present. **`app/global-error.tsx`** (replaced) — root-level fatal-error boundary; same look but rendered with inline styles since this file must render its own `<html>`/`<body>` and can't depend on the root layout's ThemeProvider or Tailwind. Plain `<img>` instead of next/image for the same reason (the asset pipeline may be what's broken). All three pages still log to `console.error` (or pino server-side for server crashes) so production diagnostics aren't affected. |
| 2026-05-30 | HVA-149 | **Mutation wrapper enforcement promoted to error.** The `useServerMutation` hook (lib/hooks/use-server-mutation.ts) + the `no-restricted-syntax` ESLint rule against raw `useTransition` were shipped earlier at `warn` level; this PR promotes the rule to `error` so CI now blocks any new raw use. To get there I annotated every existing call site (31 files) with `// eslint-disable-next-line no-restricted-syntax -- HVA-149: <reason>` so the legit uses are documented and the deferred-migration cases are tracked. Three reason categories: **(1) "URL push, not a mutation"** for filter/search/pagination/sort/date-range nav transitions (~12 files); **(2) "logoutAction returns Promise<void>, not ActionResult"** for sign-out buttons that genuinely can't use the wrapper (~6 files); **(3) "predates useServerMutation; HVA-149-cleanup TODO"** for older mutation call sites (~13 files: TaskItem, AddTaskSheet, CloseDayView, EditContactSheet, CompletedTasksList, and the admin CRUD clients) that still use the old `useState(submitting) + useTransition` pattern and should migrate to the wrapper. The TODO comments mark the migration debt so a follow-up sweep can clear them one by one. **Net effect**: new code can't bypass the wrapper without an explicit annotation + reason; CI enforces it. |
| 2026-05-30 | HVA-155 follow-up | **Best-of-period cards on exec /dashboard.** Closes the 3 dashboard items deferred from HVA-155: **Best day** (highest task completion %, tie-broken by done count), **Top customer** (highest inbound collection sum, customers deduped by phone), **Best task type** (highest completion % across the 7 task types). New query `loadExecBestOfPeriod({ execUserId, from, to })` in `lib/exec/dashboard-queries.ts` does all 3 aggregations in 3 parallel-able queries (group-by task_date / payment customer / task_type). New `<BestOfPeriodCards>` component renders a 1-col-on-mobile / 3-col-on-sm grid below PerformanceCard. **Single-date dashboard mode silently maps to "last 7 days ending on the picked date"** so the cards always have a useful window's worth of data; range mode honors the picked range verbatim. Helper `resolveBestOfWindow(filter)` returns `{from, to, label}` so the card section can show the window in its header ("Last 7 days ending 30 May" / "1 May – 14 May"). Voided payments + outbound payments filtered out of Top customer. Empty-state copy when the window has no data. |
| 2026-05-30 | Responsive sweep | **Mobile overflow fixes — Approach B sweep.** Sandeep on prod 2026-05-30: "Conversion tile's 'No target' badge is spilling out of the box, and I'm seeing similar things in many places." Confirmed root cause: classic flex-overflow — text children in `flex justify-between gap-*` rows refuse to shrink below their content min-width unless `min-w-0` is set, so badges/dots get pushed past the container's right edge at 375px. Hardened **10 components**: `DayCloseMetricTiles` (the original Conversion bug), captain `PerformanceCard` tiles + header, `PendingApprovalsCard` header, `PendingCollectionsCard` header + big rupees `text-3xl` line, `FinanceOrderList` customer-name row + 3-col rupees grid (replaced `flex-wrap` with `min-w-0 truncate`), `RequestCardMobile` customer-name header + captain meta row, `AssistRow` footer row, `AssistDetailView` details grid + message body, `NotesSection` author row + note body (added `break-words` for long URLs), exec `CloseDayView` 6-tile grid, admin `CityCardGrid` 3-col stats. Pattern applied consistently: text-bearing flex children get `min-w-0 truncate`; their siblings (badges, dots, action buttons) get `shrink-0`; large numeric values get `tabular-nums truncate`; long user text gets `break-words`. Pure CSS, zero behavior change, all 780 tests still green. |
| 2026-05-30 | HVA-76 | **Profile screen — exec + captain.** Pulled forward from phase-2-deferred per Sandeep. Shared `<ProfileView>` server component renders 6 cards: **(1) Account** (name, role, phone, email + role-scope footer); **(2) Theme** — System / Light / Dark 3-chip control via existing next-themes wiring (no new infra); **(3) Notifications** card links to the already-shipped `/profile/notifications`; **(4) Change password** card links to a new `/profile/change-password` page (HVA-29's form lifted from `/dev/change-password-test`, which now 308-redirects); **(5) App version** showing commit SHA + build date — `scripts/deploy.sh` now computes `git rev-parse --short HEAD` + ISO timestamp at every ship and bakes them as `NEXT_PUBLIC_COMMIT_SHA` / `NEXT_PUBLIC_BUILD_DATE` build-args via the same Dockerfile-ARG + bundle-grep verification pattern Turnstile/VAPID use; **(6) Logout** — destructive button + confirmation modal reusing the existing `logoutAction`. **Role-scope footer:** exec shows their captain's name + that captain's owned cities (joined `sales_executives → users + cities WHERE captain_user_id = ...`); captain shows their own owned cities; admin shows "Global" (admin profile route itself is deferred — wasn't in scope). New data loader `lib/profile/queries.ts:loadProfileForUser` returns one shape covering all three role variants. 5 new integration tests across roles + empty-state cases (`tests/lib/profile-queries.test.ts`). |
| 2026-05-30 | HVA-199 follow-up | **Assist "Link to customer" is now a search-as-you-type combobox.** Sandeep on prod 2026-05-30: "Our team will have a lot many customers. It's very difficult to have a drop down and select." Replaced the `<Select>` (capped at 30 most-recent linkable visit_requests) with a new `CustomerLinkSearch` client component backed by a new `GET /api/assist/linkable-customers?q=…` endpoint. **Endpoint** wraps the existing `loadLinkableVisitRequestsForExec` with a 20-row cap, auth-gated to the caller's own assigned visit_requests (sales_executive or super_admin). **Combobox** debounces 300ms, AbortController cancels in-flight requests on the next keystroke, popover renders results with customer name + city + stage; selected state collapses to a row with a Clear button. **Bootstrap suggestions** (10 most-recent) still preloaded server-side so the popover is non-empty on first focus. **Form contract change** — `AssistForm` initial now takes `linkedVisitRequest: LinkableVisitRequestOption \| null` (full row) instead of `linkedVisitRequestId: string \| null`; `loadAssistDetail` + `loadAssistList` projections widened with `stageName` so the edit page can pass the pre-selected value through without re-fetching. |
| 2026-05-30 | HVA-199 | **Assist section** — exec submits material-requests; captain/admin processes through a 4-stage workflow. New domain end-to-end. **Schema** (migration 0049): `assist_requests` + `assist_request_items` + `assist_request_status_history`; 3 new enums (`assist_type` single-valued today for future-proofing, `assist_status`, `assist_priority`); cascade deletes on parent; `linked_visit_request_id` ON DELETE SET NULL. **State machine**: `submitted → approved → processing → dispatched`; `rejected` terminal-reachable from any pre-terminal stage. **8 new routes** — exec `/assist` (list), `/assist/new` (accordion-shaped create form), `/assist/[id]` (read-only detail + history timeline), `/assist/[id]/edit` (locked once captain acts); captain `/captain/assist` + `/captain/assist/[id]`; admin `/admin/operations/assist` + `/admin/operations/assist/[id]`. **3 server actions** — create + update (status='submitted' only) + transition (captain team-scoped + admin global; concurrency-safe via WHERE status=expected; idempotent). **Notifications**: 5 new event types (created, approved, processing, dispatched, rejected); 2 new engine recipient roles (`assist_team_captain` resolves the exec's captain via sales_executives, `assist_submitter` resolves the original submitter); composers with captain/exec/admin variants; rules seeded via migration 0050 across in_app + push. **Audit**: 5 new event types added to both `lib/config-schema.ts` defaults + the live config row. **Captain sidebar badge**: extended `loadCaptainSidebarCounts` with `openAssistCount`. **Accordion UI** on list pages and the new-form (per user request for future assist types). **Nav wiring**: exec drawer + captain sidebar + admin operations group. proxy.ts gates `/assist/*` to sales_executive. |
| 2026-05-30 | Notif settings back button | Sandeep: "/profile/notifications doesn't have a close button." It's a full page (not a drawer), so the right pattern is back navigation — reused `BackButton` from HVA-191 (router.back() with `fallback` for deep-link case). Fallback resolves to the role's home via `ROLE_HOME[role]` so a fresh-tab visit doesn't dump the user back to /login. |
| 2026-05-30 | Notif drawer close button | Sandeep: "I could not find close button in notification window, in all three roles." Default shadcn Sheet ships a 16-px X at `top-4 right-4` opacity-70 — below the 44-px mobile tap target + visually subtle. **Fix**: pass `showCloseButton={false}` to suppress the default + add an explicit 40-px Button inside the SheetHeader's title row aligned next to "Notifications", `aria-label="Close notifications"`, `name="close"` Material Symbol. Same `NotificationBell` component drives all three portals so this lands everywhere. |
| 2026-05-30 | Notif comprehensive + prefs | Sandeep on prod: "Push notification wherever required, and settings to enable/disable kinds users want, plus first-time prompt — no popup appeared, had to find bell manually." Multi-layer build: **(A) 2 new dispatch events.** `request.created` fires from `/api/customer-request` (captain + admin pinged when a customer raises a new request — closes the biggest captain-side gap); `request.pending_approval` fires from `lib/status-transition.ts` when status enters PENDING_CAPTAIN_APPROVAL (captain + admin pinged). 4 new composers (created + pending_approval × captain + admin variants). **(B) Exec also gets pinged** on customer cancel + reschedule — added exec composer variants + rules; dispatch sites pass `execUserId` so the `exec_assigned` resolver finds the right user. **(C) Per-user opt-out infrastructure.** New `notification_preferences` table (migration 0047) with unique `(user_id, event_type, channel)`. Engine reads it after recipient resolution: row with `enabled=false` → skip; absence → use rule's default. **(D) /profile/notifications settings page** — single shared route accessible from all three portals. Lists every (event_type, channel) the user is eligible for, grouped by event; each row is a toggle backed by `setNotificationPreferenceAction` (optimistic local update + server settle). Drawer header in NotificationBell now has a "Settings" link → /profile/notifications. **(E) First-time prompt banner**: new `PushPromptBanner` Client Component mounted in all three layouts. Slim band ABOVE main content when (PushManager supported + permission=='default' + no prior dismissal). Two buttons: "Enable push" (full subscribe flow, identical to bell drawer toggle) + "Not now" (localStorage dismissal). Auto-hides once granted or denied or dismissed. **(F) Migration 0048** seeds 4 captain/admin rules for the 2 new events + 4 exec rules for cancelled/rescheduled — all mirrored across `in_app` and `push`. After this PR: captain pings on 6 events (was 3), admin pings on 5 events (was 3), exec pings on 7 events (was 5). |
| 2026-05-29 | Notif rule fill-in | Sandeep on prod: "Why notifications in captain portal and Admin portal not working?" Diagnosis (with DB evidence): the bell UI + engine + Web Push pipeline all worked correctly; the `notification_rules` table was just sparse. Only one captain rule existed (`request.rolled_back` from HVA-141) and ZERO admin rules. **Plus** two of the events that should ping captains (`request.cancelled_by_customer`, `request.rescheduled`) had no composer registered — even if rules existed they'd fail at delivery with `no_composer_for_X`. **Plus** the dispatch sites for those events didn't pass `cityCaptainUserId` in context, so the `captain_owning_city` resolver couldn't find the captain even if a rule existed. **Fix** (3 layers): (1) two new composer files `request-cancelled-by-customer.ts` + `request-rescheduled.ts` with separate captain + admin variants resolved by `ctx.recipientRole`; `compose/index.ts` branches the dispatch. (2) dispatch sites (`/api/track/[token]/cancel/route.ts` + `lib/reschedule/actions.ts`) now join `cities` and pass `cityCaptainUserId` + `cityName` in context. (3) migration `0046` seeds 5 new in_app rules + 5 mirrored push rules (captain: cancelled, rescheduled; admin: rolled_back, cancelled, rescheduled). Captain bell now pings on 3 events instead of 1; admin bell pings on 3 events instead of 0. |
| 2026-05-29 | HVA-54 | Browser-level push notifications. **Crypto setup**: `web-push` npm package + VAPID keypair generated; public key in `.env.local` + `NEXT_PUBLIC_VAPID_PUBLIC_KEY` baked into client bundle (Dockerfile ARG + deploy.sh REQUIRED_BUILD_ARGS validation extended); private key + `VAPID_SUBJECT` stay server-side. **Schema** (migration `0045`): `push_subscriptions` table with `(endpoint UNIQUE, p256dh, auth, user_agent, last_used_at)` columns + cascade delete on user. Same migration seeds `web_push` rules in `notification_rules` for every event_type that already has an `in_app` rule (idempotent ON CONFLICT). **API**: new `/api/push/subscribe` route with POST (upsert subscription, caller-scoped) + DELETE (remove by endpoint, caller-scoped). **Channel adapter** `lib/notifications/channels/web-push.ts` reuses the same `IN_APP_COMPOSERS` registry so the OS notification's title/body matches what the bell drawer shows; on 404/410 from the push service it auto-deletes the dead subscription. Wired into `engine.ts:invokeChannel` + `userTargetForChannel`. **Service worker** (`public/sw.js` cache bumped to `beakn-v3-hva54`): `push` event renders OS notification with icon/badge/tag/renotify + stashes linkUrl on `data`; `notificationclick` focuses an existing tab matching the linkUrl pathname (with `client.navigate` to refresh content) or opens a new window. **UI**: new `PushSubscribeToggle` component in the NotificationBell drawer header — 4-state UX (unsupported→render nothing / default→Enable / denied→explanatory line / granted→Disable); base64url decoder for VAPID public key + rollback path that calls `subscription.unsubscribe()` if the server POST fails (no orphan device-side registrations). Reused across all three portals since the bell is role-agnostic. |
| 2026-05-29 | HVA-53 | Notification bell now "feels alive" without manual refresh. **New endpoint** `GET /api/notifications/poll?since=<ISO>` returns `{ unreadCount, newItems[], cursor }` — caller-scoped via `getServerSession()`; cursor advances by 1ms past the newest row each tick so `gt(createdAt, cursor)` never re-yields the same row. **New hook** `useNotificationPoll` ticks every 30s, gated by document.visibilityState (background tab → paused; refocus → immediate catch-up tick + resume timer) and by drawer-open state (no point polling while the user is reading the drawer). **NotificationBell** consumes the hook: reconciles unread count every tick (so cross-tab mark-as-read flows in), prepends new items into the local list (capped at 50 to bound drawer growth), fires up to 3 individual sonner toasts per tick + a single summary toast if more arrived (avoids spam on noisy days). Toast carries an "Open" action button when the notification has a linkUrl; tap → `router.push`. Mark-as-read actions now decrement the local count optimistically since we no longer depend on layout revalidation for badge freshness. **Cost**: 100 active users × 30s tick ≈ 3 polls/sec total on the DB; query is composite-index covered. **Deferred**: HVA-55 SSE swaps the polling for a stream with the same `{ unreadCount, newItems }` payload shape (Phase 2 per CLAUDE.md). |
| 2026-05-29 | HVA-79 + HVA-87 | Sandeep: "Why notifications in captain portal and Admin portal not working?" Answer: HVA-52 was exec-only by design (Wave 3); captain (HVA-79) + admin (HVA-87) were Wave 4 backlog. Engine was already writing in_app rows for captain (HVA-141 seeds `request.rolled_back` → `captain_owning_city`) — only the UI was missing. **Fix**: plugged the role-agnostic `NotificationBell` component from HVA-52 into three new surfaces with zero new component code — desktop captain sidebar (replaces the HVA-79 stub button, anchored ml-auto in the logo row, h-8 w-8 trigger), captain mobile topbar (replaces the disabled stub Button), admin topbar (replaces the placeholder bell). Each layout (`(captain)/layout.tsx` + `admin/layout.tsx`) now fans out `loadUnreadInAppCount` + `loadRecentInAppNotifications` in parallel with its existing per-role queries. Admin layout also reads `session.user.id` (added to its narrow cast). |
| 2026-05-29 | HVA-52 | Replaced the HVA-48 placeholder bell on the exec topbar (desktop + mobile) with a real `NotificationBell` Client Component. Reads from the existing `in_app_notifications` table that `lib/notifications/engine.ts:dispatchEvent` already writes to via the `in_app` channel adapter (HVA-48). **Bell**: ghost icon button + tiny red badge with unread count (capped at "99+"). **Drawer**: Sheet from the right (full-width on mobile, max-w-md on desktop) with header "Notifications" + Mark-all-as-read button; scrollable list of the 20 most recent items showing event-type-derived icon, title, body preview, relative time, and a primary-tinted dot when unread; tap → optimistic local-list update + `markNotificationReadAction` server action + router.push to `linkUrl`. **Layout**: 3 layout-level fetches now fan out in parallel (`unreadAnnouncementsCount` + `loadUnreadInAppCount` + `loadRecentInAppNotifications`). New `lib/notifications/in-app-queries.ts` (`loadUnreadInAppCount`, `loadRecentInAppNotifications`) + `lib/notifications/in-app-actions.ts` (`markNotificationReadAction` mark-by-id + caller-scoped, `markAllNotificationsReadAction`). New `components/notifications/{NotificationBell.tsx,event-type-icon.ts}` — reused by HVA-79 (captain) without modification. 5 new integration tests in `tests/lib/notifications-in-app-queries.test.ts`. Real-time updates wait on HVA-53 (polling) / HVA-55 (SSE, Phase 2). |
| 2026-05-28 | HVA-151 (foundation) | Framework for browser-level regression catching. Added `@playwright/test` as a devDep + downloaded Chromium binaries (~113MB to `~/.cache/ms-playwright`). New `playwright.config.ts` boots a `pnpm next start -p 3100` webServer (production build mode for determinism — no HMR / React DevTools overlay noise) and runs Chromium at 1280×800 desktop. Three read-only smoke specs in `tests/e2e/smoke.spec.ts`: `/login` form fields visible, `/request` public customer form renders, unknown route 404s without crashing. New scripts `pnpm test:e2e` and `pnpm test:e2e:update` (the latter for baseline refreshes). `tests/e2e/README.md` documents usage + the DB-safety rule (read-only only until isolated test DB lands). Bonus: surfaced a latent type error in `tests/lib/verdict.test.ts` from PR #170 that vitest tolerated but strict tsc rejects — fixed via `Omit<Partial<...>, 'targets'>` to break the intersection collapse. Also fixed the broken `pnpm tsc \| tail` pre-PR-gate check in this session (tail's exit code was masking tsc failures). **Phase-1 deferrals** (followup tickets needed): mobile + tablet viewports, 3 authenticated critical flows (exec daily / captain / exec requests), visual baselines committed to git, GitHub Actions CI integration, isolated test DB for mutating flows. |
| 2026-05-28 | HVA-64 + HVA-63 | Completed the half-built Close the Day flow. **HVA-64**: replaced the static flag-icon header on `/today/close` with a sticky verdict header carrying a 🟢/🟡/🔴 ring (`bg-emerald/amber/rose` + sentiment icon), a big primary headline (revenue → orders → visits → tasks-done fallback chain), and a deterministic one-liner. Header stays pinned via `sticky top-0 z-20 backdrop-blur` while the user scrolls the breakdown below. **HVA-63**: surfaced plan-vs-actual variance % (done/totalAtSubmission) in the Plan vs Actual card header + a new pair of cells for Estimated time / Actual time (bucket-parsed minutes summed across all tasks for the day). `DayCloseMetrics` interface extended with `variancePct`, `estimatedTotalMinutes`, `actualTotalMinutes`; `loadDayCloseMetrics` + `loadFinancialMetricsForDate` + `aggregateMetrics` all updated. New `lib/today/verdict.ts:computeDayVerdict` pure helper covered by 7 unit tests. Same code path drives both the live close-out and the captain drill-down range view (variance recomputed at the aggregate level). |
| 2026-05-28 | HVA-88 | Replaced the placeholder `/admin/dashboard` with a real three-column super_admin dashboard. **Left** (~25%): today's performance tiles (visits / collections / orders / new requests / conversion% / productive hours), revenue snapshot (received today / outstanding / open quotation value), counts (open requests / completed today / cancelled today / pending captain approvals). **Middle** (~50%): per-city cards in a 3-col grid — captain avatar + name, today's visits/collections/orders, exec count, red "didn't submit plan" flag. Card click drills into `/captain/requests?city=<id>` (super_admin has escape-hatch access to the captain shell). **Right** (~25%): time-sorted alerts feed — other-city queue + unreplied admin help + approvals aging >24h. Pinned **first-time setup** banner at top until cities + captains + execs all exist. SSE deferred to Phase 2 per CLAUDE.md (page is `force-dynamic`; navigation re-fetches). New `lib/admin/dashboard-queries.ts` with 6 fan-out loaders called in parallel from the page. |
| 2026-05-28 | HVA-194 | Moved the production logout server action from `app/dev/logout-test/actions.ts` → `lib/auth/logout-action.ts`. The action was load-bearing in prod (every sidebar + drawer + admin-footer + exec-avatar-menu imports it) but lived under a path that looked dev-only — misleading. All 7 import sites updated (5 production sidebars/menus + 1 dev page trigger + the file itself moved). Old file deleted. No behaviour change — same function, same callers, just an import-path rename. `proxy.ts` still blocks `/dev/*` HTTP routes in prod; the server-action mechanism doesn't depend on route accessibility. |
| 2026-05-28 | HVA-192/193/195/196 | Cleanup bundle. (192) Deleted untracked `.env.local.bak` from prod working dir — never committed, `.gitignore` already excludes `.env*`. (193) Deleted `scripts/align-migrations-table-hva111.sql` after verifying no runtime references; `docs/migrations.md` historical note updated. (195) Clarified the byte-identical-snapshot comment in `app/(captain)/layout.tsx` — the actual regression guard is `tests/captain-shell/captain-nav.test.ts` (nav-config shape, not DOM-level snapshot). (196) Diagnostic: confirmed Caddyfile IS bind-mounted from `/opt/beakn-home-visit-app/infra/caddy/Caddyfile:/etc/caddy/Caddyfile:ro` — old "no bind-mount" memory was wrong. Updated MEMORY → caddy-infra with verified state + new edit protocol (just edit the host file, run `caddy reload`; no docker cp needed). Drift between repo + live file noted (live has Hermes Agent block not in committed snapshot) — separate decision before syncing. |
| 2026-05-28 | HVA-128 | Drop misleading "(or the assigned captain is inactive)" clause from HVA-42 escalation email — the routing code only checks `cities.captain_routing_email` non-empty; it never reads `users.is_active` on the linked captain (intentional decoupling per HVA-42 design). Email copy also clarified: "Please assign a captain to this city" → "Please set a routing email for this city in admin settings" (the missing thing is the routing column, not the captain assignment). Pure copy fix in `lib/email-templates/captain-new-request.ts`. |
| 2026-05-28 | HVA-129 | HVA-127 polish — two changes: (1) `/captain/requests` "Open" pill renamed to "New" per Sandeep's original spec; URL key stays as `?bucket=open` for backward compatibility so deep links don't break. (2) Captain sidebar (both desktop + mobile drawer) gains badge counts next to Requests / Pending Approvals / Finance. Counts come from new `lib/captain/sidebar-counts.ts` using the same team-scope visibility as the underlying list pages, so the badge "6" matches what the captain sees on click-through. Layout fans out the count query in parallel with the unread-announcements count via Promise.all so the sidebar doesn't serialize them. Cancelled requests excluded everywhere. Super_admin (no city assignments) sees no badges. |
| 2026-05-28 | HVA-197 | Sandeep on prod: "in the request page, Buttom Four buttons are not properly aligned. They are not looking good." Root cause: three different heights in play on the action-button row (h-9 for Advance default, h-11 for Rollback/Reassign, h-12 for everything else) plus inconsistent text-sm/text-base and px-4/px-5 padding. When 3-4 buttons rendered together (e.g. captain on PENDING_CAPTAIN_APPROVAL sees Rollback + Reassign + Reject + Approve), the row looked ragged. **Fix**: standardised all action buttons to `h-12 px-5 text-base font-medium`. Variant + color differences kept for hierarchy. 3 files touched: advance-status-button.tsx, rollback-status-button.tsx, reassign-request-button.tsx. |
| 2026-05-27 | HVA-145-FIX | Sandeep: "also enable share sheet beside copy". **Fix**: `CopyTrackingLink` (the shared component on `/requests/[id]` + `/submitted/[token]`) gains a primary Share button next to Copy. Tries `navigator.share()` first (opens OS native share sheet on mobile — WhatsApp / SMS / email / etc.); falls back to clipboard with a "Link copied — paste into WhatsApp / SMS / email" toast when the API isn't available (most desktop browsers). AbortError swallowed (user dismissed sheet, not an error). Customer's post-submit page also benefits — same component. New `shareTitle` / `shareText` props let callers supply context for the share sheet; request-detail prefills with the customer name. |
| 2026-05-27 | HVA-145 | Sandeep on prod: "If the customer loses the URL, there is no chance we can get the URL back. It's better if we have a URL copy in the request section." **Fix**: new "Customer tracking link" section on `/requests/[id]` (between Customer details and Notes), visible to captain + exec + admin. Renders the full URL with `select-all` styling + a Copy button (sonner toast + 2s "Copied" confirmation), reusing the existing `CopyTrackingLink` Client Component from `/submitted/[token]`. URL constructed from `visit_requests.tracking_token` + `BETTER_AUTH_URL` env (defaults to https://visits.beakn.in). No schema change — `tracking_token` already exists; only the SELECT widened to include it. |
| 2026-05-27 | HVA-191 | Sandeep walked prod: "Whenever I click on the back button, it is directly navigating me to the home page. Both captains and sales executive portal." Root cause: `app/requests/[id]/page.tsx` (the most-used detail page across both portals) hardcoded the back arrow to `ROLE_HOME[role]` — captain hit `/captain/dashboard`, exec hit `/today`, regardless of where the user came from. Other detail pages had a related (smaller) issue with hardcoded list paths. **Fix**: new `components/ui/back-button.tsx` Client Component using `router.back()` with a `fallback` prop for the no-history case (deep links, fresh tabs). All 6 detail-page back arrows swapped: request detail (captain + exec), captain contacts/[contactId], captain team/[execId], exec leads/[leadId], captain collections/calendar, exec finance/calendar. Browser back now honors actual history. |
| 2026-05-27 | HVA-179 (PR14) | Sandeep: "weekly report information is not in sync." DB probe: Veera has no day_plan for today (2026-05-27); Singham's ₹5,000 payment today was correctly counted on the Finance dashboard but silently dropped from the Weekly Report. Root cause: `loadExecDayClose` range-mode iterated only over dates that had a day_plan, so payments / quotations / orders on no-plan days never made it into the 7-day aggregate. **Fix**: range mode now iterates EVERY date in [from, to]; for plan-less days, a new `loadFinancialMetricsForDate` helper returns revenue / quotations / orders / visits (zero task counts since tasks live under day_plans). Aggregation merges seamlessly. Same code path drives the exec dashboard's Weekly Report AND the captain drill-down's Weekly Report, so both portals now show the same numbers AND those numbers match the Finance dashboard. |
| 2026-05-27 | HVA-178 (PR13) | New `/finance` route on the sales-exec portal: same FinanceSnapshot + FinanceAgingBuckets + FinanceMethodologyNote + FinanceFiltersBar (no exec/city dropdowns since the exec IS the subject) + FinanceOrderList + FinanceListSortToggle + FinanceListPaginationNav components from the captain finance page, all parameterised with `basePath`. Queries gain a `forceExecScope` option that pins visibility to `visit_requests.assigned_exec_user_id = me` and bypasses the captain team-scope helper. New `/finance/calendar` payment calendar mirrors `/captain/collections/calendar`. EXEC_DRAWER_NAV gains "Finance" between Requests and Resources; inline-snapshot test updated. DB probe confirms Veera's self-view: 6 quoted requests (incl. Singham at VISIT_SCHEDULED), ₹2,55,000 quoted, ₹92,000 received. |
| 2026-05-27 | HVA-177 (PR12-FIX5) | Sandeep: "If Captain enrolls anything on behalf of an exec, attribute it to the exec." PR12-FIX4 covered payments only — quotation count + per-exec collections breakdown still ran on actor-based attribution. **Fix**: three more sites switched from `submitted_by_user_id` / `recorded_by_user_id` to `visit_request.assigned_exec_user_id`: (1) exec dashboard quotation count (lib/today/metrics.ts), (2) captain dashboard team quotation count (lib/captain/dashboard-queries.ts:loadTeamPerformance), (3) per-exec collections rollup in loadTeamExecStatuses. DB probe confirms: Singham's quotation submitted by Arjun (captain) now counts toward Veera's team (was 0, now 1). |
| 2026-05-27 | HVA-176 (PR12-FIX4) | Sandeep on prod: "The amount that we received hasn't been reflected in the Captain's dashboard." DB probe: Arjun (captain) recorded Singham's ₹5,000 payment himself. Captain dashboard `loadTeamPerformance` and exec dashboard `loadDayCloseMetrics` were filtering payments by `payment.recorded_by_user_id IN team_execs` — but Arjun isn't on his own team, so the payment dropped from both surfaces. **Fix**: attribute payments by `visit_request.assigned_exec_user_id` instead of `payment.recorded_by_user_id`. Now captain or admin recording on behalf of an exec lands correctly in the exec's hero + the team aggregate. Plus: sidebar "Pending Collections" → "Finance" (matching the page title). Plus: new `FinanceListSortToggle` on the orders + quotations list with 4 options (Outstanding desc default, Newest first, Oldest first, Order value desc); URL state via `?sort=`. DB probe confirms: with the fix, Arjun's team Revenue today = ₹5,000 (was ₹0). |
| 2026-05-27 | HVA-175 (PR12-FIX3) | Sandeep: "added a quotation of ₹25,000 + payment ₹5,000 for Singham today, dashboard shows ₹0." Root cause: the Pipeline predicate was `sequence_number = 5` (exact QUOTATION_GIVEN) but execs save the quotation at the customer's home BEFORE the stage advances. Singham at seq=3 VISIT_SCHEDULED silently dropped from every tile. Same bug hid ₹84,000 of payments at ASSIGNED stage. **Fix**: Pipeline = `sequence_number < 6` (any pre-confirmation quote with a quotation row). Received counts payments on EVERY quoted request (not just Order Book). Outstanding = (Order Book + Pipeline) − Received. Aging buckets span both axes. New `FinanceMethodologyNote` collapsible accordion explains the math in plain language per Sandeep's request. DB probe confirms: Order Book ₹2,60,000 / 5 orders, Pipeline ₹1,05,000 / 3 quotes (includes Singham), Received ₹2,27,801 (was ₹1,38,801), Outstanding ₹1,37,199. |
| 2026-05-26 | HVA-174 (PR12-FIX2) | parseView on `/captain/collections/calendar` only matched `'week'\|'month'` and fell through to the `'month'` default — clicking a day put `?view=day` in the URL but the server parsed it back to `'month'`. Result: clicking any date kept the page in month view + tiles never updated. Fix: parseView accepts `'day'` explicitly. Same hardening applied to exec + team calendar parseView. |
| 2026-05-26 | HVA-173 (PR12-FIX) | Sandeep walked /captain/collections/calendar on prod 2026-05-26 — felt "completely broken" because the default month view (anchored at today) showed dots on May 17-19 but clicking any other day landed on an empty day-view. Root cause: sparse payments + a month view with no inline list = invisible data. Fix: `CalendarClient` now renders a `PeriodEventList` below the week and month grids, grouped by day with date headers, so every event in the visible window is readable without drilling in. Day view + per-event chips unchanged. Same component is used by exec calendar, team calendar, and payment calendar — all three benefit. |
| 2026-05-26 | HVA-172 (PR12) | Replaced the /captain/collections "Coming soon" stub with a full finance dashboard. **Money snapshot tiles** (4 across, 2x2 mobile, 1x4 desktop): Order Book (status ≥ ORDER_CONFIRMED), Quotation Pipeline (status = QUOTATION_GIVEN, exclude cancelled), Received (inbound − outbound on Order Book, voided excluded), Outstanding (can go negative → amber "credit owed"). **Aging buckets** (0-7 / 8-30 / 30+ days) with horizontal progress bars showing share. **Filter bar**: debounced search + exec dropdown + city dropdown + section pills (All / Order Book / Pipeline). **Order list**: paginated 10/page, mobile cards + desktop table, sorts outstanding DESC, links to /requests/[id]. New `/captain/collections/calendar` payment-calendar route — every inbound/refund payment as a calendar event (₹amount — customer), reuses CalendarClient with new 'payment' kind tag, same filters. Window-total tiles atop the calendar. All data lives in `lib/captain/finance-queries.ts` (no migration needed — built on existing quotations + payments tables). Team-scope visibility via the existing buildCaptainRequestVisibilityWhere helper. |
| 2026-05-26 | HVA-190 (PR11) | Applied the universal filter+search+pagination rule to three captain surfaces. **/captain/calendar** gains `CalendarFiltersBar` — debounced text search (matches customer name + exec name post-query) + exec-dropdown filter (`?exec=`); `loadTeamCalendarEvents` accepts the new options and returns the team roster for the dropdown. **/captain/approvals** gains `ApprovalsFiltersBar` — debounced search (ILIKE on customer name) + exec dropdown; pagination at 10/page via `computePageRange` (in-memory slice after the per-row history sort, acceptable for the bounded pending-approval set). **/captain/requests/unassigned** gains `UnassignedSearchInput` — debounced search (name + digit phone) + server-side LIMIT/OFFSET at 10/page using `buildListUrl` for the prev/next links. All three use the established useTransition-for-URL-push pattern (not a mutation). |
| 2026-05-26 | HVA-189 (PR10) | New `exec_unavailability_schedules` table (date range + reason + actor) via migration `0044`. `lib/captain/availability.ts` resolves "unavailable today" from BOTH the existing boolean flag AND the schedule. New `addExecUnavailabilityScheduleAction` + `removeExecUnavailabilityScheduleAction` server actions (captain or super_admin, with team-ownership check). New `UnavailabilityScheduleSection` UI on /captain/team/[execId] lists upcoming windows with add/remove. `loadTeammatesForRebalance` + `bulkReassignAffectedVisitsAction` now filter out scheduled-unavailable execs alongside the boolean flag. Two new audit events seeded via the dual-write pattern. |
| 2026-05-26 | HVA-188 (PR9) | Extracted the per-request approve flow from `/api/requests/[id]/approve/route.ts` into `lib/captain/approve-request.ts`. New `bulkApproveRequestsAction` (50-row cap) iterates the helper and returns `{ approved[], failures[] }` so the UI can render partial results. `/captain/approvals` page becomes a server-render wrapper around a new `ApprovalsListClient` island with row checkboxes + a "Approve N" sticky header that opens a dialog with an optional shared note. Per-row Approve/Reject buttons stay (Reject requires a unique 50-500 char reason per row — not bulk-suitable). Inline minimal Checkbox component (no new shadcn dep). |
| 2026-05-26 | HVA-187 (PR8) | Three captain-portal improvements. **Stale alerts**: `loadPendingApprovals` + `loadPendingCollections` now return `staleCount` (>24h for approvals, >48h for collections); both dashboard cards render an amber banner linking to the queue when staleCount > 0. **Team search**: `/captain/team` accepts `?q=` and ILIKEs on full_name + phone; new `TeamSearchInput` client component pushes debounced URL. **Team Calendar**: new `lib/captain/calendar-queries.ts:loadTeamCalendarEvents` returns every team exec's visits + tasks (same dedupe rule as exec calendar) with exec-name attached; new `/captain/calendar` route reuses `CalendarClient` via a `basePath` prop addition; exec-name chips render on every event. Nav gets a "Team Calendar" entry between "My Team" and "Contacts". |
| 2026-05-26 | HVA-186 (PR7) | New `lib/api/fetch-action.ts:createFetchAction` adapts a `fetch()` call into the `ActionResult<T>` shape `useServerMutation` expects. Captain's four fetch-based request modals (`assign-request-modal`, `approve-request-modal`, `reject-request-modal`, `reassign-request-modal`) all migrate to the centralised hook through the wrapper — same API routes, same UX, but the busy/refresh/toast quadruplet is gone. fieldErrors + message pass-through preserved. ~10 remaining fetch sites (exec request-detail buttons: payment-record / refund-record / payment-void / quotation-edit / mark-installation-complete / mark-customer-rejected / advance-status / rollback-status) can adopt the same pattern when next touched. |
| 2026-05-26 | HVA-185 (PR6) | Five more mutation sites move to the centralised hook: PostponeSheet, AddLeadSheet, MoveTaskSheet, ConvertLeadSheet, EditRequestSheet. Each loses its bespoke `useState + useTransition + router.refresh + toast` quadruplet. The hook gains a second `onError` arg carrying `fieldErrors`, so forms with inline per-field error UI (AddLead, ConvertLead, EditRequest) keep that behaviour. `EditRequestResult` narrowed from `ok: boolean` to a discriminated union — strict refinement; existing callers already branched on `result.ok`. ~20 sites remain — fetch-based ones (payment/refund/void/quotation/mark-installation/reject/reassign/assign modals + advance-status-button) need a fetch wrapper; AddTaskSheet has dual actions; EditContactSheet has a collision-id side channel. Those migrate in follow-ups. |
| 2026-05-26 | HVA-184 (PR5) | Sandeep's "completed count again showing error" — Dashboard `loadExecCompletedTasksToday` no longer filters by `task_date = today`; Option B now applies to completed too (matches Pending behavior). `/tasks` gains a search input that filters Pending + Postponed rows client-side by description, type, or linked customer name (rows are already loaded — no server round-trip). Public `/request` form demotes `email`, `state`, `bhk`, `interest` to optional by widening to `z.union([z.literal(''), ...])` (kept the FieldValues type shape stable so the react-hook-form resolver didn't break this time); API route coerces empty values to `null` / `Others` / `[]` on insert. |
| 2026-05-26 | HVA-183 (PR4) | Migrated 4 hand-rolled mutation sites to the centralised `useServerMutation` hook so the HVA-149 refresh-required pattern lives in one place: `components/reschedule/RescheduleButton`, `RebalanceDialog`, `StartMyDayButton`, `MarkUnavailableToggle`. Each loses its bespoke `useState + useTransition + router.refresh + toast` quadruplet. ~25 more callsites remain — exec today actions, lead/contact sheets, payment/refund/quotation/void buttons, captain assign/approve/reject modals — and migrate incrementally as those surfaces are touched. |
| 2026-05-26 | HVA-182 (PR3) | Captain visibility on `/captain/approvals` and `/captain/requests` switches from `cities.captain_user_id` (city-scope) to `assigned_captain_user_id = me` plus an unaccepted-but-pending-in-my-cities fallback. New `lib/captain/team-scope.ts` centralises the rule. Closes the cross-captain leak where captain B could approve or see captain A's request when both owned overlapping cities. `setExecUnavailableAction` revalidates `/` layout instead of three narrow paths — every surface reading `is_unavailable` now refreshes. |
| 2026-05-26 | HVA-181 (PR2) | Dashboard pending count drops the `taskDate = today OR rolledOverAt IS NOT NULL` predicate — now matches `/tasks` page 1:1 (Option B; future-scheduled tasks like the auto-created Schedule-Visit row surface on both surfaces). Exec `/requests` moves from client-side filter to server-side: bucket → status_code IN(...) WHERE, search → LOWER LIKE on customer_name/customer_phone, pagination 10/page, debounced search updates `?q=`, bucket tabs update `?bucket=`. Bucket counts derived from a single GROUP-BY pass so pills reflect the search-filtered total. |
| 2026-05-26 | HVA-180 (PR1) | Calendar dedupes visit + auto-task by linkRequestId (was rendering both, looked like duplicate). Schedule-Visit reason + Reschedule input + EditRequestSheet datetime + RebalanceDialog all force `Asia/Kolkata` timezone (Docker server is UTC; was showing 06:30 instead of 12:00 IST and 04:30 in the timeline reason). Universal accordion-closed rule applied to admin-help inbox/thread + exec Dashboard TasksAccordion + exec /tasks TasksPageView. `DEFAULT_PAGE_SIZE` lifted from 20 → 10 with admin-help + other-city imported from the shared constant. Mandatory-field removal on exec-facing forms: AddTask (task type, date, description, estimated time), AddLead (type), ConvertLead (address, bhk), EditRequest (all `required` props dropped from FormRow). Lead.interest demoted to optional. |
| 2026-05-25 | docs sync | docs/CONTEXT.md Resources + Announcements paragraphs rewritten to reflect FIX2 (announcement_acknowledgments rename, audience/publish_date/importance, visibility/tags, pure helpers) |
| 2026-05-25 | HVA-149 (10C-partial) | lib/hooks/use-server-mutation hook bundles useTransition + router.refresh() + toast — prevents the refresh-required bug class. ESLint rule warns on raw useTransition in (exec)/(captain)/admin. One representative call site (AdminHelpSection) migrated as a template; ~60 remaining sites migrate opportunistically (HVA-149-FOLLOWUP). |
| 2026-05-25 | HVA-71 (1C) | Exec Calendar tab — Day / Week / Month views at /calendar. Day = vertical event list; Week = 7-col grid with per-day count; Month = 5-6 week density grid with dot counts. Reads visit_requests + tasks for the exec; navigation persists view/date via query string. Drawer gains a Calendar entry. |
| 2026-05-25 | HVA-72 (2B) | Reschedule flow: exec RescheduleButton on /requests/[id] (required reason, 10-500 chars); customer reschedule on /track/[token] mirroring the cancel pattern (POST /api/track/[token]/reschedule, free-text optional reason). Writes request_reschedule_history + increments visit_requests.reschedule_count + audits + dispatches request.rescheduled (silent until HVA-50). Captain-approval gate deferred. |
| 2026-05-25 | HVA-77 + HVA-94 (3C-lite) | Admin Help round-trip: AdminHelpSection on /requests/[id] for exec to send (10-500 chars); admin inbox at /admin/operations/admin-help with reply-once UI; email notifications on both legs (sendEmail composers inline); admin sidebar shows unread count badge derived directly from admin_help_messages (no engine dep) |
| 2026-05-25 | HVA-85 (6B) | Mark Exec Unavailable + auto-rebalance prompt: toggle flips sales_executives.is_unavailable; when set unavailable AND exec has future-scheduled visits, RebalanceDialog opens listing each visit with per-row reassign-to dropdown (active teammates only). Bulk action transactionally reassigns + writes history rows + audit events + dispatches notifications. |
| 2026-05-25 | HVA-95 (5B) | Other-city Queue at /admin/operations/other-city. Lists out-of-area Submitted requests. Manually Route modal picks a captain + records reason; SUBMITTED → ASSIGNED + history + audit. Sidebar Other-city Queue link updated; All Requests marked placeholder until built. |
| 2026-05-25 | HVA-93 (7A) | Holidays config — admin CRUD at /admin/settings/workflow/holidays (single-date, all-cities). Flipped Workflow & Status subgroup from "Coming soon" to active. Audit events holiday_created / holiday_updated wired (migration 0038) |
| 2026-05-25 | Settings accordion | Admin Settings sidebar group restructured into 6 collapsible accordions per HVA-89 (Organization / Audit & Content / Notifications shipped; Workflow & Status / Targets / AI & Report Cards rendered as "Coming soon" placeholders). Active subgroup auto-expands based on URL. |
| 2026-05-25 | UI unification | Admin Resources/Announcements lists now render the same ResourcesView/AnnouncementsView the team sees, with an overlay ✏️ icon-button per card. Append-only semantics preserved (announcements Manage dialog only toggles published) |
| 2026-05-25 | HVA-61 (10B) | Mark-Done sonner toast gains an Undo action button (5s duration) on top of the existing persistent inline Undo on completed task cards |
| 2026-05-25 | HVA-113 (9A) | Captain deactivation blocked when active sales execs still report; action-layer guard returns 409 with exec count + names; closes HVA-91 spec divergence |
| 2026-05-25 | HVA-39 (7B) | Customer-initiated cancellation: Cancel button + reason picker on /track/[token]; POST /api/track/[token]/cancel; audit + notification fan-out (silent until HVA-50 seeds rules); migration 0037 appends audit event |
| 2026-05-25 | HVA-37 (8B) | BHK-matched proposal + standard catalogues download on customer tracking page, resolved via tagged Resources (1bhk/2bhk/3bhk/4bhk + catalogue tags); strict visibility='all' filter on the public read query |
| 2026-05-25 | HVA-156-FIX2 (2C) | Full HVA-120 + HVA-121 spec closure: Resources gain visibility + tags; Announcements gain admin-managed categories + audience + importance rename + scheduled publish_date + explicit ack tracking with admin ack-rate display; captain ack-drilldown query helper added |
| 2026-05-25 | HVA-89 (1B) | Admin Settings consolidation: all config-y admin surfaces move under /admin/settings/{group}/{page}; sidebar collapses People + Content groups into one Settings group; old URLs 308-redirect (next.config.ts) |
| 2026-05-25 | HVA-156-FIX1 | Resources rework: URL bookmarks + admin-managed categories CRUD + dropdown/search filter + Open/Share buttons (Web Share API + copy-link fallback) |
| 2026-05-23 | HVA-156 | Resources + Announcements: schema (3 tables, 2 enums), super_admin CRUD UI, exec + captain read surfaces, per-user read-tracking, drawer unread badge |
| 2026-05-23 | docs | Claude Code owns full ship pipeline (auto-merge + auto-deploy for low-risk PRs); remove broken SSH/su steps from ship process |
| 2026-05-23 | docs | Add STATE.md maintenance rule to CLAUDE.md — every feature PR must update STATE.md in the same PR |
| 2026-05-23 | HVA-170-FIX3 | UI polish: customer name on task rows + EXEC_DRAWER_NAV in desktop sidebar + LinkedCustomerChip (PR #109) |
| 2026-05-22 | HVA-170-FIX2 | Customer link display fix on edit/clone (linkSearch state init was empty) + SQL same-day filter fix |
| 2026-05-22 | HVA-170-FIX1 | Move semantics for Pending/Postponed, drop link auto-copy on Clone, double-submit guard |
| 2026-05-22 | HVA-51 + HVA-170 | Sales exec hamburger drawer + Tasks page + Start-My-Day enhancement + clone flow |
| 2026-05-22 | HVA-171 | Fix HVA-169 dashboard postponed predicate + calendar wiring |
| 2026-05-21 | HVA-169 (HVA-155 Parts A+B) | Sales exec analytical dashboard + nightly task roll-over cron |
| 2026-05-21 | HVA-168 | Fix Pending Approvals stale predicate + Orders click-credit attribution |
| 2026-05-21 | HVA-167 | Captain exec drill-down page `/captain/team/[execId]` |
| 2026-05-21 | HVA-154 | Captain My Team list + exec drill-down (calendar, day plan, weekly view) |
| 2026-05-20 | HVA-166 | Unify Contacts card UI (captain + exec) |
| 2026-05-19 | HVA-163 | Captain portal edit access (no delete) |
| 2026-05-19 | HVA-73 PR1 + HVA-157 + HVA-158 + HVA-159 | Contacts CRM shift + calendar picker + auto-contact + edit access |
| 2026-05-18 | HVA-153 | Captain Requests tab search + pagination + filter |
| 2026-05-18 | HVA-80 | Captain Dashboard two-column layout |
| 2026-05-17 | HVA-137 | Captain approval gate (closes exec bypass to Order Executed) |
| 2026-05-16 | HVA-66 | Request detail screen with customer info + status timeline |
| 2026-05-18 | HVA-65 | Exec Requests tab (5 buckets) |
| 2026-05-23 | docs | Defer HVA-165 (contact merge) — moved to Frozen pending real-world duplicate patterns from team usage |
| 2026-05-19 | HVA-73 PR2 + PR3 | Notes UI on request detail + both contact detail surfaces (append-only timeline + optimistic write area, commit 6d37f12) |
| 2026-05-19 | HVA-161 | Broaden exec contact visibility to assignment trail (commit 73a7b18 — landed under the HVA-73 PR3 commit message) |
| 2026-05-19 | HVA-73 | Leads section (unified form) |

For tickets older than 2026-05-16, see Linear archive (search project: Phase 1 — MVP Launch).

---

## Queued (not yet started)

- **HVA-151** — Playwright visual regression (Phase 2 candidate)

---

## Cleanup tickets to file (from recon)

(All STATE.md-tracked cleanup tickets shipped in HVA-192/193/194/195/196.)

---

## Blocked

WhatsApp provider activation (Libromi) shipped HVA-45 (2026-05-31). 8 customer-facing Utility templates Meta-approved on 2026-05-31 and wired through the engine in HVA-46 + HVA-47 (2026-06-01) — see Recently shipped. Remaining WhatsApp-dependent items below still need work:

- **HVA-49** — internal exec/captain WhatsApp notifications (not yet drafted as Meta templates)
- **HVA-50** — admin-editable rule UI for notification_rules
- **HVA-79** — opt-in checkbox on customer /request form
- **HVA-155 Part C** — 9:30 PM WhatsApp day-close reminder

---

## Frozen / pending real-world data

- **HVA-170 extensions** — Additional task management ideas (Move/Duplicate buttons beyond what shipped, dedicated /tasks-vs-dashboard architecture) frozen pending 2–3 days of real-world usage data on /tasks page (shipped 2026-05-22). Most ideas in the original placeholder may turn out to be unnecessary or contradict auto-roll-over behavior.
- **HVA-165** — Contact merge flow. Deferred pending real-world team usage of the contacts system. Sandeep's call 2026-05-23: ship nothing speculative; if the team hits duplicate-contact pain (mistaken double-entries, phone-format drift, business-vs-customer split for the same human) the schema + UI design should be informed by the actual duplicate patterns, not invented up-front. Revisit when concrete duplicate cases surface.

---

## Phase 2 (deferred)

- SSE / real-time updates (HVA-55)
- Optimistic UI (HVA-150)
- Playwright visual regression (HVA-151)
- Push notifications via VAPID (env vars exist in `.env.example`, not wired)
- WhatsApp via Interakt (env vars exist, not wired)
- Multi-language
- AI report cards (Phase 1 spec §11 — no ticket yet)
- Advanced analytics (forecasting, predictive)
- Mark Exec Unavailable toggle (HVA-85) — may lift if operationally needed
- Rolling-deploy / zero-downtime cutover (current rebuild has a few-second gap)
- Containerised drizzle-kit migrator (eliminates host-side override)
