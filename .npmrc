# pnpm 11 runs a deps-status-check before every script, which fails hard on
# the "ignored build scripts" warning even though it's non-blocking. Disable
# the pre-flight; we manage builds via the `pnpm.onlyBuiltDependencies` field
# in package.json.
verify-deps-before-run=false
