# Beakn HVA — Current State

**Last updated:** 2026-06-14 (HVA-293 — day plans auto-close at 23:55 IST; + HVA-292 walk-session fixes: calendar postpone/IST, customer reschedule, start-my-day recent tasks, admin Revenue & KPI drill-downs)

This file captures what's live, what's next, what's blocked, what's deferred. Update after every PR merge.

---

## Phase 1 — Live on prod

### Customer-facing
None yet. Customers raise requests via beakn.in main site, not via HVA. HVA is internal-only.

### Captain portal (13 pages)
- Dashboard with team performance metrics, pending approvals, pending collections (HVA-80, HVA-168)
- Requests tab with search + pagination + filter-by-exec (HVA-127, HVA-153)
- My Team list with exec drill-down — calendar, day plan, report cards, weekly view (HVA-154, HVA-167)
- Contacts page with unified card UI (HVA-73)
- Mobile hamburger drawer (HVA-152)
- Captain approval gate — exec can't bypass to Order Executed (HVA-137)
- Edit access on contacts, requests, team (HVA-163; no delete anywhere)
- Approvals queue, Collections queue, Unassigned requests bucket

### Sales exec portal (11 pages)
- Day plan: Start My Day → visit tasks → close day workflow (HVA-60, HVA-64)
- Dashboard with pending tasks (incl. rolled-over), postponed, completed today (HVA-169)
- Tasks page with Pending / Postponed / Completed accordion (HVA-170)
- Tasks: move semantics for Pending+Postponed, clone for Completed (HVA-170-FIX1)
- Requests tab with 5 buckets, mirrors captain pattern (HVA-65)
- Request detail with customer info + status timeline (HVA-66)
- Contacts/Leads with unified form (Customer/Business toggle) (HVA-73)
- Mobile hamburger drawer mirrors captain pattern; 8 items: Dashboard / Today / Tasks / Contacts / Requests / Resources / Announcements / Profile (HVA-51, HVA-156)
- Resources surface (admin-published sales scripts / pricing / brand assets / training, grouped by category) (HVA-156)
- Announcements surface (admin broadcasts with severity badges + per-user unread badge on drawer) (HVA-156)
- Edit access on contacts, requests, tasks (HVA-159; no delete anywhere)
- Calendar picker for future-dated tasks (HVA-157)
- Auto-task creation on captain assignment (HVA-158)

### Admin portal (14 pages — fully responsive as of HVA-117)
- Dashboard (3-column layout, collapses to 1-col on mobile)
- Operations: Admin Help Inbox / Other-city Queue / Assist Requests (list + detail) / Leaderboard
- Settings → Organization: Cities CRUD / Captains CRUD with reset-password / Executives CRUD
- Settings → Audit & Content: Resources / Resource Categories / Announcements / Announcement Categories
- Settings → Notifications: Customer Support Phone
- Settings → Workflow: Holidays
- Shell: persistent left sidebar at lg+, mobile drawer + hamburger topbar below lg (HVA-117)

### Public-facing pages
- Customer visit-request form at /request (HVA-34, HVA-35)
- Customer tracking at /track/[token]
- Login
- Set-password (first-login pin, HVA-26)

### Cross-cutting
- Nightly task roll-over cron at 21:31 IST (HVA-169)
- Audit log on status changes (per `config.audit_enabled_events`)
- Status timeline visible on request detail
- Three-layer auth defence (proxy.ts + layout gates + per-action authorize)
- Better-Auth phone+password sessions with 5-attempt rate limit
- Email via SMTP (nodemailer); WhatsApp channel adapter exists but stub until provider activates

---

## Recently shipped (most recent first)

| Date | Ticket | Summary |
|---|---|---|
| 2026-06-14 | HVA-293: day plans auto-close at 23:55 IST | Execs are often too busy to close their day. New cron `GET /api/cron/auto-close-day` (CRON_SECRET) seals any open day plan (`plan_date <= today IST`) at 23:55 IST, setting `closed_at` + a new `auto_closed` flag (migration 0078). Tasks are NOT touched — unupdated tasks stay `pending` and the next-day 21:31 roll-over picks them up. Host crontab line installed (18:25 UTC). Safety chain: 21:30 reminder → 21:31 roll-over → 23:55 hard close. (PR #316) |
| 2026-06-14 | HVA-292: walk-session fixes (calendar, reschedule, today, admin dashboard) | Batch from Sandeep's walk. **Calendar**: postponed tasks now resolve to their postponed-to date (was stuck on the original day); visit-schedule files the auto-task on the IST date (was UTC, off for 00:00–05:30 IST visits); calendar visit branch filters by IST date. **Customer reschedule**: was erroring — `proxy.ts` didn't allowlist `/api/track/`, so the token-authed reschedule/cancel POSTs were 307'd to /login (added the prefix); reschedule now also moves the linked visit task to the new date so the calendar follows. **Start my day**: recent open tasks (last 7d, not in today's plan) now show post-start too via the extracted `RecentOpenTasksAccordion`, so carried-over work can still be pulled into today. **Admin dashboard**: Revenue & pipeline promoted to a full-width grouped stat-tile grid (was cramped/truncating); the 5 top KPI cards are now clickable → `/admin/dashboard/[metric]` drill-downs (search + 20/page) mirroring each SSOT metric; Productive tile replaced with New requests (intake). (PR #316) |
| 2026-06-14 | HVA-290: dashboard redesign across all six surfaces | Built on the HVA-289 foundation, in one pass per Sandeep. **Exec**: Today tab unchanged + new **Overall** tab (FY-to-date default + calendar; warnings + monthly target pinned top; collapsible groups Revenue & finance / Orders & conversion / Visits & activity; ⓘ on every tile; Outstanding pinned "as of today"). **Captain**: Today unchanged + **Overall** tab (FY team metrics + per-exec **target finish line** — revenue vs monthly target, ranked progress bars). **Admin**: free calendar replaced with **three fixed tabs** Today / This month / Overall (the tab is the range). **City → full captain replica**: `/admin/portal/[captainId]/dashboard` rewritten to render the exact captain dashboard (Today \| Overall, scoped to captainId, reusing captain components + shared `CAPTAIN_OVERALL_METRIC_KEYS`) instead of the old shrunk view. **Finance** (exec /finance + captain /collections): shared `FinanceSnapshot` tiles gained ⓘ explainers + denser fonts (no tabs — those surfaces are as-of-now). New shared primitives: `DashboardTabNav`, `DateRangePicker` (param-preserving), `MetricTileGrid`. No metric formula changed — SSOT (`lib/metrics`) untouched. (PR #314) | PR1 of the agreed dashboard redesign (foundation → exec → captain → admin). Purely additive shared primitives — no existing page touched. `lib/date.ts` gains financial-year helpers (FY = Apr 1 → Mar 31): `financialYearStartYear` / `financialYearBounds` / `financialYearToDate` / `financialYearLabel`. The metrics registry now tags every metric `temporality: 'range' | 'snapshot'` (+ `pinWhenSnapshot` on Outstanding) — presentation metadata only, no loader/formula change; `outstanding` + `pending_approvals` are the snapshots. New `lib/dashboard/metric-display.ts` carries `formatMetricValue` (paise/percent/minutes/count) + `isMetricTileVisible` / `showsAsOfTodayBadge` — the single rule behind "pin Outstanding, hide other snapshots when range ≠ today". New `components/dashboard/MetricTile.tsx` — one compact tile (denser fonts, no oversized heroes) with the ⓘ info button rendering the registry explainer + an "as of today" badge. 19 unit tests (FY helper + display rule + registry-tagging guard). The exec PR assembles these into the Today/Overall tabs. (PR #313) |
| 2026-06-14 | HVA-288: status timeline shows bypassed stages as "skipped" | Follow-up to HVA-287. Sandeep asked why the complete track (Assigned → Visit Scheduled → Visit Completed) wasn't shown for a CartPlus order. Clarified the two cases — a request that went through a real home visit before being quoted in CartPlus **merges** into the existing request and already shows those stages; only a *direct* online order genuinely never had them. Decision: show the full 10-stage ladder always, rendering the bypassed stages greyed and struck-through as **"Skipped · online order"** (symmetry with the forward "Pending" placeholders already shown). Both timelines updated: staff `TimelineRow` and customer `TimelineDot` gained a `skipped` variant; each page computes the bypassed set as active stages between Submitted and the current stage whose seq is absent from the recorded history (rollback-safe; empty for normal/merged requests so they're unchanged). Online vs not is detected per surface (staff: quotation source = portal; track: a portal quotation exists). UI only — no schema/migration/server action. (PR #312) |
| 2026-06-14 | HVA-287: fix CartPlus order status timeline (missing history row) | Sandeep flagged that a CartPlus order's status timeline looked emptied out. Root cause: the create-new branch of `handler-order-created.ts` set the request directly to QUOTATION_GIVEN but wrote **no** `request_status_history` row, so the timeline (built from history rows + future stages) showed only a synthetic "Submitted" — wrongly marked current — then jumped to the future stages, with the middle of the ladder missing. HVA-285's status mapping didn't cover it (a `pending` order maps to QUOTATION_GIVEN, the stage it was already created at → no advance). Fix: the create-new branch now writes an initial history row anchoring the request at Quotation Given (from = null, since a CartPlus order originates there with no home-visit stages — per Sandeep, skipped stages stay hidden), and `apply-status` still composes on top (a `confirmed` order adds a second row → Order Confirmed). Migration **0077** idempotently backfills the missing anchor row for all 5 existing `source='portal'` requests (at their current stage, attributed to the assigned exec, timestamped to creation). Tests: brand-new order → one QUOTATION_GIVEN row; confirmed-on-create → two ordered rows. (PR #311) |
| 2026-06-14 | HVA-286: customer /track page → Status · Order · Payments tabs | Sandeep: the public tracking link should show order details + payment details/history, not just status — tabbed for easy scanning, with room for a future Razorpay "Pay online". New `TrackTabs` client wrapper (Radix Tabs, mirrors the request-page shell) with 3 tabs, **defaulting to Status**; the existing status centrepiece + timeline + customer actions + support tickets now live under **Status**. New **Order** tab (`OrderTab`) — items (name · qty × unit · line total) from the CartPlus quotation + Order total, with a graceful empty state until a quotation exists. New **Payments** tab (`PaymentsTab`) — Order total / Paid / Balance due (or Overpaid), customer-safe payment history (date · amount · mode; refunds shown as 'Refund'; **internal fields hidden** — no staff name, reference numbers, or notes, since the link is public/token-only), voided payments excluded, and a disabled **'Pay online — coming soon'** placeholder when a balance is due (reserved for Razorpay). Page loads the portal quotation + line items (removed-filtered) + payments and computes the summary via the shared `computeCollectionSummary`. Walk-verified at 390px across all three tabs. (PR #310) |
| 2026-06-14 | HVA-285: CartPlus order status → Beakn stage | Sandeep: CartPlus will emit three order statuses that drive Beakn; everything after Order Confirmed stays Beakn-driven. New shared helper `lib/webhooks/cartplus/apply-status.ts` (`applyCartplusOrderStatus`), called inside both the created and updated/status_changed handlers AFTER the quotation is written: **pending → QUOTATION_GIVEN**, **confirmed → ORDER_CONFIRMED** (inserts a `request_status_history` row → shows on /track + counts in Booked/orders), **cancelled → cancel** the request. **Forward-only** (advances only when current stage seq < target; never backward; no-op if already at/past). A pending/confirmed on a previously-cancelled request **reactivates** it first (supersedes HVA-282's blanket reactivation with an explicit status-driven rule). An order arriving already `confirmed` is created at ORDER_CONFIRMED. CartPlus confirm is the source of truth for confirmation (no separate Beakn captain confirm for that step; the seq-9 PENDING_CAPTAIN_APPROVAL gate near execution stays Beakn-driven). Tests: pending stays / confirmed advances + history / confirmed-on-create / forward-only no-revert; all CartPlus + metrics/finance/support suites green. (PR #309) |
| 2026-06-14 | HVA-284: CI parallel lanes + sharded Vitest | Sandeep: many small changes, can't wait 14–15 min per CI. Measured the single serial job — Vitest was ~9 min of ~14 (single-threaded, shared testcontainer). Rewrote `.github/workflows/playwright.yml` into 3 parallel jobs: **checks** (tsc+build), **vitest** (matrix shard 1/4..4/4, each runner its own container), **e2e**. Wall-clock ~5 min, full coverage intact (PR #308's own run finished in ~4 min on the new workflow). (PR #308) |
| 2026-06-14 | HVA-283: route CartPlus `order.updated` edit events | Walk-bug from Sandeep's testing: CartPlus fires **`order.updated`** for edits (price/qty/item add+remove) and `order.status_changed` for status flips — but the receiver only handled created/status_changed/cancelled, so every `order.updated` was stored `result='noop'` and **silently ignored** (prod evidence: order 187 edits to ₹28,709.95/₹29,608.95, order 183 → ₹6,450, all noop). One-line fix in `app/api/webhooks/cartplus/route.ts`: route `order.updated` through the same `handleCartplusOrderStatusChanged` (identical payload + intent — refresh header, soft-remove dropped items, reactivate a cancelled request). Regression test: an `order.updated` with a new total updates the quotation (result 'ok', not 'noop'). Note: pre-fix `order.updated` rows already stored as 'noop' won't auto-reprocess — re-saving the edit in CartPlus sends a fresh event. (PR #307) |
| 2026-06-14 | HVA-282: CartPlus order ↔ existing-request merge | Sandeep: a customer who raised a request (and holds a /track link) shouldn't lose sight of their order when the real quotation comes from CartPlus. **`handler-order-created.ts`** now, before creating a new request, looks for the customer's **newest open request under the same contact (by phone) that has no quotation yet and is at/before QUOTATION_GIVEN** — and MERGES the CartPlus order into it: attaches the portal quotation + line items, and advances status to QUOTATION_GIVEN with a proper `request_status_history` row (so the customer's existing tracking link + the /track timeline + transition metrics all reflect it). The existing request's exec assignment is left untouched. If no merge target exists (a **second** order — the first already claimed the request and now has a quotation — or a customer with no prior request), it falls through to creating a **new** request (original behaviour). So order #1 reuses the customer's link; orders #2, #3… each get their own request. Concurrent/duplicate safety preserved by the `quotations.visit_request_id` + `portal_quotation_id` unique indexes (collisions roll back → CartPlus retry self-heals). **Reactivation** (Sandeep 2026-06-14): a CartPlus `order.status_changed` arriving on a CANCELLED request now un-cancels it (clears `cancelled_at` + cancellation fields) — CartPlus updating an order means it's active again; a real cancel still comes via `order.cancelled` (guarded on the payload order.status). **Cache refresh**: the webhook route now `revalidatePath('/', 'layout')` on every successful handler so CartPlus changes appear immediately on the cached dashboards/finance/contact surfaces (request detail page was already force-dynamic). **Not yet wired:** the fresh WhatsApp tracking-link send for newly-created requests — blocked on a Meta-approved template (Sandeep's action); the request + token are created, only the auto-send is pending. **Tests**: `tests/api/cartplus-order-merge.test.ts` (merge into web request + status/history; 2nd order → new request; no-prior → new; cancel→status_changed reactivates). CartPlus suites green. (PR #306) |
| 2026-06-13 | HVA-281: quotation → target split | Sandeep: the exec's number on a request is a **Target** (a goal); the **actual quotation** (value + line items) is owned by CartPlus; finance counts CartPlus actuals only. **Schema** (migration 0075): `visit_requests.target_value_paise` (nullable) + backfill of existing manual quotations' value → their request's target. **Request page** (`collection-section.tsx`): the old "Add/Edit quotation" box is replaced by a **Target value** editor (new `TargetEditButton` → `POST /api/requests/[id]/target`, same authz as the old quotation route); the Quotation block is now **read-only, CartPlus-only** (`source='portal'`) with its line items read-only; Summary "Quoted" uses the portal quotation only. The old `quotation-edit-button.tsx` + `POST /api/requests/[id]/quotation` are now unlinked from the UI (left in place, finance-gated out — documented cleanup follow-up). **Finance gating** (`source='portal'`) applied to **every** quotation money-math site so dashboards/finance/reports all agree: SSOT `orders_value` / `quotations_count` / `quotations_value` / `outstanding`; captain `finance-queries.ts` (5 join sites) + dashboard **Pending Collections** + sidebar outstanding count; exec `target-progress.ts` (both monthly-orders meters); `contacts-queries.ts`; reports `sales.ts` / `team.ts` / `geography.ts`. Manual rows never enter money math — quotations table is CartPlus-only by construction going forward. **Contact page**: order query gated to the CartPlus actual + shows the CartPlus order number (`#…`) per order. **Audit**: `request_target_updated` event added to `lib/config-schema.ts` defaults + appended to the live `audit_enabled_events` row (migration 0076, dual-write rule). The old `quotation-edit-button.tsx` + `POST /api/requests/[id]/quotation` are unlinked from the UI (left in place, finance-gated out — documented cleanup follow-up). **Tests**: new `tests/finance-portal-gating.test.ts` (manual=0 vs portal counts vs manual→portal flip); money fixtures across the suite flipped to seed `source:'portal'` (manual quotations are targets now). (PR #305) |
| 2026-06-13 | HVA-280: CartPlus webhook hardening | Full integration audit (HMAC/route/record/handlers) ground-truthed vs the live DB; three verified bugs fixed. **H1 — stale line items:** `handler-order-status-changed` upserted incoming items but never removed ones the customer deleted in CartPlus, so the live quotation drifted. Now soft-removes absent items via new `quotation_line_items.removed_at` (no-deletes rule; dispatch FK refs would break on hard delete); a re-add clears the flag. "Current items" reads filter `removed_at IS NULL` — request-page `loadLineItems` + support dispatch queue. **H2 — failed events lost on retry:** the webhook_events row is written before the handler runs, so a transient handler failure made CartPlus's retry dedupe as 'duplicate' and the handler never re-ran (edit silently lost). `recordCartplusEvent` now returns the colliding row's result; the route reprocesses when result='error', short-circuits only on 'ok'/'noop'. **H3 — concurrent edits:** `SELECT … FOR UPDATE` on the quotation row in status_changed + cancelled handlers serializes same-order edits. Verified NON-issues: partial-unique indexes on portal_quotation_id + portal_line_item_id exist (dup quotations/line-items blocked + self-heal on retry); leads.phone dup race is the deliberate app-dedup design (documented, out of scope). Migration 0074: `removed_at`. Tests: 3 new (H1 remove+re-add, H2 reprocess-on-error + no-reprocess-on-ok); 71/71 across CartPlus+support suites unchanged. (PR #304) |
| 2026-06-12 | HVA-279: admin dashboard redesign — the missing picker added | The old Command Center had NO date control — everything hardcoded today-vs-yesterday. Now: **DashboardHeader** (shared with captain; new `subtitle` prop) carries the one from–to picker (365-day clamp validator, same as the other two portals); **Hero** = Collected for the window with delta vs the previous same-length period (uses captain's `resolveDateFilter` for compare windows + labels); **KPI strip** grows to 5 windowed tiles (new **Booked** via `orders_value`); **City cards** windowed ("N not started" sub-stat stays as-of-today and says so); **Revenue & pipeline panel**: "Received today" → "Collected" (windowed), "Completed today" → **"Delivered"** (windowed; explainer states it's the fulfilment milestone vs the Orders booking event — the A8 confusion), snapshot rows (Outstanding / Open quotation / Open requests / Pending approvals) wear AS-OF-NOW tags; **Alerts** tagged as-of-now. Query layer: `loadAdminGlobalMetrics/RevenueSnapshot/Counts` take a `DateRange`, `loadCityCards(range, istToday)`; `AdminGlobalMetrics` fields renamed window-neutral (visits/collectedPaise/bookedPaise/ordersCount/newRequests/productiveMinutes) + `bookedPaise` added; metrics-ssot test updated. Walk-verified: 31-day desktop (hero ₹12,345 + Booked ₹50K + city cards windowed + **Outstanding ₹37,655 proving the F6 fix**) and today-mobile at 390px. (PR #303) |
| 2026-06-12 | HVA-278: captain dashboard redesign — same one-picker model | **Performance card**: "Revenue" → "Collected" (net-cash tooltip) + new **"Booked"** tile (`orders_value` via SSOT — sales momentum vs cash reality side by side); **conversion denominator fix completed on the captain path** — `loadWindowAggregates` still divided by visit-TASKS after HVA-276 fixed the SSOT loader; now uses `loadVisitedRequestsCount` (new `visitedRequests`/`bookedRupees` fields in `WindowAggregates`, `booked` in `TeamPerformance`; regression test: 5 ticked tasks + 2 visited + 1 confirmed → 50% not 20%); no_target badge → gray dot (the badge truncated tile labels at 390px). **Pending Approvals/Collections**: AS-OF-NOW tags + explicit scope sublabels ("requests you've accepted" vs "accepted + unaccepted in your cities") + the approvals tooltip no longer describes the pre-HVA-168 dual semantics. **Exec rows**: the aged-rolled-over condition is now visible — badge says "⚑ stuck tasks" when there are zero overdue, and the expansion explains "carried over for more than 3 days" (`TeamExecStatus.hasAgedRolledOver` exposed). **Dates**: validator + picker widened 30→365 days with clamping (same silent-reset bug as exec); `DateRangePicker`/`DashboardHeader` take `maxDaysBack`. AsOfNowTag moved to `components/dashboard/` (shared with exec). Walk-verified at 390px: today = traffic lights + targets; 32-day = every tile recomputed, net collections, stuck-tasks badge. (PR #302) |
| 2026-06-12 | HVA-277: exec dashboard redesign — one picker rules every tile | Sandeep: "change every dashboard… the info has to modify every tile when we change the dates." New `/dashboard`: **WindowTiles** (8 tiles — Collected ₹ / Booked ₹ / Customers visited / Orders / Conversion / Quotations / Contacts captured / Tasks done — ALL driven by one from–to picker, every tile sublabeled in plain words, SSOT registry + 2 new window helpers `loadExecTaskWindowCounts` / `loadExecContactsCaptured`); **NextUpCard** ("What's next?" — day state + first pending task, AS-OF-NOW tag); **PendingOnMeCard** (overdue tasks / ₹ outstanding / warnings, AS-OF-NOW tag); TasksAccordion stays as THE action list; compact month-target + streak below. REMOVED: HeroMetrics, StatusBanner, Day Closure tile section, WeeklyReportCard (fixed last-7), BestOfPeriodCards, LeadsEnrolledCard (lifetime) — their numbers are now picker-driven. **Date handling**: picker + server validator widened 30→365 days back, and invalid/out-of-range dates CLAMP instead of silently resetting to today (the literal "I picked 31 days and it landed in today" bug). **F6 platform bug found while walking**: SSOT `outstanding` returned ₹0 for every scope on every portal — drizzle renders `${table.col}` as a BARE column inside raw-sql projections of no-join queries, so the correlated subqueries matched nothing; fixed via `${table}.id` qualification in `lib/metrics/outstanding.ts` + same hardening in city-drill + support orders queries; rendering rule pinned by test (see memory → drizzle-raw-sql-correlation). Tests: outstanding regression + window-query tests. Walk-verified at 390px: today = zeros + live cards; 32-day pick = every tile recomputes. (PR #301) |
| 2026-06-12 | HVA-276: dashboard calc fixes (HVA-275 audit fallout) | Sandeep delegated audit verdicts ("you decide, I veto"). Four calc bugs fixed: **(F1)** conversion % denominator is now DISTINCT requests entering VISIT_COMPLETED (was completed visit-TASKS — could read 200%); fixed in SSOT `lib/metrics/conversion.ts` (new `loadVisitedRequestsCount`) AND the exec day-close path (`lib/today/metrics.ts` + range aggregation in `exec-drill-queries.ts`, new `DayCloseMetrics.visitedRequests` field). **(F2)** captain per-exec Collections now net of refunds (same CASE as team Revenue — rows finally sum to the card). **(F3)** IST wrap (`AT TIME ZONE 'Asia/Kolkata'`) added to day-close `changed_at`/`submitted_at` date casts — post-midnight events no longer land on the previous day. **(F4)** day-close orders count ORDER_CONFIRMED only (legacy union with ORDER_EXECUTED made one request an order on two days); dead `ORDERS_STAGE_CODES` removed from captain queries. **(F5)** stale ⓘ explainers/tooltips rewritten (revenue/outstanding are net; conversion states its denominator). VISIT_COMPLETED added to `STATUS_CODES`; `lib/today` now imports VISIT_TASK_TYPES from the SSOT constants. **Tests**: 5 regressions in `tests/dashboard-calc-fixes.test.ts`, each named after the old wrong behaviour. 1077/1077. Number map + verdicts archived on HVA-275. (PR #300) |
| 2026-06-12 | HVA-274: request header phone → badge-scale pill | Sandeep: "the top phone number of customer is distracting. its too big and killing the UI. we have to make it small like city name." `StickyRequestHeader.tsx` only: the full-size tel Button row is gone; the phone now renders as a city-badge-scale rounded pill (mono text, phone icon, still one-tap `tel:`) in the badge row after the city badge. The freed second row now appears only when a primary action exists. Screenshot-verified at 390px. (PR #298) |
| 2026-06-12 | HVA-273: Quick Capture contacts — name + phone, rapid-fire | Sandeep ask; 4 design decisions locked via AskUserQuestion (quick-first, stay-open rapid-fire, exec-city silent, duplicate-tell-and-link). **Server** (`app/(exec)/leads/_actions/addLead.ts > quickAddLeadAction`): name (2..100) + 10-digit phone only; type=Customer, interests=[], city resolved from `sales_executives.city_id` (helpful error if unset); **duplicate phone** (the project dedup key) returns `error='duplicate'` — with `fieldErrors.dupLeadId/dupName` when the existing lead was captured by the SAME exec, withheld otherwise (no identity leak across the captured-by visibility boundary); fieldErrors doubles as the typed side-channel because useServerMutation already forwards it. **Sheet** (`AddLeadSheet.tsx`): opens in `view='quick'` (Name + Phone + Save contact + Close); "Add full details" switches to the classic full form carrying over typed values; Save keeps the sheet open — fields clear, emerald "Saved ✓ {name} — next one?" strip, focus back to Name; amber duplicate strip with "Open contact →" link (or a generic registered-with-Beakn message for non-visible duplicates). Deliberate deviation from the HVA-150 optimistic pattern documented in-code: quick saves are awaited (sub-second) with a disabled-button busy state instead of juggling multiple in-flight temp rows in a sheet that stays open. **Tests**: 5 new in `tests/exec-leads/quick-add.test.ts` (happy path incl. city auto-fill + Customer defaults; same-exec duplicate exposes id+name and creates no second row; other-exec duplicate withholds identity; phone-shape rejection; captain refused). 24/24 across the leads suite. **Walked** via a throwaway Playwright run — quick view, saved-strip, and duplicate-link states all screenshot-verified before ship (which also caught a stale-build false start). |
| 2026-06-11 | HVA-269: Framer Motion phase 2 — queue exits + dashboard stagger | Sandeep approved phase 1's feel ("the feel is right. its smooth"). **Kit**: new `AnimatedLi` primitive (motion.li twin of AnimatedItem so ul>li semantics + screen readers stay intact; same spring + reduced-motion fallback). **Surfaces**: tickets queue rows (`TicketsQueueClient`) collapse out when claim/resolve moves them off the current filter, survivors glide up; captain approvals rows (`ApprovalsListClient`) melt away on approve/reject (incl. bulk); captain dashboard cards rise in with 60ms stagger via FadeRise client wrappers around UNTOUCHED server-rendered card children (RSC architecture preserved). **Gate**: tsc, build, 11/11 e2e (golden journey exercises the animated tickets queue end-to-end; captain-workflow exercises approvals surface). |
| 2026-06-11 | HVA-267: Framer Motion phase 1 — motion kit + exec Today list + bottom-nav pill | Sandeep approved framer-motion (12.40.0) after the bundle trade-off was presented — decision recorded in memory (feedback_framer-motion-approved). **Kit** `components/motion/motion-kit.tsx`: AnimatedList/AnimatedItem (AnimatePresence enter/exit + layout glide, spring 420/32), FadeRise, all collapsing to zero-motion under prefers-reduced-motion; house rule: pages use these primitives, never raw motion.* configs; client components only. **Surfaces**: exec bottom nav active pill now SLIDES between tabs via shared layoutId (static fallback under reduced motion); Today "other tasks" rows rise in on add and height-collapse out on done/postpone/promotion with survivors gliding (works with the HVA-150 pending-row pattern — pending rows render at 70% opacity inside AnimatedItem). **Later phases**: tickets queue + requests lists; captain approvals exit + dashboard stagger. **Gate**: tsc, build, 11/11 e2e incl. golden journey + exec daily loop with animations live. |
| 2026-06-10 | HVA-262: video pilot — recorded Today-loop walkthrough in the exec manual | Sandeep approved a 1-video pilot before committing to all 6 manual clips. **Harness**: `playwright.videos.config.ts` (testDir `tests/videos` — CI's default `pnpm test:e2e` never sees it; phone-portrait 390×844 viewport, `video: on` pinned to viewport size, slowMo 600ms) + `tests/videos/today-loop.record.spec.ts` (camera-operator spec: login → Start My Day → add task → mark done with outcome → /today/close → confirm close; `pause()` beats give viewers time to read each screen; handles both chips and free-text outcome shapes). Runs through the existing run-e2e runner via `pnpm test:e2e --config=playwright.videos.config.ts` (arg passthrough; testcontainer + seed + :3100 server reused). **Publish**: `scripts/publish-videos.sh` converts each `video.webm` to H.264 mp4 (`-pix_fmt yuv420p -movflags +faststart -crf 26` — iPhone Safari can't play Playwright's webm; host has ffmpeg) and writes to `/var/www/docs/videos/` THROUGH the filebrowser container (`docker exec -i filebrowser sh -c 'cat > /srv/var/www/docs/...'`) because /var/www/docs is root-owned and caddy's mount is read-only; filebrowser mounts host / read-write at /srv. Served at docs.1site.ai/videos/. **Pilot result**: first take passed (35.3s run, 32.4s clip, 912KB webm → 244KB mp4); frames verified via ffmpeg extraction (Start-My-Day pressed, task added toast, outcome confirm, close-day dialog all visible and readable). Embedded in the manual's Daily Loop section as a `<video controls playsinline>` block (both the beakn-docs editable source and the published copy). **Manual home**: source `/var/www/beakn-docs/sales-exec-user-manual.html` (beakn-writable), published `https://docs.1site.ai/sales-exec-user-manual.html` (republish via the same filebrowser trick). **Remaining 5 clips** (login/install, requests+status+schedule, quotation+payment, tickets, contacts/assist) pending Sandeep's pilot verdict. HVA-263 (in-app "?" help button) queued behind. |
| 2026-06-10 | HVA-261: golden journey e2e — the core business flow protected end-to-end | Sandeep ask after the test-coverage discussion: "one automated journey test." **Spec** `tests/e2e/golden-journey.spec.ts` (serial, desktop project only — tablet/mobile exclude it via `testIgnore` because the journey MUTATES its seeded request and would race itself across viewport projects): (1) captain assigns the seeded SUBMITTED request to the exec via the unassigned queue + Assign modal; (2) exec schedules the visit via the calendar dialog (datetime-local fill + Schedule confirm, asserts the status badge flips); (3) exec adds a ₹50,000 quotation on the Order tab; (4) exec records a ₹20,000 UPI payment; (5) customer opens /track/[token] with NO login and sees the scheduled status + support entry point; (6) customer raises a support ticket through the real dialog (subject + description + Turnstile); (7) exec claims it on /tickets (Take this → In progress chip → Resolve); (8) customer sees Resolved + the reopen button on /track. **Turnstile**: CI's build now bakes Cloudflare's official always-pass TEST site key (`1x00000000000000000000AA` in playwright.yml) pairing with the always-pass test secret the e2e runner already defaulted to — so steps 6–8 run fully in CI. A bundle built with a REAL key (e.g. local prod build) can't pass headlessly: step 6 detects the challenge-blocked toast and soft-skips 6–8 via a shared `ticketCreated` flag — the pipeline half (1–5) must always pass. **Seed**: `tests/setup/e2e-seed.ts` adds `journeyRequest` (Journey Customer, SUBMITTED, fully unassigned, own tracking token) reserved for this spec; other specs must not touch it. **Verified locally**: 8/8 pass in 20.6s with the test key baked; 5 pass + 3 soft-skip with the real key; full e2e suite 32 passed with 3 mobile login-timeout flakes that pass in isolation (VPS resource contention from 3 viewport projects + the serial journey on one box — CI runners have more headroom; watch the first CI run). **Gotchas learned**: `test.skip(callback)` can't read `testInfo.project.name` (fixtures only) — project exclusion belongs in playwright.config `testIgnore`; seeded names with parentheses break naive `new RegExp(name)` toast matchers. |
| 2026-06-10 | HVA-260: finance sync — Pending Collections card matches the Finance page | Sandeep decision 2026-06-10 after the finance-consistency discussion: card and page must always show the same number. **Change** (`lib/captain/dashboard-queries.ts`): `loadPendingCollections` was scoped by team-exec roster (`loadCaptainTeamIds` → `assigned_exec IN (...)`), which missed CartPlus orders whose `created_by` had no exec mapping — those create a quotation immediately, so the Finance page (captain-visibility scope) showed money the dashboard card didn't. Card now uses the page's exact scope: `buildCaptainRequestVisibilityWhere(captainUserId, { captainCityIds })` = accepted-by-me OR unaccepted-in-my-cities. Internals refactored to `loadPendingCollectionsScoped(scopeWhere, filter)` (same pattern as HVA-258's approvals fix); `loadPendingCollectionsForExecIds` unchanged for the admin city drill. Sidebar outstanding count verified already on the same visibility scope — all three finance surfaces now agree. **Regression test** pins the three cases: accepted no-exec order counted (the CartPlus gap), normal accepted order counted, other captain's order excluded. **Locked business rules** recorded in memory (`project_finance-source-of-truth.md`): HVA is the only money source of truth (payments always recorded manually; never import CartPlus payment_status); cities never change (business-level definition); captain accounts are company seats that survive staff turnover (new captain inherits the same phone + portal account). **Pre-PR gate**: tsc clean, build clean, 20/20 dashboard-queries + 37/37 across dashboard/finance-snapshot/sidebar-counts before the new test was added. |
| 2026-06-09 | HVA-259: bug-audit hardening — closes out the 2026-06-09 audit | LOW-severity batch, final PR of the audit (HVA-257 HIGH → HVA-258 MEDIUM → this). **(1) CartPlus money validation** (`lib/webhooks/cartplus/order-payload.ts`): `total_amount`/`unit_price`/`line_total` accepted arbitrary floats and `Math.round(x*100)` silently rounded malformed amounts like 123.456; new shared `rupeeAmount` zod refinement rejects >2 decimal places (1e-6 epsilon soaks float representation noise so 1234.56 still passes). **(2) Schema-drift: 4 partial indexes** that exist in migrations but weren't declared in Drizzle — `support_tickets_claimed_by_idx` (WHERE claimed_by IS NOT NULL), `webhook_secrets_active_idx` (WHERE revoked_at IS NULL), `users_portal_exec_id_unique_idx` + `cities_cartplus_store_id_unique_idx` (partial UNIQUE WHERE col IS NOT NULL) — all now declared with `.where(...)` so a future drizzle-kit introspect won't flag them and the type layer knows the uniques exist. No migration needed (DB already correct). **(3) Rate-limit ordering** (`app/api/customer/support-tickets/route.ts`): the attempt row was inserted before the limit check, so rejected attempts also counted and users were blocked one submission earlier than the stated 5/24h; insert now conditional on `n < RATE_LIMIT_MAX`. **(4) ticket-categories admin client** converted from hand-rolled `await action` + `router.refresh()` to `useServerMutation` (two hooks: create + update; onSuccess closes the dialog) per the HVA-149 standard. NOTE: `announcement-categories-client.tsx` has the same hand-rolled pattern — left untouched (predates the audit, working) but flagged here for a future cleanup. **(5) Count-query symmetry** (`lib/exec/tasks-page-queries.ts`): completed-tasks count query now carries the same 3 LEFT JOINs as its rows query — joins are 1:1 today so no behavior change, but the asymmetric pair would silently diverge the moment whereExpr gains a condition on a joined column. **Tests**: 49/49 across cartplus webhook/handlers/admin + customer-support-tickets + exec tasks-page-queries; tsc + build clean. **Audit summary**: 14 confirmed findings → all fixed (3 PRs); 3 false positives discarded during verification. |
| 2026-06-09 | HVA-258: bug-audit MEDIUM fixes — captain scope alignment + audit backfill + proxy gate | From the 2026-06-09 bug audit, plus a live prod report that landed mid-fix and turned out to be the same bug class. **(1) Request-detail captain gate team-scoped** (`app/requests/[id]/page.tsx`): was `cityCaptainUserId === me` only — bounced even the request's OWN assigned captain whenever the city had no captain (the "Other" city has captain_user_id NULL) or belonged to another captain. Sandeep hit this live: request 019e60de…5c31 (city "Other", assigned captain Arjun) silently redirected to /captain/dashboard?denied=1 — "completely not visible". Gate now allows: assigned captain = me, OR assigned exec reports to me, OR I own the city (kept for the unassigned/SUBMITTED routing flow). **(2) Dashboard Pending Approvals card team-scoped** (`lib/captain/dashboard-queries.ts`): card counted via cities.captain_user_id while the /captain/approvals page it links to uses buildCaptainRequestVisibilityWhere (assigned_captain = me) — counts disagreed for captains sharing cities. Internals refactored to `loadPendingApprovalsScoped(scopeWhere)`; card passes the team-scope WHERE; `loadPendingApprovalsForCityIds` unchanged for the admin city drill. Existing tests updated to set assigned_captain_user_id (realistic: a request at PENDING_CAPTAIN_APPROVAL always has an accepting captain); new regression test pins captain-A-sees / captain-B-(city-owner-only)-doesn't. **(3) Audit dual-write backfill** (migration 0073): `city_config_updated` was in config-schema defaults + emitted by the cities admin route but never appended to the prod audit_enabled_events row → every city-config audit event silently dropped on prod since the event shipped. Idempotent CASE-append like 0068–0072. **(4) proxy.ts layer-1 gate**: /tickets added to the exec-only block (captain has /captain/tickets, admin /admin/tickets; super_admin escape-hatched above the block). **(5) Metrics scope fallback** (`loadWindowAggregates`): `scope = {}` fell back to GLOBAL aggregation if the team lookup missed — unreachable today by construction but the wrong failure mode; now degrades to single-exec scope. **Tests**: 62/62 across proxy + captain-dashboard + captain requests-page; tsc + build clean. |

_Showing the 25 most recent ships. Older history → [docs/STATE-archive.md](docs/STATE-archive.md) (166 entries)._


For tickets older than 2026-05-16, see Linear archive (search project: Phase 1 — MVP Launch).

---

## Queued (not yet started)

- **HVA-151** — Playwright visual regression (Phase 2 candidate)

---

## Cleanup tickets to file (from recon)

(All STATE.md-tracked cleanup tickets shipped in HVA-192/193/194/195/196.)

---

## Blocked

WhatsApp provider activation (Libromi) shipped HVA-45 (2026-05-31). 8 customer-facing Utility templates Meta-approved on 2026-05-31 and wired through the engine in HVA-46 + HVA-47 (2026-06-01) — see Recently shipped. Remaining WhatsApp-dependent items below still need work:

- **HVA-49** — internal exec/captain WhatsApp notifications (not yet drafted as Meta templates)
- **HVA-50** — admin-editable rule UI for notification_rules
- **HVA-79** — opt-in checkbox on customer /request form
- **HVA-155 Part C** — 9:30 PM WhatsApp day-close reminder

---

## Frozen / pending real-world data

- **HVA-170 extensions** — Additional task management ideas (Move/Duplicate buttons beyond what shipped, dedicated /tasks-vs-dashboard architecture) frozen pending 2–3 days of real-world usage data on /tasks page (shipped 2026-05-22). Most ideas in the original placeholder may turn out to be unnecessary or contradict auto-roll-over behavior.
- **HVA-165** — Contact merge flow. Deferred pending real-world team usage of the contacts system. Sandeep's call 2026-05-23: ship nothing speculative; if the team hits duplicate-contact pain (mistaken double-entries, phone-format drift, business-vs-customer split for the same human) the schema + UI design should be informed by the actual duplicate patterns, not invented up-front. Revisit when concrete duplicate cases surface.

---

## Phase 2 (deferred)

- SSE / real-time updates (HVA-55)
- Optimistic UI (HVA-150)
- Playwright visual regression (HVA-151)
- Push notifications via VAPID (env vars exist in `.env.example`, not wired)
- WhatsApp via Interakt (env vars exist, not wired)
- Multi-language
- AI report cards (Phase 1 spec §11 — no ticket yet)
- Advanced analytics (forecasting, predictive)
- Mark Exec Unavailable toggle (HVA-85) — may lift if operationally needed
- Rolling-deploy / zero-downtime cutover (current rebuild has a few-second gap)
- Containerised drizzle-kit migrator (eliminates host-side override)
